{"title":"Presentando mis datos: Dashboards (Shiny) y apps web","markdown":{"yaml":{"bibliography":["bib/references.bib","bib/packages.bib"]},"headingText":"Presentando mis datos: Dashboards (Shiny) y apps web","containsRefs":false,"markdown":"\n\n```{r, echo=FALSE, message=FALSE, warning=FALSE}\nlibrary(ggplot2)\nlibrary(ggdist)\nlibrary(moments)\nlibrary(fitdistrplus)\nlibrary(nortest)\nlibrary(tseries)\nlibrary(fBasics)\nlibrary(dplyr)\nlibrary(tidyr)\nlibrary(tibble)\nlibrary(purrr)\nlibrary(scales)\nlibrary(lubridate)\nlibrary(vroom)\nlibrary(zoo)\nlibrary(palmerpenguins)\n```\n\n\nComo no podía ser de otra manera, vamos a culminar este viaje en el entendimiento del análisis de datos con la creación de reportes presentables a otras personas. Cuando nosotros hacemos cálculos en los datos que hemos creado, creamos resúmenes de tablas, gráficos o incluso mapas, no podemos solo soltar esa información en crudo al púbico objetivo (coordinadores de otras áreas no relacionadas a datos, ejecutivos, o el público general). Siempre debemos buscar crear una visualización general de la información que estamos transmitiendo, procurando que esta sea amigable y entendible para las demás personas. Uno mismo como analista de datos entiende qué significan los cálculos realizados en un data frame, o los coeficientes devueltos por un modelo estadístico/modelo de ML, lo que es lógico al ser nosotros quienes creamos el proceso, pero ¿y las demás personas? ¿Cómo podemos validar, por ejemplo, ante un jefe de área, que un proyecto en la empresa no es viable?, ¿o que una cuenta tiene el potencial para duplicar los ingresos? Todas estas respuestas pueden ser respondidas con la creación de un dashboard (tablero) o un reporte.\n\n¿Qué diferencia hay entre un dashboard (tablero) y un reporte? Básicamente, un dashboard tiene el propósito de mostrarnos múltiples métricas y datos fáciles de entender en una sola vista, lo que nos ayudará a tener un conocimiento más general de un negocio, área o tópico específico, mientras que un reporte será mucho más específico, procurando explicar ciertas métricas o resultados obtenidos de una forma mucho más meticulosa.\n\nEl capítulo final del presente libro tendrá como objetivo las siguientes dos tareas:\n\n1.  Entender, crear, customizar y llevar al límite los dashboards creados mediante la librería `shiny` de R, así como explotar el funcionamiento de estos programas no solo con la creación de tableros de datos, si no para otras situaciones, como la automatización de tareas.\n2.  Comprender cómo estructurar aplicaciones creadas con shiny, desde la organización de sus archivos hasta la construcción de interfaces, servidores, módulos, filtros, gráficos y tablas interactivas. Este libro no tiene como objetivo revisar únicamente ejemplos básicos, por lo que buscaremos entender cómo convertir nuestros códigos de R en aplicaciones funcionales, reutilizables y relativamente complejas.\n\nSin más que añadir a la presentación del capítulo final, ¡comencemos con ello!\n\n## Shiny\n\nEl método por excelencia para crear dashboards diréctamente en R es a través de la librería `shiny`, la cual nos brindará las herramientas no solo para crear el tablero que necesitemos, sino también para customizarlo, añadirle todas las funcionalidades que podamos imaginar, combinarlo con múltiples librerías e incluso con otros lenguajes de programación, como JavaScript.\n\nPropiamente, la librería `shiny` no se enfoca únicamente en la creación de un dashboard, en realizad se utiliza para crear **aplicaciones web**. Una aplicación web no es más que un programa que, a partir de ciertos parámetros establecidos por el usuario, le devuelve un resultado en específico. Es importante que sepamos diferenciar a las aplicaciones web de dashboards como los creados en Power BI. Mientras que en este último solo podremos crear informes basados en datos y, como mucho, exportar los datos en crudo a un archivo excel, una aplicación web es más maleable y versatil. Con `shiny` no solo podemos crear dashbaords, también podremos crear procesos de automatización, vistas de modelos estadísticos, e inclusive creación en tiempo real de reportes en, por ejemplo, formatos PDF, excel, etc. El límite de uso para una aplicación web solo será el que te permita tu imaginación.\n\n### ¿Cómo funciona una *Shiny app*?\n\nUna *Shiny app* se basa en **inputs** (información de entrada) y **outputs** (resultados), lo que dicho de otra manera, implica que a partir de los parámetros de entrada que el usuario introduzca (input), el programa va a generar los cálculos pertinentes en el *backend*[^dashboards-1] para devolver un resultado (output), el cual puede ser de cualquier tipo, como una cadena de texto, un número, un gráfico, una tabla o una simple notificación [@R-shiny]. Gráficamente:\n\n[^dashboards-1]: El *backend* es la parte de una aplicación o programa que el usuario no ve, donde se ejecutan los códigos para devolver los cálculos finales.\n\n![Lógica de una shiny app](imagenes/inputoutput.pdf){#fig-relacion fig-align=\"center\" width=\"440\"}\n\nLa app que construyamos siempre va a incluir estos tres elementos:\n\n1.  **Información global**: Los datos que vamos a cargar, las librerías a utilizar y las opciones generales que vamos a establecer por default.\n2.  **UI**: Conocido también como el *User Interface* o interfaz de usuario (conocida también como *frontend*), será el lienzo sobre el cual nosotros customizaremos el programa, le daremos estilo y mostraremos tanto los *inputs* como los *outputs*.\n3.  **Server**: El servidor de nuestro programa o *backend*, donde se establecerá la lógica de los cálculos de nuestro programa, para poder mostrar los *outputs* en el *UI*.\n\nA pesar de que estos elementos pueden ir en el mismo archivo o en archivos separados, siempre serán lo mínimo necesario para poder crear una aplicación web. Más adelante veremos elementos adicionales, cómo los módulos, llamado a códigos específicos, etc, pero por el momento nos centraremos en estos tres pilares fundamentales.\n\n### Comenzando a construir mi primer programa\n\nGeneralmente, el orden lógico para construir nuestro programa será:\n\n$$\n Global \\rightarrow UI \\rightarrow Server\n$$\n\nEn una sección global, típicamente comenzaremos cargando las librerías necesarias, por ejemplo:\n\n```{r, eval=FALSE}\nlibrary(shiny)\nlibrary(dplyr)\n\noptions(...)\n\ndatos_generales <- read.csv(\"misdatos.csv\")\n```\n\nPara después pasar a diseñar la interfaz de usuario.\n\nEn ciertos casos tendremos que crear una carpeta adicional dentro del directorio del projecto, denominada **www**. Esta carpeta tendrá como objetivo el guardar elementos adicionales que después vamos a referenciar en la app, como pueden ser imágenes, logotipos, código HTML/CSS, etc. Más adelante veremos algunos ejemplos de uso de esta carpeta.\n\n### UI\n\nUna interfaz de usuario típicamente estará contenida dentro de una función `fluidPage()`, la cual no es más que un contenedor basado en HTML. De hecho, si nosotros ejecutamos de forma separada la función, podremos notar que el resultado es código en HTML:\n\n```{r}\nlibrary(shiny)\n\nas.character(fluidPage())\n```\n\nUsualmente las funciones que más vamos a utilizar para customizar nuestra interfaz son:\n\n-   `fluidPage()`: Para crear páginas que se adapten al tamaño de la pantalla (ya sea de computadora o celular).\n\n-   `fixedPage()`: Para crear páginas basadas en un tamaño fijo de pantalla.\n\n-   `navbarPage()`: Para crear barras de navegación. Usualmente va dentro de funciones como `fluidPage()`, `fixedPage()` o `bootstrapPage()`.\n\n-   `column()`: Para crear contenedores `div` dentro de un contenedor (`fluidPage()`, por ejemplo) de tipo columna. Requiere establecer el tamaño de la columna mediante el argumento `width`, con valores que van de 1 a 12 (12 para abarcar toda la pantalla).\n\n-   `fluidRow()`: Para crear filas en las que se pueden introducir de forma alineada columnas.\n\nExisten otros tipos de contenedores más enfocados a dashbaords, los cuales estarán proporcionados por librerías como `shinydashboard`, `shinydashboardPlus`, `bs4Dash`, `argonDash`, entre otros, pero esas dependencias serán analizadas más adelante con detalle. Por ahora comenzaremos por dar un entendimiento a la forma en la que funciona la customización básica de una aplicación web.\n\nEmpecemos creando un contenedor simple con `fluidPage()`. Supongamos que en ese contenedor queremos introducir 4 columnas diferentes, por lo que, sabiendo que el ancho máximo de una columna es 12, entonces tendremos que dividir ese número entre 4 para poder generar esas columnas en la misma línea, es decir:\n\n```{r, eval=FALSE}\nfluidPage(\n  column(width = 3), column(width = 3), column(width = 3), column(width = 3)\n)\n```\n\nSi en lugar de establecer una anchura igual a *3*, esta fuera igual *4*, entonces la última columna se enviaría debajo del resto de contenedores, esto ya que la suma de anchos de las primeras tres ya daría el valor máximo de *12*.\n\nEs posible anidar contenedores `column()` para crear subsecciones dentro de nuestro programa. En un caso en el tengamos una columna con un ancho de, por ejemplo, *6*, si creamos otra columna dentro de la primera, esta última va a interpretar el espacio como su 100% de hoja, por lo que si nosotros establecemos un `width = 12` en la columna secundaria, esta tendrá el tamaño total de la columna superior con `width = 6` (manteniendo un margen de espaciado). Si estableciéramos el ancho de la columna secundaria en `width = 6`, esto sería el equivalente a un `width = 3` para una columna exterior, debido al ancho del contenedor superior. Gráficamente:\n\n![Subcolumnas](imagenes/width.pdf){fig-align=\"center\" width=\"400\"}\n\nEl código en este caso es bastante sencillo, ya que solo escribiremos columnas dentro de otras:\n\n```{r, eval=FALSE}\nfluidPage(\n  column(\n    width = 6,\n    column(width = 6),\n    column(width = 6)\n  )\n)\n```\n\nAdemás del espaciado y funciones básicas de HTML, nosotros también podemos aplicar estilo CSS a una aplicación web, por ejemplo: Por default, una columna siempre alineará los elementos contenidos dentro de ella, como textos o gráficos, a la izquierda, por lo que podemos aplicar una justificación con el argumento `align = \"center\"`, es decir:\n\n```{r, eval=FALSE}\nfluidPage(\n  column(width = 6, align = \"center\")\n)\n```\n\nAsí como cambiar parámetros de estilo, como el color de fondo, por ejemplo:\n\n```{r,eval=FALSE}\nfluidPage(\n  column(width = 6, align = \"center\",\n         style = \"background-color: red\")\n)\n```\n\nVayamos a un ejemplo más interesante: Supón que queremos crear una página donde el título se encuentre centrado hasta arriba, existan dos columnas y, dentro de la segunda columna, existan dos secciones, una debajo de la otra, es decir, lo visto en la figura 9.2. Para crear esta vista, simplemente tendremos que anidar los contenedores de tal forma que podamos establecer dicha vista. En este caso, las columnas diseñadas aplicadas de la siguiente forma:\n\n```{r, eval=FALSE}\nfluidPage(\n    column(width = 12, align = \"center\", h1(\"Título del programa\")),\n    column(width = 6, align = \"center\", p(\"Primer columna\")),\n    column(width = 6,\n      column(\n        width = 12, align = \"center\", p(\"Primer subcolumna\"), \n        style = \"background-color: #c96b73\"\n      ),\n      column(\n        width = 12, align = \"center\", p(\"Segunda subcolumna\"),\n        style = \"background-color: #70c96b\"\n      )\n    )\n  )\n```\n\n![Interfaz simple](imagenes/interfazusuario.pdf){fig-align=\"center\" width=\"380\"}\n\nSi nosotros cargamos esta interfaz en nuestro navegador (o el visualizador local de RStudio), el aspecto de esa vista será:\n\n![Vista de una interfaz](imagenes/uiprueba.png){fig-align=\"center\" width=\"420\"}\n\nNótese que, para introducir el título, usamos otro tipo de contenedor HTML `h1()`, mientras que para textos de cada columna, usamos un contenedor simple de texto `p()`. Además de lo anterior, también podemos notar que el texto de la segunda subcolumna se encuentra prácticamente pegado al de la primera, esto ocurre ya que, al no existir elementos dentro del espaciado entre ambas columnas, el contenedor de texto se introducirá en la parte superior. En cuanto añadamos algunos otros *inputs* y *outputs*, podremos comenzar a ver la diferencia entre espacios de cada contenedor.\n\nComo ya te habrás dado cuenta, una ventaja notable del uso de shiny para crear apps web es la facilidad para crear una interfaz muy rápidamente. Si nosotros tradujéramos este código a HTML, tendríamos:\n\n```{r, eval=FALSE}\n<div class=\"container-fluid\">\n  <div class=\"col-sm-12\" align=\"center\">\n    <h1>Título del programa</h1>\n  </div>\n  <div class=\"col-sm-6\" align=\"center\">\n    <p>Primer columna</p>\n  </div>\n  <div class=\"col-sm-6\">\n    <div class=\"col-sm-12\" align=\"center\">\n      <p>Primer subcolumna</p>\n    </div>\n    <div class=\"col-sm-12\" align=\"center\">\n      <p>Segunda subcolumna</p>\n    </div>\n  </div>\n</div>\n```\n\nAlgo más complicado de interpretar para alguien que no tenga mucha experiencia con la creación de páginas web.\n\nRegresemos a la pantalla que estamos creando.\n\n#### Inputs\n\nUna vez establecido el diseño de nuestra app, podemos comenzar a introducir los elementos con los que el usuario va a interactuar, como lo son selectores y botones. Estos inputs serán una forma de interactuar dinámicamente con las funciones del servidor. Los principales *inputs* que vamos a utilizar serán:\n\n-   `selectInput()`: Para seleccionar un elemento de una lista desplegable.\n-   `numericInput()`: Para introducir valores numéricos como inputs.\n-   `textInput()`: Para introducir cadenas de texto como inputs.\n-   `sliderInput()`: Para introducir valores numéricos dentro de un rango establecido.\n-   `dateInput()`: Para introducir fechas individuales.\n-   `dateRangeInput()`: Para introducir rangos de fechas (fecha inicial y fecha final).\n-   `fileInput()`: Para subir archivos como inputs (Ej. Documentos excel o csv).\n-   `checkboxInput()`: Para retornar valores booleanos como inputs.\n-   `radioButtons()`: Para seleccionar un elemento concreto. Recomendable en lugar de un `selectInput()` cuando tenemos pocos elementos a escoger.\n-   `actionButton()`: Para retrasar la renderización de un output hasta dar click a un evento (botón).\n\nEstos inputs son las versiones nativas de la librería `shiny`, sin embargo, existen versiones mucho más customizables y amigables a la vista, provenientes de la librería `shinyWidgets`, por ejemplo: `pickerInput()` como equivalente de `selectInput()`, o `airDatepickerInput()` como equivalente de `dateInput()` y `dateRangeInput()` (si tenemos el parámetro `range = TRUE`), entre otros elementos. Poco a poco iremos viendo cada uno de ellos.\n\nAhora, ¿cómo usamos estos *inputs* para interactuar con el servidor? Dependiendo de cada input, se requerirán ciertos parámetros específicos, por ejemplo, en el caso de un `selectInput()`, tendremos que añadir las opciones a escoger dentro de un vector, mientras que un `textInput()` no requerirá que añadamos necesariamente ninguna opción (aunque si podemos establecer una sugerencia predefinida), ya que este último tiene la intención de aceptar un texto libre como input. A pesar de esas diferencias, todos los *inputs* necesariamente requerirán un elemento: El **InputID**. Un InputID no es más que un nombre único que utilizaremos como identificador para llamar del lado del servidor y que, en consecuencia, permita obtener correctamente las selecciones o parámetros que el usuario haga con ese input específico, por ejemplo:\n\n```{r, eval=FALSE}\nselectInput(\n  inputId = \"mis_opciones\",\n  label = \"Opciones\",\n  choices = c(\"Opcion 1\", \"Opcion 2\", \"Opcion 3\")\n)\n```\n\nEn este caso, para nosotros poder llamar a este input en el lado del servidor, lo haremos mediante el código `input$mis_opciones`. Necesariamente debemos hacer uso de `input$` para seleccionar el objeto en cuestión, ya que de esta forma el programa identifica que se trata de un input dinámico y no de un objeto preestablecido en la sesión.\n\nLos *inputs* (dependiendo del caso) devuelven las respuestas seleccionadas como un vector, por lo que si hay una selección múltiple en alguno de ellos, lógicamente podremos acceder a cada uno de sus elementos mediante indexación, por ejemplo, `input$mis_opciones[1]`. Más adelante veremos las implicaciones de vectores múltiples del lado del servidor y cómo trabajarlos correctamente.\n\n#### Outputs\n\nAsí como los inputs son solo una cara de la moneda, siendo *aquello que el usuario introduce como parámetro(s)*, podemos definir a los *outputs* como aquellos elementos que serán retornados a partir de la información establecida, siendo resultados dinámicos con los que podremos interactuar en mayor o menor medida, dependiendo del tipo de elemento devuelto. Dicho en otras palabras, un *output* no es mas que el objeto resultante de todo el procesamiento de información realizada en el *backend*, a partir de los parámetros que se hayan introducido. Típicamente, los *outputs* en aplicaciones web suelen ser: cadenas de texto, gráficos, mapas y elementos interactivos (*outputs* de los que podemos obtener más información), y estarán en función de lo que queramos mostrar en nuestro programa.\n\nEstos objetos resultantes son mucho más sencillos de entender dentro del `UI` que sus contrapartes (*inputs*), ya que suelen estar basados únicamente en tres parámetros: *OutputID* (identificador único del elemento resultante que queremos mostrar), *width* (anchura del elemento) y *height* (altura del elemento). Veamos un ejemplo:\n\n```{r, eval=FALSE}\nfluidPage(\n  column(\n    width = 3,\n    selectInput(\n      inputId = \"mis_opciones\",\n      label = \"Opciones\",\n      choices = c(\"Opcion 1\", \"Opcion 2\", \"Opcion 3\")\n      )\n  ),\n  column(\n    width = 9,\n    plotOutput(\"mi_grafico\", width = \"400px\", height = \"600px\")\n  )\n)\n```\n\nDel código mostrado anteriormente, podemos darnos cuenta de que cada elemento, ya sean *inputs* o *outputs*, tiene un identificador único, con el que podremos interactuar con esos elementos en particular, pero ¿cómo exactamente logramos que las entradas y salidas de nuestro programa se conecten entre si? Aquí es donde entra en juego el corazón de nuestro programa: **El servidor** o **server**.\n\n### Server\n\nHasta ahora, hemos visto rápidamente cómo funciona una interfaz en shiny, así como el manejo que podemos hacer para customizar la aplicación que el usuario final va a ver en su computadora, pero esta sección nos ayudará a entender cómo funciona la magía en nuestra programa, es decir, la forma en la que haremos que los parámetros establecidos nos den un resultado específico. Al principio será un tema bastante sencillo, pero poco a poco se irá complicando, por lo que procuraré ser lo más simple posible en las explicaciones de la lógica utilizada en esta parte.\n\nPrimero, comencemos por entender el servidor en si. Un código o *script* en R no es interactivo en si, ya que necesitamos establecer objetos y valores para obtener resultados, ¿cómo podemos hacerlo dinámico? Simplemente convirtiendo ese *script* en una función, donde los objetos y valores sean parámetros de ella. El servidor de una aplicación web no es más que una función con dos parámetros: *input* y *output*, principalmente. Habrá aplicaciones en las que establezcamos un tercer parámetro: *session*, el cual veremos con detalle más adelante, cuando profundicemos en la reactividad del servidor.\n\nTípicamente, el servidor se verá de la siguiente manera:\n\n```{r, eval=FALSE}\nserver <- function(input, output){\n  \n}\n```\n\n#### Conectando la interfaz: Reactividad\n\nComo recordarás, anteriormente te mencioné que la forma en la que nosotros podremos acceder a un elemento de un *input* o un *output* será mediante su ID, al cual llamaremos con un signo `$`. Regresemos al ejemplo anterior, es decir:\n\n```{r, eval=FALSE}\nfluidPage(\n  column(\n    width = 3,\n    selectInput(\n      inputId = \"mis_opciones\",\n      label = \"Opciones\",\n      choices = c(\"Opcion 1\", \"Opcion 2\", \"Opcion 3\")\n      )\n  ),\n  column(\n    width = 9,\n    plotOutput(\"mi_grafico\", width = \"400px\", height = \"600px\")\n  )\n)\n```\n\nObviamente, la idea de esa interfaz es mostrar un gráfico (identificado por el ID `mi_grafico`) que estará en función de una de las opciones del input `mis_opciones`. Para nosotros poder referirnos a ese gráfico en el lado del servidor, vamos a crear un objeto que lo renderice con la función `renderPlot()`, tal que:\n\n```{r, eval=FALSE}\nserver <- function(input, output){\n  \n  output$mi_grafico <- renderPlot({\n    plot(x, y)\n  })\n  \n}\n```\n\nNótese que, además del uso del paréntesis, también utilicé corchetes dentro de la función `renderPlot()`, ya que eso me permitirá una mayor flexibilidad al momento de escribir códigos más complejos cuando quiera renderizar otro tipo objeto.\n\nEn de la función `renderPlot()` es donde vamos a hacer uso de funciones menos dinámicas para generar el gráfico, siendo en este caso `plot()`. Todas las librerías de gráficos, mapas y objetos interactivos que esten habilitadas para trabajar con aplicaciones web siempre tendrán una función con el prefijo `render`, la cual será aquella que estableceremos en el servidor para generar el objeto que necesitemos. Ahora, las funciones de estilo `render` son elementos *reactivos*, es decir, aquellos que cambian su valor dependiendo de los elementos de entrada (*inputs*), como lo mencioné anteriormente.\n\n#### *Intermedio*: Un ejemplo práctico\n\nAhora que tenemos un conocimiento más general del funcionamiento de una aplicación web, vamos a hacer una breve pausa con las explicaciones, para poner en práctica lo aprendido hasta ahora con un ejemplo real. Utilicemos el [el siguiente set de datos](https://github.com/Jorge-hercas/r-para-inteligencia-de-negocios/blob/main/CSV/english_education.csv), proveniente de la *Oficina para estadísticas nacionales de UK*, el cual tiene como objetivo el medir los niveles educativos de diferentes ciudades y pueblos de UK. Comencemos por visualizar los datos en cuestión:\n\n```{r, eval=FALSE}\nuk_data <- read.csv(\"https://raw.githubusercontent.com/Jorge-hercas/\n                    r-para-inteligencia-de-negocios/main/CSV/\n                    english_education.csv\")\n\nuk_data |> \n  head()\n```\n\n```{r, echo=FALSE}\nuk_data <- read.csv(\"https://raw.githubusercontent.com/Jorge-hercas/r-para-inteligencia-de-negocios/main/CSV/english_education.csv\")\n\nuk_data |> \n  as_tibble() |> \n  head()\n```\n\nSupongamos que de ese conjunto de datos queremos visualizar el promedio de población registrada en 2011 (`population_2011`) por región de la localidad `rgn11nm`. Esa tarea es en realidad muy sencilla, ya que simplemente podemos crear un gráfico con `ggplot2` que nos muestre la información que queremos, tal que:\n\n```{r, eval=FALSE}\nuk_data |> \n  group_by(rgn11nm) |> \n  summarise(promedio = mean(population_2011, na.rm = T)) |> \n  na.omit() |> \n  arrange(desc(promedio)) |> \n  ggplot(aes(x = promedio, y = forcats::fct_reorder(rgn11nm, promedio) ))+\n  geom_col(fill = \"darkred\") + \n  theme_minimal() +\n  ylab(label = \"Region\")\n```\n\nLa parte interesante comienza cuando queremos realizar múltiples vistas accionables por un usuario ajeno a nuestro código. Supongamos que queremos filtrar nuestros datos dinámicamente por tipo de ciudad (`size_flag`), aquí tenemos dos opciones: Generar un filtro que acepte solo un argumento (`==`) o un filtro múltiple (`%in%`). Comencemos con un filtro de un solo argumento, el cual vamos a implementar en una app que nos permita realizar esta operación a partir de un `selectInput()`. Primero, nuestro selector necesitará obtener las opciones de tamaños de ciudad disponibles en nuestros datos, por lo que podemos hacer uso de una función `unique()`, tal que:\n\n```{r, eval=FALSE}\nselectInput(inputId = \"ciudad\", label = \"Tipo de ciudad\", \n            choices = unique(uk_data$size_flag))\n```\n\nAdemás, podemos reordenar los valores por orden alfabético:\n\n```{r, eval=FALSE}\nselectInput(inputId = \"ciudad\", label = \"Tipo de ciudad\", \n            choices = sort(unique(uk_data$size_flag)))\n```\n\nEl `inputId` del selector es *ciudad*, por lo que para aplicar el filtro dentro del código del gráfico, tendremos que llamarlo en una función `filter()` de la siguiente manera:\n\n```{r, eval=FALSE}\nuk_data |> \n  filter(size_flag == input$ciudad) |>\n  group_by(rgn11nm) |> \n  summarise(promedio = mean(population_2011, na.rm = T)) |> \n  na.omit() |> \n  arrange(desc(promedio)) |> \n  ggplot(aes(x = promedio, \n             y = forcats::fct_reorder(rgn11nm, promedio) ))+\n  geom_col(fill = \"darkred\") + \n  theme_minimal() +\n  ylab(label = \"Region\")\n```\n\nObviamente, este código de forma individual no funciona, para que la orden pueda ser ejecutada necesariamente debe estar dentro de una función de renderización, es decir:\n\n```{r, eval=FALSE}\noutput$grafico <- renderPlot({\n      \n    uk_data |> \n      filter(size_flag == input$ciudad) |> \n      group_by(rgn11nm) |> \n      summarise(promedio = mean(population_2011, na.rm = T)) |> \n      na.omit() |> \n      arrange(desc(promedio)) |> \n      ggplot(aes(x = promedio, \n                 y = forcats::fct_reorder(rgn11nm, promedio) ))+\n      geom_col(fill = \"darkred\") + \n      theme_minimal() +\n      ylab(label = \"Region\")\n      \n    })\n```\n\nLa cual estará conectada a la interfaz del programa a partir del *ID* del gráfico, siendo en este caso:\n\n```{r, eval=FALSE}\nplotOutput(\"grafico\")\n```\n\nVamos a generar una vista centrada del gráfico y selector, que además incluya un breve título:\n\n```{r, eval=FALSE}\nfluidPage(\n    column(\n      width = 12,\n      align = \"center\",\n      h1(\"Mi primer shiny app\"),\n      br(),\n      selectInput(\"ciudad\", \"Tipo de ciudad\", \n                  choices = unique(uk_data$size_flag)),\n      br(),\n      plotOutput(\"grafico\", width = \"900px\")\n    )\n  )\n```\n\nNótese que en este caso, para centrar completamente los elementos de la interfaz, generamos una columna de tamaño completo, en la cual separamos los elementos de forma vertical mediante un `br()`, equivalente a `<br/>` en HTML.\n\nFinalmente, para nosotros poder ejecutar nuestra aplicación, haremos uso de la función `shinyApp()`, por lo que el código en conjunto será:\n\n```{r, eval=FALSE}\n# Librerías\nlibrary(ggplot2)\nlibrary(dplyr)\nlibrary(shiny)\n\n# Datos\nuk_data <- read.csv(\"https://raw.githubusercontent.com/Jorge-hercas/\n                    r-para-inteligencia-de-negocios/main/CSV/\n                    english_education.csv\")\n# UI\nfrontend <- fluidPage(\n    column(\n      width = 12,\n      align = \"center\",\n      h1(\"Mi primer shiny app\"),\n      br(),\n      selectInput(\"ciudad\", \"Tipo de ciudad\", \n                  choices = unique(uk_data$size_flag)),\n      br(),\n      plotOutput(\"grafico\", width = \"900px\")\n    )\n  )\n# SERVER\nbackend <- function(input, output){\n    \n    output$grafico <- renderPlot({\n      \n      uk_data |> \n        filter(size_flag == input$ciudad) |> \n        group_by(rgn11nm) |> \n        summarise(promedio = mean(population_2011, na.rm = T)) |> \n        na.omit() |> \n        arrange(desc(promedio)) |> \n        ggplot(aes(x = promedio, \n                   y = forcats::fct_reorder(rgn11nm, promedio) ))+\n        geom_col(fill = \"#ad3d42\") + \n        theme_minimal() +\n        ylab(label = \"Region\")\n      \n    })\n  }\n\n# App\nshinyApp(ui = frontend, server = backend)\n```\n\nSiendo la app resultante el de la figura 9.5.\n\n![Una shiny app simple](imagenes/primerapp.png){fig-align=\"center\" width=\"520\"}\n\n#### Objetos `reactive()`\n\nLos objetos `reactive()` son una forma en la que nosotros convertiremos nuestros datos a una función dinámica, la cual usaremos después para generar nuestros *outputs*. Este tipo de elementos son convenientes cuando queremos ramificar una cierta operación en múltiples elementos de salida, por ejemplo: Supongamos que queremos generar dos gráficos diferentes que estarán ligados al mismo filtro. Podríamos directamente generar el código en cada uno de los objetos `render`, escribiendo en cada caso el filtro que necesitamos, sin embargo, si queremos escalar este programa, añadiendo cada vez más y más elementos de salida, individualizar esta operación a cada caso hará que nuestro programa se vuelva muy lento. Una forma más eficiente de ejecutar estas operaciones será partiendo de un objeto `reactive()` que ya filtre nuestros datos, para después únicamente llamarlo en cada `render`. Gráficamente:\n\n![Diagrama de un objeto \\`reactive\\`\\`](imagenes/reactive.pdf){fig-align=\"center\" width=\"280\"}\n\nRegresemos a nuestro ejemplo anterior. Vamos a añadir un gráfico adicional que ahora mida el porcentaje de residentes que tienen ingresos altos (arriba de 10,000), divido por zonas con/sin universidades, es decir:\n\n```{r}\nuk_data |> \n  group_by(rgn11nm, university_flag) |> \n  summarise(\n    promedio_ing = \n      mean(activity_at_age_19_employment_with_earnings_above_10_000, \n           na.rm = T)) |> \n  na.omit() |> \n  ggplot(aes(y = promedio_ing, x = rgn11nm, fill = university_flag)) +\n  geom_col(position = position_dodge(width = 0.9), width = 0.7) + \n  theme_minimal()+\n  theme(legend.position = \"bottom\",\n        axis.text=element_text(size=7),\n        axis.text.x = element_text(angle = 30))+\n  scale_fill_manual(values = c(\"#ad3d42\", \"#3d4ead\"),\n                    name = \"¿Tiene escuela?\") +\n  labs(\n    x = \"Zona\",\n    y = \"Promedio\")\n```\n\nSupongamos que a este gráfico le queremos aplicar el mismo filtro que estamos usando en el otro elemento, es decir, por tipo de ciudad (`filter(size_flag == input$ciudad)`). Conforme vayamos agregando gráficos y otros elementos, estos partirán de la misma base, que es la tabla de datos filtrada dinámicamente. Vamos a tomar esta base y convertirla en una función `reactive()`, tal que:\n\n```{r, eval=FALSE}\ndatos_dinamicos <- reactive({\n  \n  uk_data |> \n        filter(size_flag == input$ciudad)\n  \n})\n```\n\nPor tanto, para cada *output* que utilice como base ese filtro, simplemente utilizaremos el objeto (o función) `datos_dinamicos()`, por ejemplo:\n\n```{r, eval=FALSE}\noutput$grafico <- renderPlot({\n      \n      datos_dinamicos() |> \n        group_by(rgn11nm) |> \n        summarise(promedio = mean(population_2011, na.rm = T)) |> \n        na.omit() |> \n        arrange(desc(promedio)) |> \n        ggplot(aes(x = promedio, \n                   y = forcats::fct_reorder(rgn11nm, promedio) ))+\n        geom_col(fill = \"#ad3d42\") + \n        theme_minimal() +\n        ylab(label = \"Region\")\n      \n    })\n```\n\nLo que hará más eficiente nuestra aplicación. Nótese que, al momento de convertir un objeto en un elemento reactivo, este pasa a ser una función, por lo que ahora debemos llamarlo con `datos_dinamicos()`, en lugar de `datos_dinamicos`.\n\nVayamos con el código completo, utilizando ahora dos columnas para cada gráfico. Añadiendo la función en el servidor, el código será:\n\n```{r, eval=FALSE}\nfrontend <- fluidPage(\n    column(\n      width = 12,\n      align = \"center\",\n      h1(\"Mi primer shiny app\"),\n      br(),\n      selectInput(\"ciudad\", \"Tipo de ciudad\", \n                  choices = unique(uk_data$size_flag)),\n      \n    ),\n    column(\n      width = 6, align = \"center\",\n      plotOutput(\"grafico\", width = \"600px\")\n    ),\n    column(\n      width = 6, align = \"center\",\n      plotOutput(\"grafico_2\", width = \"600px\")\n    )\n  )\n\nbackend <- function(input, output){\n    \n    datos_dinamicos <- reactive({\n      \n      uk_data |> \n        filter(size_flag == input$ciudad)\n      \n    })\n    \n    output$grafico <- renderPlot({\n      \n      datos_dinamicos() |> \n        group_by(rgn11nm) |> \n        summarise(promedio = mean(population_2011, na.rm = T)) |> \n        na.omit() |> \n        arrange(desc(promedio)) |> \n        ggplot(aes(x = promedio, \n                   y = forcats::fct_reorder(rgn11nm, promedio) ))+\n        geom_col(fill = \"#ad3d42\") + \n        theme_minimal() +\n        ylab(label = \"Region\")\n      \n    })\n    \n    output$grafico_2 <- renderPlot({\n      \n      datos_dinamicos() |> \n        group_by(rgn11nm, university_flag) |> \n        summarise(\n          promedio_ing = \n            mean(activity_at_age_19_employment_with_earnings_above_10_000, \n                 na.rm = T)) |> \n        na.omit() |> \n        ggplot(aes(y = promedio_ing, x = rgn11nm, fill = university_flag)) +\n        geom_col(position = position_dodge(width = 0.9), width = 0.7) + \n        theme_minimal()+\n        theme(legend.position = \"bottom\",\n              axis.text=element_text(size=7))+\n        scale_fill_manual(values = c(\"#ad3d42\", \"#3d4ead\")) +\n        labs(\n          x = \"Zona\",\n          y = \"Promedio\")\n      \n    })\n  }\n\nshinyApp(ui = frontend, server = backend)\n```\n\nEn este caso, la aplicación resultante será:\n\n![App con múltiples outputs](imagenes/reactiveapp.png){fig-align=\"center\" width=\"520\"}\n\nAl igual que en la funciones comunes, cuando utilizamos un objeto `reactive()`, este necesariamente debe retornar al final de la evaluación el objeto que queremos utilizar globalmente. En el ejemplo anterior únicamente hicimos uso de un filtro y una concatenación de objetos `|>`, pero en situaciones donde hagamos evaluaciones, por ejemplo, con condiciones, será necesario retornar directamente el objeto fuera de esa evaluación. Tomemos como ejemplo el caso anterior, donde hicimos el filtro condicional, supongamos que ahora vamos a aplicar ese filtro solo en caso de que un *input* booleano sea verdadero, en tal caso el objeto `reactive()` será:\n\n```{r, eval=FALSE}\ndatos_dinamicos <- reactive({\n      \n      if (input$bool == T){\n        resultado <- uk_data |> \n          filter(size_flag == input$ciudad)\n      }else{\n        resultado <- uk_data\n      }\n  uk_data\n      \n    })\n```\n\nDonde el objeto resultante de esa evaluación se encuentra hasta el final de la función.\n\n#### Eventos reactivos\n\nCuando seleccionamos ciertas opciones, no siempre queremos que estas se ejecuten de inmediato. Piensa en una aplicación que tiene tres, cinco o hasta más *inputs* con los que generar un gráfico o elemento cualquiera. Seleccionar todos los parámetros nos va a llevar un tiempo, por lo que para un usuario que esté usando el programa, será bastante incómodo tener que ver múltiples renderizaciones del objeto mientras termina de seleccionar todos los parámetros, por lo que en esa situación lo más óptimo sería utilizar un boton de acción que utilizaremos como **evento**, para que el objeto de salida sea ejecutado únicamente cuando ocurra dicho evento (el botón sea precionado). Una vez más, regresemos a la aplicación que hemos creado hasta ahora, supongamos que queremos renderizar ambos gráficos hasta que presionemos un botón, tal que:\n\n```{r, eval=FALSE}\nactionButton(inputId = \"graficar\", label = \"Generar gráficos\")\n```\n\nPodemos añadir dicho boton en la interfaz de nuestra app, debajo del selector:\n\n```{r, eval=FALSE}\nfluidPage(\n    column(\n      width = 12,\n      align = \"center\",\n      h1(\"Mi primer shiny app\"),\n      br(),\n      selectInput(\"ciudad\", \"Tipo de ciudad\", \n                  choices = unique(uk_data$size_flag)),\n      actionButton(inputId = \"graficar\", label = \"Generar gráficos\")\n    ),\n    column(\n      width = 6, align = \"center\",\n      plotOutput(\"grafico\", width = \"600px\")\n    ),\n    column(\n      width = 6, align = \"center\",\n      plotOutput(\"grafico_2\", width = \"600px\")\n    )\n  )\n```\n\nPara ligar los gráficos a la acción de presionar el botón, solo debemos englobarlos en una función `observeEvent()`:\n\n```{r, eval=FALSE}\nobserveEvent(input$graficar,{\n  \n  output$grafico <- renderPlot({\n      \n      datos_dinamicos() |> \n        group_by(rgn11nm) |> \n        summarise(promedio = mean(population_2011, na.rm = T)) |> \n        na.omit() |> \n        arrange(desc(promedio)) |> \n        ggplot(aes(x = promedio, \n                   y = forcats::fct_reorder(rgn11nm, promedio) ))+\n        geom_col(fill = \"#ad3d42\") + \n        theme_minimal() +\n        ylab(label = \"Region\")\n      \n    })\n    \n  output$grafico_2 <- renderPlot({\n      \n      datos_dinamicos() |> \n        group_by(rgn11nm, university_flag) |> \n        summarise(\n          promedio_ing = \n            mean(activity_at_age_19_employment_with_earnings_above_10_000, \n                 na.rm = T)) |> \n        na.omit() |> \n        ggplot(aes(y = promedio_ing, x = rgn11nm, fill = university_flag)) +\n        geom_col(position = position_dodge(width = 0.9), width = 0.7) + \n        theme_minimal()+\n        theme(legend.position = \"bottom\",\n              axis.text=element_text(size=7))+\n        scale_fill_manual(values = c(\"#ad3d42\", \"#3d4ead\")) +\n        labs(\n          x = \"Zona\",\n          y = \"Promedio\")\n      \n    })\n})\n```\n\nEl uso de `observeEvent()` no está limitado a un solo elemento. Sencillamente podemos ligar la renderización de un *output* a múltiples accionables, añadiéndolos a una lista, por ejemplo:\n\n```{r, eval=FALSE}\nobserveEvent(list(input$graficar, input$otro_evento),{\n  \n  ...\n  # Procesos ligados a múltiples eventos\n  ...\n  \n})\n```\n\nEl hecho de que un proceso se encuentre relacionado a múltipes eventos no significa que todos tengan que ocurrir, si no que al menos uno de ellos se ejecute, sea cual sea este.\n\nLos eventos comunmente están asociados a botones, sin embargo, no es necesario que este sea el caso. De igual forma pueden estar ligados a ciertos *inputs* de otras clases, como selectores o booleanos. Todo dependerá del contexto de la aplicación y lo que necesitemos para mejorar la experiencia del usuario final.\n\nAhora, debemos considerar que estos eventos suponen que, el accionar el botón o *input* en nuestro programa, mantendrá la renderización activa, es decir, que al nosotros utilizar el botón, cuando volvamos a seleccionar parámetros en el programa, ya no será necesario volver a presionarlo, ya que el *evento esperado* ya ocurrió, por lo que ahora los gráficos se van a ejecutar con normalidad, sin estar ligados a nada. Cuando nosotros queremos asegurarnos de que un objeto únicamente sea renderizado cada que el elemento de acción es ejecutado, independientemente de las veces que hagamos esto, tendremos que utilizar `bindEvent()` en lugar de `observeEvent()`. Para el uso de `bindEvent()`, en lugar de englobar las funciones de renderización, las vamos a concatenar:\n\n```{r, eval=FALSE}\n    output$grafico <- renderPlot({\n        \n        datos_dinamicos() |> \n          group_by(rgn11nm) |> \n          summarise(promedio = mean(population_2011, na.rm = T)) |> \n          na.omit() |> \n          arrange(desc(promedio)) |> \n          ggplot(aes(x = promedio, \n                     y = forcats::fct_reorder(rgn11nm, promedio) ))+\n          geom_col(fill = \"#ad3d42\") + \n          theme_minimal() +\n          ylab(label = \"Region\")\n        \n      }) |> \n      bindEvent(input$graficar)\n```\n\nDe esta forma, no importa que ya hayamos ejecutado el evento, para cada nueva renderización del gráfico tendremos que volver a accionarlo.\n\n#### La sesion\n\nA la función que creamos para el servidor de nuestra aplicación también podemos añadirle un tercer argumento: `session`, siendo el código:\n\n```{r, eval=FALSE}\nserver = function(input, output, session){\n  ...\n}\n```\n\nEl elemento `session` nos permitirá acceder al estado individual de cada sesión (valga la redundancia) creada por la aplicación, es decir, cuando nosotros ejecutamos una shiny app, cada vez que abrimos el programa en una pestaña diferente del navegador, esta pestaña representará una sesión individual que responderá únicamente a esa vista, siendo el argumento `session` una forma de acceder a esa información individual e interacturar con ella.\n\nNo siempre vamos a querer partir del estado original de una aplicación cuando la ejecutemos de nuevo, sobre todo cuando hay una cantidad considerable de parámetros que ya hemos establecido en el análisis que estemos haciendo. Podemos hacer uso de `session` para, por ejemplo, guardar ciertos parámetros dentro del link de nuestra app y, de manera automática, inicializarla con las opciones que hayamos dejado al finalizar la sesión anterior. Retomemos la aplicación que hemos creado hasta ahora, con el argumento `session` añadido en el servidor; Para poder inicializarla desde un punto específico, simplemente debemos utilizar el argumento `enableBookmarking`, es decir:\n\n```{r, eval=FALSE}\nshinyApp(ui = frontend, server = backend, enableBookmarking = \"url\")\n```\n\nPara acceder al link exacto del estado actual de la aplicación, podemos hacer uso del *input* `bookmarkButton()` (añadiéndolo en el UI), que nos permitirá copiar ese link al portapapeles.\n\nEl uso de `session` no solo se limita a la información de la sesión actual, si no también a la manipulación dinámica de ella, por ejemplo, con los *inputs*. Pensemos en el *input* que utilizamos en la app de muestra para filtrar datos, este contiene las siguientes opciones:\n\n```{r}\nunique(uk_data$size_flag)\n```\n\nLa tabla de datos también contiene la columna `university_flag`, que es mucho más general, al ser solamente un indicativo de las regiones con universidad. Suponamos que solo queremos mostrar en el selector del filtro aquellas opciones que tienen (o no) universidad, de manera condicional. Podemos cambiar el contenido del selector dinámicamente con la función `updateSelectInput()`, que toma la sesión en consideración para poder modificarla. Supongamos que ahora tenemos como *input* general el selector del indicativo de universidad, es decir:\n\n```{r, eval=FALSE}\nselectInput(\"escuela\", \"¿Tiene universidad?\", \n            choices = unique(na.omit(uk_data$university_flag)))\n```\n\nA partir de ese *input* vamos a modificar el otro, tal que:\n\n```{r, eval=FALSE}\nfunction(input, output, session){\n  \n  observeEvent(input$escuela, {\n    # Datos reactivos\n    datos_filt <- reactive({\n      uk_data |> \n        filter(university_flag == input$escuela)\n    })\n    # Actualización de la sesión\n    updateSelectInput(session = session, inputId = \"ciudad\", \n                      choices = unique(datos_filt() ))\n  })\n  \n}\n```\n\nEn este caso, lo único que hicimos fue crear un objeto `reactive` con el data frame filtrado a cierta opción, para después tomar ese objeto y obtener los valores únicos que queden de la columna `size_flag`. Finalmente, el código completo de la app será:\n\n```{r, eval=FALSE}\nfrontend <- fluidPage(\n  column(\n    width = 12,\n    align = \"center\",\n    h1(\"Mi primer shiny app\"),\n    br(),\n    selectInput(\"escuela\", \"¿Tiene universidad?\", \n                choices = unique(na.omit(uk_data$university_flag))),\n    selectInput(\"ciudad\", \"Tipo de ciudad\", \n                choices = unique(uk_data$size_flag)),\n    actionButton(inputId = \"graficar\", label = \"Generar gráficos\")\n    \n  ),\n  column(\n    width = 6, align = \"center\",\n    plotOutput(\"grafico\", width = \"600px\")\n  ),\n  column(\n    width = 6, align = \"center\",\n    plotOutput(\"grafico_2\", width = \"600px\")\n  )\n)\n\nbackend <- function(input, output, session){\n  \n  observeEvent(input$escuela, {\n    datos_filt <- reactive({\n      uk_data |> \n        filter(university_flag == input$escuela)\n    })\n    \n    updateSelectInput(session = session, inputId = \"ciudad\", \n                      choices = unique(datos_filt()$size_flag ))\n  })\n  \n  \n  datos_dinamicos <- reactive({\n    \n    uk_data |> \n      filter(size_flag == input$ciudad)\n    \n  })\n  \n  observeEvent(input$graficar,{\n    \n    output$grafico <- renderPlot({\n      \n      datos_dinamicos() |> \n        group_by(rgn11nm) |> \n        summarise(promedio = mean(population_2011, na.rm = T)) |> \n        na.omit() |> \n        arrange(desc(promedio)) |> \n        ggplot(aes(x = promedio, \n                   y = forcats::fct_reorder(rgn11nm, promedio) ))+\n        geom_col(fill = \"#ad3d42\") + \n        theme_minimal() +\n        ylab(label = \"Region\")\n      \n    })\n    \n    output$grafico_2 <- renderPlot({\n      \n      datos_dinamicos() |> \n        group_by(rgn11nm, university_flag) |> \n        summarise(\n          promedio_ing = \n            mean(activity_at_age_19_employment_with_earnings_above_10_000, \n                 na.rm = T)) |> \n        na.omit() |> \n        ggplot(aes(y = promedio_ing, x = rgn11nm, fill = university_flag)) +\n        geom_col(position = position_dodge(width = 0.9), width = 0.7) + \n        theme_minimal()+\n        theme(legend.position = \"bottom\",\n              axis.text=element_text(size=7))+\n        scale_fill_manual(values = c(\"#ad3d42\", \"#3d4ead\")) +\n        labs(\n          x = \"Zona\",\n          y = \"Promedio\")\n      \n    })\n  })\n}\n\nshinyApp(ui = frontend, server = backend, enableBookmarking = \"url\")\n```\n\n#### Invalidando la reactividad\n\nNo en todas las situaciones en las que queramos ejecutar un evento vamos a necesitar que esta acción ocurra invariablemente (o que ocurra de la misma forma), ya que podemos tener ciertos *inputs* con valores incompatibles, o sin los valores necesarios para ejecutar de la forma correcta el *output* requerido. Regresemos al ejemplo que hemos venido trabajando hasta ahora, donde tenemos el *input* `selectInput()`:\n\n```{r, eval=FALSE}\nselectInput(\"ciudad\", \"Tipo de ciudad\", choices = unique(uk_data$size_flag))\n```\n\nAl seleccionar datos individuales, este *input* por default siempre tendrá un valor asignado, sin embargo, si modificáramos el párametro `multiple = T` para aceptar más de un valor (manteniendo vacío el parámetro `selected`):\n\n```{r, eval=FALSE}\nselectInput(\"ciudad\", \"Tipo de ciudad\", choices = unique(uk_data$size_flag),\n            multiple = T)\n```\n\nEsto permitirá que comencemos nuestra app sin un valor seleccionado por default. Obviamente, al no tener un valor establecido, el gráfico conectado a este *input* no se renderizará, ya que la amplitud del vector a filtrar será igual a cero, por lo que en esta situación podríamos invalidar la renderización de los gráficos en cuestión, esto haciendo uso de las funciones `validate()` (para generar un validador previo que evitará la renderización del objeto) y `need()` (para especificar el *input* a evaluar). El uso de estas funciones es bastante sencillo, siendo que simplemente debemos aplicarlas como un prefijo dentro de la función de renderización del *output*, por ejemplo, con `output$grafico_2`:\n\n```{r, eval=FALSE}\noutput$grafico_2 <- renderPlot({\n  \n      validate(\n        need(input$ciudad, message = \"Escoge un valor!\")\n      )\n      \n      datos_dinamicos() |> \n        group_by(rgn11nm, university_flag) |> \n        summarise(\n          promedio_ing = \n            mean(activity_at_age_19_employment_with_earnings_above_10_000, \n                 na.rm = T)) |> \n        na.omit() |> \n        ggplot(aes(y = promedio_ing, x = rgn11nm, fill = university_flag)) +\n        geom_col(position = position_dodge(width = 0.9), width = 0.7) + \n        theme_minimal()+\n        theme(legend.position = \"bottom\",\n              axis.text=element_text(size=7))+\n        scale_fill_manual(values = c(\"#ad3d42\", \"#3d4ead\")) +\n        labs(\n          x = \"Zona\",\n          y = \"Promedio\")\n      \n    })\n```\n\nPor lo que, si no tenemos un valor establecido en `input$ciudad`, en lugar de devolver un error, se nos devolverá un mensaje de alerta preestablecido por nosotros:\n\n![Validando inputs](imagenes/validate.png){fig-align=\"center\" width=\"520\"}\n\nSencillamente, también podemos aplicar operaciones en una función `need()`, cuando queremos ser más específicos en las situaciones en las que queremos que no se renderice un objeto, por ejemplo:\n\n```{r, eval=FALSE}\nvalidate(\n  need(input$ciudad == \"Small Towns\", \n       message = \"Esta selección es inválida. Escoge otra\")\n)\n```\n\nDe esta forma, cuando se seleccione un valor que cumpla con esa condición, a pesar de tener un elemento en el vector del *input*, igualmente nos devolverá un mensaje de alerta en lugar del gráfico a renderizar.\n\n::: callout-warning\n## Cuidado\n\nNo olvides que un selector múltiple generará un vector de longitud *\\>=1*, por lo que necesitarás cambiar el operador de filtro en el objeto `reactive()` de `==` a `%in%`, en este caso:\n\n```{r, eval=FALSE}\ndatos_dinamicos <- reactive({\n    uk_data |> \n      filter(size_flag %in% input$ciudad)\n  })\n```\n:::\n\nUsar `validate()` no es la única manera en la que podemos cancelar la renderización de un elemento, ni la más agradable visualmente, pero si la más sencilla. Veamos algunas formas más interesantes de abordar este problema, utilizando la librería `shinypop` (del [siguiente reporsitorio de Github](https://github.com/dreamRs/shinypop)). Este paquete es una recopilación de funciones escritas en javaScript para mostrar notificaciones y mensajes de alerta, por lo que podemos utilizar estos elementos a nuestro favor para manejar la reactividad.\n\nComencemos con un ejemplo sencillo: Vamos a evaluar la renderización de los gráficos en la app que estamos construyendo, solo que en esta ocasión lo haremos mediante una estructura `if`, tal que:\n\n```{r, eval=FALSE}\nobserveEvent(input$graficar,{\n    \n    if (is.null(input$ciudad)==F){\n      \n      output$grafico <- renderPlot({\n      ...\n    })\n    \n    output$grafico_2 <- renderPlot({\n      ...\n    })\n    \n    }\n  })\n```\n\nEn este caso, si no seleccionamos un valor para `input$ciudad` lógicamente el vector resultante será de longitud cero, equivalente a un objeto `NULL`, por lo que al no cumplir esa condición, los gráficos no se crearán y no se devolverá un mensaje de error. Ahora, lo que si queremos que se devuelva es un mensaje de alerta emergente, por lo que podemos añadir a la evaluación una función de la librería `shinypop`, como puede ser `noty(text = \"El input 'ciudad' está vacío. Añade un valor\", type = \"warning\")`, resultando en:\n\n```{r, eval=FALSE}\nobserveEvent(input$graficar,{\n    \n    if (is.null(input$ciudad)==F){\n      \n      output$grafico <- renderPlot({\n      ...\n    })\n    \n    output$grafico_2 <- renderPlot({\n      ...\n    })\n    \n    }else{\n      noty(text = \"El input 'ciudad' está vacío. Añade un valor\", \n           type = \"warning\")\n    }\n  })\n```\n\nEn este caso, para poder visualizar la notificación emergente, tendremos que añadir el código `use_noty()` en la parte inicial de la interfaz, tal que:\n\n```{r, eval=FALSE}\nfluidPage(\n  use_noty(),\n  ...\n)\n```\n\nYa que debemos añadir esta dependencia al código HTML de la aplicación, de forma similar a como añadimos dependencias de JavaScript en la sección `<head>` de una plantilla HTML.\n\nFinalmente, al querer obtener el *output* sin valores en el *input*, el resultado gráfico será:\n\n![Alerta emergente en una app](imagenes/notie.png){fig-align=\"center\" width=\"520\"}\n\nEstas notificaciones son algo pequeñas y no siempre las más intuitivas, por lo que podríamos utilizar algo un poco más *\"llamativo\"*, por ejemplo, con una notificación en el centro de la pantalla con la función `nx_report_warning()`. Para poder aplicar esta función, esta vez debemos insertar `use_notiflix_report()` en la parte inicial de la interfaz, tal que:\n\n```{r, eval=FALSE}\nfluidPage(\n  use_notiflix_report(),\n  ...\n)\n```\n\nY aplicar `nx_report_warning()` en el servidor, de manera similar a como lo hicimos en el caso anterior:\n\n```{r, eval=FALSE}\nobserveEvent(input$graficar,{\n    \n    if (is.null(input$ciudad)==F){\n      \n      output$grafico <- renderPlot({\n      ...\n    })\n    \n    output$grafico_2 <- renderPlot({\n      ...\n    })\n    \n    }else{\n      nx_report_warning(title = \"Cuidado\",\n                        message = \"No se ha seleccionado el input\",\n                        button = \"De acuerdo\")\n    }\n  })\n```\n\nResultando en:\n\n![Alerta emergente con boton](imagenes/notifix.png){fig-align=\"center\" width=\"320\"}\n\nUna notificación mucho más clara e interactiva para el usuario final. Por supuesto, el uso de de estas notificaciones no es más que un aditamento en una estructura de control para invalidar la renderización de un elemento, siendo que existen muchas otras librerías que nos pueden ayudar a cumplir este mismo propósito, como es el caso de `shinyalert`, `shinytoastr`, `shinyvalidate`, `awn`, entre otros recursos.\n\n### Interfaz dinámica\n\nLos objetos reactivos que pueden ser ejecutados del lado del servidor no solo se limitan a gráficos, textos, tablas o mapas simples. De igual forma, podemos renderizar interfaces completas de forma dinámica, basándonos en otros *inputs*. En la app trabajada hasta ahora, de manera estática establecimos que cada gráfico se mantenga en una columna por separado, ¿qué tal si hacemos de esto una opción para el usuario final? De tal forma que ahora, basados en una selección, podamos establecer la forma en la que visualizaremos ambos gráficos. Comencemos por reemplazar ambas columnas contenedoras de los gráficos por un *output*, así como añadir una selección inicial que nos permita establecer la orientación en la que veremos los elementos:\n\n```{r, eval=FALSE}\nfrontend <- fluidPage(\n  column(\n    use_notiflix_report(),\n    width = 12,\n    align = \"center\",\n    h1(\"Mi primer shiny app\"),\n    br(),\n    radioButtons(\"tipo_vista\", \"Tipo de vista\", \n                 choices = c(\"Horizontal\", \"Vertical\"),\n                 inline = T),\n    selectInput(\"escuela\", \"¿Tiene universidad?\", \n                choices = unique(na.omit(uk_data$university_flag))),\n    selectInput(\"ciudad\", \"Tipo de ciudad\", \n                choices = unique(uk_data$size_flag)),\n    actionButton(inputId = \"graficar\", label = \"Generar gráficos\")\n    \n  ),\n  uiOutput(\"interfaz\")\n)\n```\n\nAhora, del lado del servidor, simplemente vamos a trabajar esta parte de la interfaz como cualquier *output*, tal que:\n\n```{r, eval=FALSE}\noutput$interfaz <- renderUI({\n    \n    if (input$tipo_vista == \"Horizontal\"){\n      tagList(\n        column(\n          width = 6, align = \"center\",\n          plotOutput(\"grafico\", width = \"600px\")\n        ),\n        column(\n          width = 6, align = \"center\",\n          plotOutput(\"grafico_2\", width = \"600px\")\n        )\n      )\n    }else{\n      column(\n        width = 12,\n        align = \"center\",\n        plotOutput(\"grafico\", width = \"600px\"),\n        br(),\n        plotOutput(\"grafico_2\", width = \"600px\")\n      )\n    }\n    \n  })\n```\n\nLa app resultante será:\n\n![UI dinámica](imagenes/vistaver.png){#fig-ver fig-align=\"center\" width=\"220\"}\n\nNótese que, en el caso donde tenemos dos columnas, las englobamos en una función `tagList()`, ya que el objeto *output* resultante debe ser uno, por lo que al tener dos columnas, de no introducirlas dentro de `tagList()`, se nos devolvería un error.\n\nAsí como los roles de trabajo en Power BI, podemos utilizar las interfaces dinámicas para generar páneles que solo se muestren a ciertos usuarios, tema que analizaremos con detalle más adelante, cuando veamos la seguridad en aplicaciones web.\n\n#### Inputs en el servidor\n\nAnteriormente ya vimos como actualizar valores de un *input* en el servidor mediante la sesión, pero también tenemos la opción de crear *inputs* temporales que se generen en el servidor, como es el caso de algunas notificaciones emergentes. Retomemos las notificaciones de la librería `shinypop`, por ejemplo: `nx_confirm()`. Esta notificación se interpreta como un selector de dos elementos, por lo que la podemos aplicar como un *input* temporal que solo es generado bajo una situación específica. Supongamos que en la aplicación trabajada hasta ahora queremos mandar una alerta cada que se seleccione un cierto valor, siendo que, en caso de confirmar la notificación se generen los gráficos correspondientes, es decir:\n\n```{r, eval=FALSE}\nobserveEvent(input$graficar,{\n  \n  nx_confirm(\n      inputId = \"confirmacion\",\n      title = \"¿Seguro que quieres generar el gráfico?\",\n      button_ok = \"Si, ahora\",\n      button_cancel = \"Aún no\"\n    )\n  \n})\n\nobserveEvent(input$confirmacion,{\n  \n  if (input$confirmacion == T){\n    \n      output$grafico <- renderPlot({\n        ...\n      })\n      \n      output$grafico_2 <- renderPlot({\n        ...\n      })\n    }\n  \n})\n```\n\nEn este caso, al nosotros dar click en el botón para generar el gráfico, se nos devolverá una alerta de confirmación en forma de un *input* temporal, por lo que para poder generar los gráficos correspondientes, dado el evento `input$confirmacion`, simplemente tendremos que establecer las renderizaciones de objetos dentro este mismo.\n\n### Subidas y bajadas de archivos\n\nLos *inputs* y *outputs* no necesariamente deben limitarse a cadenas de texto, números o fechas simples, estos también pueden ser un archivo completo, desde archivos *.xlsx*, *.csv*, hasta documentos *.pdf*, entre muchos otros formatos. Del lado del *input*, el poder cargar un archivo en una shiny app nos permite una mucho mayor flexibilidad para crear ciertos análisis, aunque eso también implica que debemos ser cuidadosos respecto al tipo de archivos que permitiremos que el usuario suba, ya que no en todos los casos van a funcionar de la manera más óptima. Del lado del *output* la cosa se vuelve más sencilla, ya que la descarga de documentos estará más controlada, con el código que establezcamos para generar el documento, lo que nos permitirá minimizar errores en este lado del proceso.\n\n#### File *inputs*\n\nSubir un documento a una shiny app es bastante sencillo, ya que solo necesitaremos aplicar una función `fileInput()`, por ejemplo:\n\n```{r, eval=FALSE}\nfileInput(inputId = \"archivo_a_subir\", \n          label = \"Sube tu archivo\", \n          placeholder = \"doc.xlsx\")\n```\n\nAl momento de nosotros subir un documento, este se va a interpretar como un *input* que se va a ramificar en varios elementos:\n\n-   **datapath**: Ruta temporal del archivo.\n-   **name**: Nombre original del archivo.\n-   **size**: Peso total del archivo en bytes.\n-   **type**: Tipo de archivo.\n\nCada uno de elementos nos serán útiles en diferentes situaciones que veremos a continuación, pero principalmente utilizaremos el elemento `datapath`, ya que con este vamos a leer el documento subido mediante otras funciones, como pueden ser `read_excel()`, `read.csv()`, etc.\n\nVamos a crear un ejemplo sencillo de aplicación para subir documentos. En este ejemplo vamos a utilizar la librería `reactable` [@R-reactable] para crear una tabla condicional basada en el archivo que utilicemos como *input*. Comencemos por la interfaz:\n\n```{r, eval=FALSE}\nlibrary(reactable)\n\nfrontend <- fluidPage(\n    column(\n      width = 12, align = \"center\",\n      h1(\"App para subir archivos\")),\n    sidebarPanel(\n      width = 3,\n      align = \"center\",\n      fileInput(\"archivo\", \"Archivo a subir\"),\n      numericInput(\"filas\", \"Número de filas a leer\", value = 999),\n      selectInput(\"columnas\", \"Columnas a seleccionar\", multiple = T,\n                  choices = NULL)),\n    column(\n      width = 9,\n      reactableOutput(\"tabla_dinamica\")\n    )\n  )\n```\n\nAhora, ¿qué queremos que haga nuestra aplicación? Primero, el *input* `filas` va a determinar la cantidad de filas que vamos a leer de nuestro archivo, siendo que con dicho archivo vamos a actualizar las selecciones del *input* `columnas`, para que ahora nos aparezcan todas las columnas existentes del documento de forma condicional, lo que nos permitirá seleccionarlas para visualizar el *output* `tabla_dinamica`. Vayamos por partes: El documento que vayamos a subir, sea cual sea, será el evento detonante para que, primero, el selector de columnas obtenga valores y, posterior a esto, podamos visualizar la tabla renderizada con las columnas seleccionadas. El primer paso lógico será convertir el documento a subir en un elemento reactivo, es decir:\n\n```{r, eval=FALSE}\ndata <- reactive({\n      data <- readxl::read_excel(input$archivo$datapath, \n                                 n_max = input$filas)\n      data\n    })\n```\n\nDonde utilizamos `input$filas` para limitar la cantidad de filas retornadas.\n\nLógicamente, `input$archivo` es el evento que nos permitirá actualizar las opciones y la tabla, por lo que vamos a englobar dichos elementos en un `observeEvent()`, tal que:\n\n```{r, eval=FALSE}\nobserveEvent(input$archivo,{\n      \n      updateSelectInput(session = session,\n                        \"columnas\", \n                        choices = colnames(data()),\n                        selected = colnames(data()))\n      \n      output$tabla_dinamica <- renderReactable({\n        data() |> \n          select(one_of(input$columnas)) |> \n          reactable()\n      })\n      \n    })\n```\n\nDonde simplemente utilizamos el dato reactivo que cargamos en nuestra app para obtener y seleccionar los nombres de las columnas, mientras que la renderización de la tabla únicamente se va a basar en estas selecciones, por lo que necesariamente deberá estar englobada dentro del mismo evento.\n\nDados los pasos anteriores, el servidor de nuestra se conformará simplemente de ambos elementos, de la siguiente manera:\n\n```{r, eval=FALSE}\nbackend <- function(input, output, session){\n    \n    data <- reactive({\n      data <- readxl::read_excel(input$archivo$datapath, \n                                 n_max = input$filas)\n      data\n    })\n    \n    observeEvent(input$archivo,{\n      \n      updateSelectInput(session,\"columnas\", \n                        choices = colnames(data()),\n                        selected = colnames(data()))\n      \n      output$tabla_dinamica <- renderReactable({\n        validate(\n          need(input$columnas, \n               message = \"Selecciona al menos una columa\"))\n        data() |> \n          select(one_of(input$columnas)) |> \n          reactable()\n      })\n    })\n    \n  }\n```\n\nEsta app nos permite leer cualquier archivo de tipo *excel*, pero esto invariablemente nos conducirá a un pequeño problema: ¿Qué pasaría si el usuario intentara subir otro tipo de archivo? Por ejemplo, un documento *.csv*. La función `read_excel()` es imcompatible con este tipo de archivos, por lo que podemos plantear dos soluciones a esta problemática:\n\n1.  Aplicar una función condicional `tryCatch()`, para que, en caso de fallar con la lectura de un archivo *.csv* mediante `read_excel()`, utilice `read.csv()` en consecuencia. Para ello, simplemente debemos establecer esta última como función a ejecutar en caso de un error en la lectura del documento, siendo:\n\n    ```{r, eval = FALSE}\n    data <- reactive({\n        data <- \n        tryCatch({\n            readxl::read_excel(input$archivo$datapath, \n                               n_max = input$filas)\n        }, error = function(e){\n           read.csv(input$archivo$datapath, \n                    nrows = input$filas)\n        })\n      data\n    })\n    ```\n\n    Sin embargo, esta opción aún da pie a que el usuario introduzca un archivo ajeno a documentos de tipo *.csv* o *.xlsx*, generando otro error en el programa, y obviamente lo último que queremos hacer es crear un `tryCatch()` anidado, por lo que esta no es la solución más óptima.\n\n2.  Limitar las opciones de subida a solamente aquellos documentos con terminación *.csv* o *.xlsx*, es decir:\n\n    ```{r, eval = FALSE}\n    fileInput(\"archivo\", \"Archivo a subir\", accept = \".xlsx\")\n    ```\n\n    O bien, añadir más de un tipo de archivo disponible a subir, usando un vector como argumento `c(\".xlsx\", \".csv\")`.\n\n::: callout-tip\n## Tip\n\nAmbas soluciones son igual de validas, por lo que para escoger la más óptima, es recomendable pensar a partir de los posibles errores que pueda cometer el usuario final en cada caso, siendo la mejor opción la que de pie a menos problemáticas, sobre todo al escalar nuestra app.\n:::\n\nAhora, ¿hay un tamaño máximo permitido para los archivos cargados? Si. Por default, las apps en shiny solo permiten subir archivos no mayores a cinco megabytes (expresado en su equivalente en bytes: cinco millones), número que podemos configurar en el servidor de nuestra app o en la sección global, mediante la opción `shiny.maxRequestSize`, siendo en este caso:\n\n```{r, eval = FALSE}\noptions(shiny.maxRequestSize = 30000000)\n```\n\nEl número máximo estará expresado en bytes, por lo que para subir un documento de, por ejemplo, treinta megabytes, será necesario establecer treinta millones de bytes, ya sea que lo hagamos con el número directo o simplificándolo con una multiplicación `30*1000^2`.\n\n#### Descargas\n\nLa descarga de archivos en apps shiny no es más que una forma dinámica de obtenerlos, mediante una función `downloadHandler()` llamada por un *input* `downloadButton()` o `downloadLink()`, con la que ejecutaremos funciones más sencillas de exportación, como pueden ser `write.csv()`, `writexl::write_xlsx()`, e incluso algunas basadas en formatos mucho más especializados, como lo son `ggplot2::ggsave()` o `openxlsx::saveWorkbook()`. Esta sección es especialmente interesante, ya que la descarga de archivos es considerablemente más sencilla que la subida de estos, además de que tenemos un control mucho mayor del resultado, por lo que podremos customizarlo de tal forma que sea muy agradable para el usuario final.\n\nComencemos con un ejemplo básico: Exportar los datos visualizados de nuestra app en una tabla resumida. Para este ejemplo utilizaremos el set de datos `cheeses.csv`, [disponible en el repositorio oficial del libro,](https://github.com/Jorge-hercas/r-para-inteligencia-de-negocios/tree/main/CSV) siendo:\n\n```{r, eval=FALSE}\ndata <- read.csv(\"https://raw.githubusercontent.com/Jorge-hercas/\n                 r-para-inteligencia-de-negocios/main/CSV/cheeses.csv\")\n```\n\nHagamos un ejercicio sencillo de agrupamiento y filtrado de datos, en donde tendremos dos *inputs* principales: Un filtro con los colores existentes de los quesos guardados en el set de datos, así como un selector de columnas que utilizaremos para agrupar los datos y, posteriormente, obtener un conteo de cada grupo. Además de estos selectores, vamos a añadir un **botón de descarga** con un ID igual a `descarga`, objeto que representará el evento con el que importaremos los datos en un formato *.xlsx*. La interfaz en cuestión será:\n\n```{r, eval=FALSE}\nfrontend <- fluidPage(\n    column(\n      width = 12, align = \"center\",\n      h1(\"App para descargar documentos\")\n    ),\n    sidebarPanel(\n      width = 3,\n      align = \"center\",\n      selectInput(\"color\", \"Color del queso:\", \n                  choices = unique(data$color),\n                  selected = unique(data$color),\n                  multiple = T),\n      selectInput(\"agrupacion\", \"Agrupar por:\",\n                  choices = colnames(data),\n                  selected = \"country\"),\n      downloadButton(\"descarga\", \"Descarga tus datos\",\n                     icon = icon(\"file-excel\"))\n    ),\n    column(\n      width = 9,\n      reactableOutput(\"tabla_resumen\")\n    )\n  )\n```\n\nDonde `downloadButton()` será nuestro elemento para descargar los datos. Del lado del servidor, partiremos de un objeto reactivo que tendrá el formato final del resumen, con el cual vamos a generar una tabla para visualizarlo y, con el mismo objeto, generar la descarga:\n\n```{r, eval=FALSE}\nresumen <- reactive({\n      data |> \n        filter(color %in% input$color) |> \n        group_by_at(input$agrupacion) |>\n        summarise(\n          Conteo = n()\n        ) |> \n        arrange(desc(Conteo))\n    })\n```\n\nPara obtener estos datos en un documento *.xlsx*, utilizaremos la función `downloadHandler()`, la cual tendrá como argumento dos funciones anónimas: La primer función se encargará asignar un nombre al documento a partir de una cadena de caracteres, mientras que la segunda se encargará de llamar a la función generadora del documento (`writexl::write_xlsx()` en este caso), utilizando como argumento el nombre generado en la función anterior. El código en cuestión será:\n\n```{r, eval=FALSE}\noutput$descarga <- downloadHandler(\n      filename = function(){\n        paste0(\"resumen por \",input$agrupacion,\".xlsx\")\n      },\n      content = function(file){\n        writexl::write_xlsx(\n          x = list(\n            Datos = resumen()\n          ),\n          path = file\n        )\n      }\n    )\n```\n\nDonde los *inputs* también pueden ser usados como argumentos para generar el nombre del archivo de una forma muy sencilla.\n\nFinalmente, dada la función anterior, el servidor de nuestra app será:\n\n```{r, eval=FALSE}\nbackend <- function(input, output){\n    \n    resumen <- reactive({\n      data |> \n        filter(color %in% input$color) |> \n        group_by_at(input$agrupacion) |>\n        summarise(\n          Conteo = n()\n        ) |> \n        arrange(desc(Conteo))\n    })\n    \n    output$tabla_resumen <- renderReactable({\n      resumen() |> \n        reactable(\n          pagination = F,\n          compact = T\n        )\n    })\n    \n    output$descarga <- downloadHandler(\n      filename = function(){\n        paste0(\"resumen por \",input$agrupacion,\".xlsx\")\n      },\n      content = function(file){\n        writexl::write_xlsx(\n          x = list(\n            Datos = resumen()\n          ),\n          path = file\n        )\n      }\n    )\n    \n  }\n```\n\n#### Dinamizando reportes\n\nLa descarga de archivos es genial, si, pero este no es ni de cerca el límite en los documentos descargables que podemos generar. Con la suficiente creatividad podemos generar un reporte completo en, por ejemplo, un archivo de excel, que ya se encuentre graficado y customizado para el usuario final.\n\nEn esta sección crearemos desde cero un reporte, utilizando las librerías `openxlsx2` y `mschart`, las cuales nos permitirán hacer la respectiva customización y generación de gráficos. Vamos a trabajar con el set de datos `walmart` (que incluye datos generales sobre ventas por zona, margenes, impuestos, etc), [disponible en el repositorio oficial del libro,](https://github.com/Jorge-hercas/r-para-inteligencia-de-negocios/tree/main/CSV) siendo:\n\n```{r, eval=FALSE}\nwalmart_data <- read.csv(\"https://raw.githubusercontent.com/Jorge-hercas/\n                         r-para-inteligencia-de-negocios/main/CSV/\n                         walmart.csv\")\n```\n\n```{r, echo=FALSE}\nwalmart_data <- read.csv(\"https://raw.githubusercontent.com/Jorge-hercas/r-para-inteligencia-de-negocios/main/CSV/walmart.csv\")\n```\n\nPara este ejercicio, comenzaremos por crear una interfaz que incluya los mismos gráficos/tablas que conformarán el reporte generado en excel. Empecemos con algunas métricas clave:\n\n-   Ciudades por cantidad de ventas\n-   Margen promedio\n-   Ingreso bruto\n-   Ingreso/Número de ventas por día\n-   Categoría de producto más vendida\n\nPara estas métricas, vamos a aplicar algunos filtros generales, con la información de las columnas `Branch`, `City`, `Customer.type`, `Gender` y `Payment`, además de incluir el boton de descarga al final del panel:\n\n```{r, eval=FALSE}\nfiltros_panel <- sidebarPanel(\n      width = 3,\n      align = \"center\",\n      h3(\"Filtros\"),\n      checkboxGroupInput(\n        \"branch\", \"Branch\", choices = unique(walmart_data$Branch),\n        selected = unique(walmart_data$Branch),\n        inline = T\n      ),\n      checkboxGroupInput(\n        \"genero\", \"Genero\", choices = unique(walmart_data$Gender),\n        selected = unique(walmart_data$Gender),\n        inline = T\n      ),\n      selectInput(\n        \"ciudad\", \"Ciudad\", choices = unique(walmart_data$City),\n        selected = unique(walmart_data$City), multiple = T\n      ),\n      selectInput(\n        \"tipo_cliente\", \"Tipo de cliente\", \n        choices = unique(walmart_data$Customer.type),\n        selected = unique(walmart_data$Customer.type), multiple = T\n      ),\n      selectInput(\n        \"tipo_pago\", \"Tipo de pago\", \n        choices = unique(walmart_data$Payment),\n        selected = unique(walmart_data$Payment), multiple = T\n      ),\n      downloadButton(\"download\", \"Descargar reporte\", \n                     icon = icon(\"file-excel\"))\n    )\n```\n\nComo siguiente paso, vamos a generar un panel que incluya tres cajas de texto con las métricas generales *\"margen promedio\"*, *\"ingreso bruto total\"* y *\"categoría más vendida\"*, un gráfico de series de tiempo para los ingresos por fecha en una columna individual que abarque todo el ancho de la pantalla, y dos gráficos en la última columna para representar la proporción de las ventas por ciudad y las ventas por tipo de producto, ordenadas de mayor a menor:\n\n```{r, eval=FALSE}\noutputs_panel <- column(\n      width = 9,\n      column(\n        width = 4,\n        shinydashboard::valueBox(subtitle = \"Margen promedio\", \n                                 value = textOutput(\"margen\"), \n                                 width = 12,\n                                 icon = icon(\"percent\"),\n                                 color = \"green\")\n      ),\n      column(\n        width = 4,\n        shinydashboard::valueBox(subtitle = \"Ingreso bruto total\", \n                                 value = textOutput(\"ingreso_bruto\"), \n                                 width = 12,\n                                 icon = icon(\"dollar-sign\"),\n                                 color = \"green\")\n      ),\n      column(\n        width = 4,\n        shinydashboard::valueBox(subtitle = \"Categoría de producto \n                                 más vendida\", \n                                 value = textOutput(\"categoria\"), \n                                 width = 12,\n                                 icon = icon(\"list\"),\n                                 color = \"green\")\n      ),\n      column(\n        width = 12,\n        align = \"center\",\n        plotOutput(\"historico\")\n      ),\n      column(\n        width = 5,\n        align = \"center\",\n        plotOutput(\"top\")\n      ),\n      column(\n        width = 7,\n        align = \"center\",\n        plotOutput(\"categorias_ventas\")\n      )\n    )\n```\n\nSiendo la interfaz:\n\n```{r, eval=FALSE}\nfrontend <- fluidPage(\n  shinyWidgets::useShinydashboard(),\n  filtros_panel,\n  outputs_panel\n)\n```\n\nNótese que estoy utilizando la función `useShinydashboard()` de la librería `shinyWidgets`, ya que esta me permite exportar las cajas de texto existentes de la librería `shinydashboard` a una página simple, sin tener que usar una interfaz de esta última librería.\n\nEl panel resultante para esta app será:\n\n![Interfaz del reporte](imagenes/reporteventasdescarga.png){fig-align=\"center\" width=\"600\"}\n\nPor supuesto, ahora debemos armar el servidor para poder generar todas estas vistas. Comencemos por crear un objeto reactivo general para aplicar los filtros y calcular el ingreso ($Precio * Cantidad$):\n\n```{r, eval=FALSE}\ndata <- reactive({\n      walmart_data |> \n        filter(Branch %in% input$branch & Gender %in% input$genero \n               & City %in% input$ciudad & Customer.type \n               %in% input$tipo_cliente & Payment %in% input$tipo_pago ) |> \n        mutate(Ingreso = Unit.price*Quantity,\n               Date = floor_date(as_date(Date), \"week\"))\n    })\n```\n\nAhora, a partir de este objeto vamos a generar los gráficos adicionales, sin embargo, las métricas que estamos calculando en estos gráficos también será exportada al reporte que generaremos en excel más adelante, por lo que también guardaremos el resumen de estos datos en objetos reactivos, por ejemplo:\n\n```{r, eval=FALSE}\nhistorico_data <- reactive({\n      data() |> \n        group_by(Date) |> \n        summarise(Ingreso = sum(Ingreso, na.rm = T))\n    })\n\noutput$historico <- renderPlot({\n      historico_data() |> \n        ggplot(aes(x = Date, y = Ingreso)) +\n        geom_area_pattern(pattern = \"gradient\",\n                          pattern_fill = \"white\",\n                          pattern_fill2 = \"#74b47c\") +\n        theme_minimal() + \n        labs(title = \"Ingresos por fecha\") +\n        geom_text(aes(label=scales::comma(Ingreso)), \n                  position=position_dodge(width=0.9), vjust=-0.25)\n    })\n```\n\nEn este ejemplo solamente exportaremos los datos de los gráficos al reporte de excel, por lo que la información de las cajas de texto será calculada directamente en el *output*, ya que no la reutilizaremos para nada más. Dado lo anterior, el servidor será:\n\n```{r, eval=FALSE}\nbackend <- function(input, output, session){\n    \n    data <- reactive({\n      walmart_data |> \n        filter(Branch %in% input$branch & Gender %in% input$genero \n               & City %in% input$ciudad & Customer.type \n               %in% input$tipo_cliente & Payment %in% input$tipo_pago ) |> \n        mutate(Ingreso = Unit.price*Quantity,\n               Date = floor_date(as_date(Date), \"week\"))\n    })\n    \n    output$margen <- renderText({\n      x <-\n      data() |> \n        summarise(\n          margen = mean(gross.margin.percentage, na.rm = T)\n        )\n      \n      scales::percent(x$margen/100, accuracy = 0.1)\n    })\n    \n    output$ingreso_bruto <- renderText({\n      x <-\n        data() |> \n        summarise(Ingreso = sum(Ingreso, na.rm = T))\n      \n      scales::comma(x$Ingreso, accuracy = 0.1, prefix = \"$\")\n    })\n    \n    output$categoria <- renderText({\n      x <-\n        data() |> \n        group_by(Product.line) |> \n        summarise(Ventas = sum(Ingreso, na.rm = T)) |> \n        arrange(desc(Ventas)) \n      \n      stringr::str_trunc(x$Product.line[1], width = 14)\n    })\n    \n    historico_data <- reactive({\n      data() |> \n        group_by(Date) |> \n        summarise(Ingreso = sum(Ingreso, na.rm = T))\n    })\n    \n    output$historico <- renderPlot({\n      historico_data() |> \n        ggplot(aes(x = Date, y = Ingreso)) +\n        geom_area_pattern(pattern = \"gradient\",\n                          pattern_fill = \"white\",\n                          pattern_fill2 = \"#74b47c\") +\n        theme_minimal() + \n        labs(title = \"Ingresos por fecha\") +\n        geom_text(aes(label=scales::comma(Ingreso)), \n                  position=position_dodge(width=0.9), vjust=-0.25)\n    })\n    \n    top_data <- reactive({\n      data() |> \n        group_by(City) |> \n        summarise(Ingreso = sum(Ingreso)) |> \n        mutate(City = forcats::fct_reorder(City, Ingreso))\n    })\n    \n    output$top <- renderPlot({\n      top_data() |> \n        ggplot(aes(area = Ingreso, fill = City, \n                   label = scales::comma(round(Ingreso),\n                                         prefix = \"$\"))) +\n        geom_treemap() + \n        scale_fill_manual(\n          values = c(\"#7c8c84\", \"#74b47c\", \"#32a640\")\n        ) +\n        geom_treemap_text(\n          color = \"white\",\n          padding.y = unit(20, \"mm\"),\n          size = 15) +\n        theme_minimal() + \n        labs(title = \"Ingresos por region\")\n    })\n    \n    categorias_ventas_data <- reactive({\n      data() |> \n        group_by(Product.line) |> \n        summarise(Ventas = sum(Ingreso, na.rm = T)) |> \n        arrange(desc(Ventas))\n    })\n    \n    output$categorias_ventas <- renderPlot({\n      categorias_ventas_data() |> \n        ggplot(aes(y = forcats::fct_reorder(Product.line, \n                                            Ventas), \n                   x = Ventas,\n                   fill = Ventas)) + \n        geom_col(width = 0.5) + \n        scale_fill_viridis_b() + \n        theme_minimal() + \n        labs(y = \"Cateogoría de producto\")\n    })\n    \n  }\n```\n\nNótese que, para el gráfico `historico` utilizamos la función `geom_area_pattern()` de la librería `ggpattern`, esto para poder generar un degradado en el gráfico de series de tiempo que nos indique los puntos de ingreso máximos, mientras que para el gráfico `top`, utilizamos las funciones `geom_treemap_text()` y `geom_treemap_text()` de la librería `treemapify`, esto para poder generar un gráfico categórico de este estilo, por tanto, las librerías a utilizar en la sección global serán:\n\n```{r, eval=FALSE}\nlibrary(ggplot2)\nlibrary(shiny)\nlibrary(treemapify)\nlibrary(ggpattern)\nlibrary(shinyWidgets)\nlibrary(shinydashboard)\n```\n\nClaro que, para el caso de `shinyWidgets` solo vamos a importar una función, por lo que es más conveniente llamarla mediante `shinyWidgets::useShinydashboard()`, en lugar de cargar completamente la librería.\n\nAhora, ya que tenemos los objetos que exportaremos al reporte descargable (`historico_data()`, `top_data()` y `categorias_ventas_data()`), vamos a crear el documento excel, utilizando la librería `openxlsx2`. Primero, debemos crear un objeto que será nuestro libro de trabajo y añadirle las tres hojas respectivas a cada objeto reactivo, acción que haremos de la siguiente manera:\n\n```{r, eval=FALSE}\nlibrary(openxlsx2)\ncolores <- RColorBrewer::brewer.pal(3, \"RdYlGn\")\n\nwb_workbook() |> \n  wb_add_worksheet(\"Datos historicos\", \n                   gridLines = F, tabColor = colores[1]) |> \n  wb_add_worksheet(\"Tops\", \n                   gridLines = F, tabColor = colores[2]) |> \n  wb_add_worksheet(\"Categorías\", \n                   gridLines = F, tabColor = colores[3])\n\n```\n\nDonde cada hoja tiene un color asignado de la paleta de colores `RdYlGn`. A pesar de que las hojas de nuestro reporte son estáticas, no podemos guardarlas en la sección global, ya que el objeto `wb_workbook()` es un objeto de clase `R6`, un tipo de objeto mutable, es decir, que no necesita volver a ser guardado para modificar su estructura original con nuevas líneas de código, por lo que necesariamente tendremos que crear un libro nuevo cada que descarguemos el reporte. Vamos a añadir las gráficas respectivas a cada caso, utilizando la librería `mschart`. Comencemos por el gráfico de series de tiempo para el objeto `historico_data()`:\n\n```{r, eval=FALSE}\nlibrary(mschart)\n\nwb_workbook() |> \n  wb_add_worksheet(\"Datos historicos\", \n                   gridLines = F, tabColor = colores[1]) |> \n  wb_add_worksheet(\"Tops\", \n                   gridLines = F, tabColor = colores[2]) |> \n  wb_add_worksheet(\"Categorías\", \n                   gridLines = F, tabColor = colores[3]) |> \n  wb_add_mschart(\n    sheet = \"Datos historicos\",\n    graph = ms_linechart(\n            historico_data(),\n            x = \"Fecha\", y = \"ingresos\") |>\n            chart_ax_x(num_fmt = \"[$-es-ES]mmm yyyy\") |> \n            chart_ax_y(num_fmt = \"#,##0⁠\"),\n          dims = \"D2\")\n```\n\nResultando en el siguiente objeto, cuando descarguemos el reporte:\n\n![Gráfico histórico de ventas](imagenes/reporteexcel.png){fig-align=\"center\" width=\"500\"}\n\nNótese que es posible customizar el formato de los ejes en los gráficos, siendo que, en este caso particular, añadimos una separación a los dígitos con `\"#,##0⁠`, ademas de establecerla fecha en formato *mes-año*.\n\nYa que tenemos clara la estructura para añadir objetos al reporte, vayamos con el resto de hojas:\n\n```{r, eval=FALSE}\nwb_workbook() |> \n  wb_add_worksheet(\"Datos historicos\", \n                   gridLines = F, tabColor = colores[1]) |> \n  wb_add_worksheet(\"Tops\", \n                   gridLines = F, tabColor = colores[2]) |> \n  wb_add_worksheet(\"Categorías\", \n                   gridLines = F, tabColor = colores[3]) |> \n  wb_add_mschart(\n    sheet = \"Datos historicos\",\n    graph = ms_linechart(\n            historico_data(),\n            x = \"Fecha\", y = \"ingresos\") |>\n            chart_ax_x(num_fmt = \"[$-es-ES]mmm yyyy\") |> \n            chart_ax_y(num_fmt = \"#,##0⁠\"),\n          dims = \"D2\") |> \n  wb_add_mschart(\n            sheet = \"Tops\",\n            graph = ms_barchart(\n              top_data(),x = \"City\", y = \"Ingreso\") |>\n              chart_ax_x(num_fmt = \"[$-es-ES]mmm yyyy\") |>\n              chart_ax_y(num_fmt = \"#,##0⁠\"),\n            dims = \"D2\") |> \n  wb_add_mschart(\n            sheet = \"Categorías\",\n            graph = ms_barchart(\n              categorias_ventas_data(),x = \"Product.line\", \n              y = \"Ventas\") |>\n              chart_ax_x(num_fmt = \"[$-es-ES]mmm yyyy\") |>\n              chart_ax_y(num_fmt = \"#,##0⁠\"),\n            dims = \"D2\") |> \n  wb_save(path = file)\n```\n\nDonde `file` será el parámetro creado con `filename` en la función `downloadHandler()` que usaremos para crear el reporte.\n\nFinalmente, la función completa será:\n\n```{r, eval=FALSE}\noutput$download <- downloadHandler(\n      filename = function() {\n        paste0(\"Reporte de ventas-\", Sys.Date(), \".xlsx\")\n      },\n      content = function(file) {\n        wb_workbook() |> \n          wb_add_worksheet(\"Datos historicos\", \n                           gridLines = F, tabColor = colores[1]) |> \n          wb_add_worksheet(\"Tops\", \n                           gridLines = F, tabColor = colores[2]) |> \n          wb_add_worksheet(\"Categorías\", \n                           gridLines = F, tabColor = colores[3]) |> \n          wb_add_mschart(\n            sheet = \"Datos historicos\",\n            graph = ms_linechart(\n              historico_data(),x = \"Date\", y = \"Ingreso\") |>\n              chart_ax_x(num_fmt = \"[$-es-ES]mmm yyyy\") |>\n              chart_ax_y(num_fmt = \"#,##0⁠\"),\n            dims = \"D2\") |> \n          wb_add_mschart(\n            sheet = \"Tops\",\n            graph = ms_barchart(\n              top_data(),x = \"City\", y = \"Ingreso\") |>\n              chart_ax_x(num_fmt = \"[$-es-ES]mmm yyyy\") |>\n              chart_ax_y(num_fmt = \"#,##0⁠\"),\n            dims = \"D2\") |> \n          wb_add_mschart(\n            sheet = \"Categorías\",\n            graph = ms_barchart(\n              categorias_ventas_data(),x = \"Product.line\", \n              y = \"Ventas\") |>\n              chart_ax_x(num_fmt = \"[$-es-ES]mmm yyyy\") |>\n              chart_ax_y(num_fmt = \"#,##0⁠\"),\n            dims = \"D2\") |> \n          wb_save(path = file)\n      }\n    )\n```\n\nResultando en:\n\n::: {#fig-excel layout-ncol=\"2\"}\n![Gráfico por ciudad](imagenes/reporteexcel1.png){#fig-excel1 align=\"center\" width=\"300\"}\n\n![Gráfico por categoría](imagenes/reporteexcel2.png){#fig-excel2 align=\"center\" width=\"300\"}\n\nGráficos generados en un reporte dinámico\n:::\n\nAl estar ligado este documento a objetos reactivos, el resultado dependerá enteramente de los filtros y opciones que tengamos activas en nuestra app, lo que lo vuelve un reporte más dinámico.\n\nEl código de esta app es considerablemente más largo que el de los ejemplos anteriores, por lo que lo encontrarás completo en el [repositorio oficial del libro](https://github.com/Jorge-hercas/r-para-inteligencia-de-negocios/blob/main/Apps/app%20reporte%20excel.R)\n\nPor supuesto, esta solo es una introducción a los reportes que podemos generar utilizando shiny apps, siendo que podemos customizarlos aún más, generar formatos condicionales, añadir fórmulas, etc, ejemplos que no tocaremos ahora.\n\n### Customización de interfaces con HTML y CSS\n\nEn este punto ya tenemos bastante claro que una shiny app no es más que una forma de expresar código HTML, CSS y JavaScript mediante funciones de R, lo que hace lógico que podamos implementar elementos diréctamente escritos en estos lenguajes para poder customizar de manera más profunda nuestras apps. En esta sección nos enfocaremos meramente en HTML y CSS, ya que el uso de JavaScript en los programas que realicemos estará más relacionado a aspectos de funcionamiento de los widgets (sesión, interactividad y eventos).\n\n#### HTML\n\nComo ya vimos con anterioridad, las funciones dedicadas a la interfaz, como `fluidPage()`, son una forma de llamar contenedores de elementos, por ejemplo:\n\n```{r, eval=FALSE}\nfluidPage(\n  column(\n    width = 10\n  )\n)\n```\n\nEquivalente en lenguaje HTML a:\n\n```{r, eval=FALSE}\n<div class=\"container-fluid\">\n  <div class=\"col-sm-10\"></div>\n</div>\n```\n\nDonde `class` hace referencia a ciertos parámetros asignados a ese contenedor, que le darán una apariencia por default (la cual estará establecida por un estilo escrito en lenguaje CSS).\n\nTodos estos elementos, sin importar la clase, son conocidos como `tags`. Dada la denominación anterior, una forma sencilla en la que podemos acceder a dichos elementos dentro de nuestra shiny app es llamándolos a partir del objeto del mismo nombre, por ejemplo:\n\n```{r, eval=FALSE}\ntags$a()\n```\n\nUsualmente, cuando queremos establecer algunos parámetros de customización, establecer la codificación de la página (como *UTF-8*) o referenciar código adicional ubicado en otro archivo, haremos uso del tag `head()`, con el que asignaremos información que se va a aplicar a todo nuestro programa (de manera similar a la función `options()`, con la cual establecemos opciones en una sesión de R). Este tag debe ser escrito al principio de nuestro contenedor principal (`fluidPage()` en la mayoría de los casos), en la interfaz de la shiny app, por ejemplo:\n\n```{r, eval=FALSE}\nfluidPage(\n  tags$head(\n    tags$meta(charset = \"UTF-8\"),\n    tags$link(rel = \"stylesheet\", type = \"text/css\", href = \"estilo.css\")\n  ),\n  ...\n)\n```\n\nSiendo que en este caso, utilizando el tag `meta`, establecimos la codificación que seguirán las cadenas de texto y demás elementos en nuestra interfaz, y mediante el tag `link` hacemos referencia a un archivo adicional que contiene parámetros CSS que nos ayudarán a customizar nuestra app. En casos en los que tenemos programas demasiado grandes, los cuales cuentan con una cantidad considerable de páneles y/o elementos específicos, es preferible separar el parámetro `style` en un archivo separado, lo que nos permitirá tener un código mucho más limpio. Si bien es posible establecer el código CSS en la sección `head()` mediante el tag `style()`, por ejemplo:\n\n```{r, eval=FALSE}\nfluidPage(\n  tags$head(\n    tags$style(\n      HTML(\"body {\n           color: red;\n      }\")\n    )\n  ),\n  ...\n)\n```\n\nEsto solo es recomendable cuando la cantidad de elementos a customizar es corta, ya que esta sección puede crecer muy rápidamente y volver bastante confuso el código de nuestra interfaz.\n\nEn la sección `head()` también podemos incluir parámetros más específicos, como códigos escritos en javaScript para insertar animaciones/elementos interactivos, la descripción de la app, etc, por ejemplo:\n\n```{r, eval=FALSE}\nfluidPage(\n  tags$head(\n    tags$title(\"Nombre de mi app\"),\n    tags$script(src = \"codigo para animacion.js\")\n  ),\n  ...\n)\n```\n\nDebemos ser cuidadosos cuando añadamos códigos en JavaScript,\n\nTípicamente, los tags que más comunmente usaremos en el desarrollo de la interfaz serán:\n\n1.  Estructurales:\n\n-   `head()`: Para añadir metadatos.\n-   `main()`: Para priorizar contenido en una página.\n-   `header()`: Para añadir encabezados.\n-   `footer()`: Para añadir pies de página.\n-   `style()`: Para aplicar formato CSS.\n-   `script()`: Para incluir código en JavaScript.\n-   `link()`: Para incluir referencias a otros códigos (principalmente de archivos CSS más largos).\n\n2.  De formato:\n\n-   `p()`: Para añadir un párrafo.\n-   `strong()`: Para texto en negritas.\n-   `em()`: Para texto en cursiva.\n-   `a()`: Para hipervínculos.\n-   `img()`: Para imágenes.\n-   `h1()`-`h6()`: Para títulos/encabezados, siendo `h1()` el más grande y `h6()` el más pequeño.\n-   `br()`: Para generar un espaciado entre elementos.\n-   `hr()`: Para generar una línea horizontal divisora.\n-   `div()`: Para una columna contenedora de otros elementos.\n-   `li()`: Para listas ordenadas numéricamente (o alfabéticamente, usando el parámetro `type=\"A\"`). Va escrito dentro del tag `ol()`.\n\n3.  Contenedores:\n\n-   `section()`: Para definir secciones completas (vistas dentro de páginas). Útil cuando queremos aplicar estilos CSS de forma más general, afectando solo una parte específica de la app.\n-   `div()`: Para columnas y subcolumnas. Suele estar dentro de `section()`.\n-   `span()`: Para alinear elementos.\n\nAdicionalmente, también existen tags enfocados a la creación de elementos de tipo tabla, sin embargo, es poco probable que los utilicemos en shiny apps, ya que usualmente crearemos estos elementos a partir de recursos adicionales, como `kable()` de la librería `knitr` o `reactable()` de la librería con el mismo nombre.\n\n#### CSS\n\nYa que tenemos la estructura simple de una app mediante el uso de HTML (usando tags o funciones predefinidas, como es el caso de `fluidPage()`, `column()`, etc), podemos comenzar a darle un estilo más agradable para el usuario final, utilizando código en lenguaje CSS (Cascading Style Sheets). Mediante CSS podemos modificar aspectos tales como el espaciado, tamaño de los márgenes, color y tamaño del texto (independientemente de que se trate de títulos o texto de párrafos), color del fondo de pantalla, entre muchos otros parámetros. Ahora, ¿cómo aplicamos el código CSS en una shiny app? Tenemos tres formas de hacer esto:\n\n1.  Aplicándolo directamente en cada contenedor y elemento, por ejemplo:\n\n    ```{r, eval=FALSE}\n    fluidPage(\n      column(\n        width = 1,\n        style = \"background-color:red;\n                 border: 10px;\"\n      )\n    )\n    ```\n\n    Nótese que, para poder aplicar un parámetro nuevo, tenemos que finalizar el parámetro anterior mediante un signo `;`.\n\n    Escribir el estilo del elemento HTML dentro del código de nuestra interfaz no siempre es la mejor opción, ya que tenderá a volver confuso el código conforme lo vayamos escalando.\n\n2.  Aplicar el código CSS en la sección `head()`, por ejemplo:\n\n    ```{r, eval=FALSE}\n    fluidPage(\n        tags$head(\n          tags$style(\n            HTML(\n              \".col-sm-5{\n                  background-color:red;\n                  border: 10px;\n                 }\"\n            )\n          )\n        ),\n        column(\n          width = 5,\n          p(\"perro\")\n        )\n      )\n    ```\n\n    Donde `.col-sm-5` es la denominación dada al elemento `column(width = 5)`, con la que podremos identificar a qué elemento vamos a aplicarle dicho cambio.\n\n    En este caso estamos estractando todo el código CSS en una sola sección de la interfaz, lo que lo hace más fácil de interpretar pero aún nos limita a una corta escalabilidad, al tener que introducir demasiadas líneas de código dentro de la interfaz aún.\n\n3.  Referenciar el código CSS únicamente en la interfaz, escribiéndolo en un archivo separado que guardaremos en la carpeta **www**, por ejemplo:\n\n    ```{r, eval=FALSE}\n    fluidPage(\n        tags$head(\n          tags$link(rel = \"stylesheet\", \n                    type = \"text/css\", \n                    href = \"estilo.css\")\n        ),\n        column(\n          width = 5\n        )\n      )\n    ```\n\n    Y, en el código CSS:\n\n    ```{css, eval=FALSE}\n    .col-sm-5 {\n      background-color:red;\n      border: 10px;\n    }\n    ```\n\nClaramente, el uso de un archivo separado siempre será la mejor opción, ya que nos permitirá el añadir una cantidad considerable de elementos a nuestra interfaz, sin que esto se vuelva confuso conforme vayan aumentando.\n\nRetomemos al ejemplo anterior, donde aplicamos un estilo a un elemento `div` con `width=5`. Este código tiene un problema: Este estilo se aplicará a todos los contenedores que tengan el parámetro `width=5`, ya que coinciden en el nombre de la clase. Es casi seguro que nos encontraremos con situaciones en las que solo necesitemos aplicar un cierto estilo a un elemento único, sin que los demás contenedores (u otros elementos) sean afectados por ello. Para poder aplicar estilos de una forma mucho más específica, simplemente tenemos que añadir un identificador (o ID) al elemento HTML que necesitemos trabajar y, del lado del código CSS, llamarlo con ese ID, por ejemplo:\n\n```{r, eval=FALSE}\ncolumn(\n  id = \"col_per\",\n  width = 5\n)\n```\n\nY, en el código CSS:\n\n```{css, eval=FALSE}\n#col_per {\n  background-color:red;\n  border: 10px;\n}\n```\n\n#### Temas predefinidos\n\nLa versatilidad que tenemos para poder aplicar código HTML/CSS en nuestras apps es grandioza, ya que nos permite tener un control total de la apariencia que queremos que adopten, siendo únicamente limitados por nuestra creatividad, sin embargo, cuando queremos evitarnos todo el trabajo que esto conlleva, podemos hacer uso de algunos temas ya predefinidos para realizar la customización de manera rápida. Podemos acceder a estos estilos mediante algunas librerías, como `shinythemes`, `bslib`, `semantic.page`, `shinyWidgets`, entre otras. Tomemos como ejemplo el programa que creamos con anterioridad para descargar reportes en excel y vamos a añadirle un tema predefinido de `shinythemes`, siendo en este caso el tema `simplex` (véanse una lista detallada con todos los temas disponibles en [el siguiente link](https://rstudio.github.io/shinythemes/)), haciendo uso de una sola línea de código:\n\n```{r, eval=FALSE}\nlibrary(shinythemes)\n\nfluidPage(\n    theme = shinytheme(theme = \"simplex\"),\n    shinyWidgets::useShinydashboard(),\n    filtros_panel,\n    outputs_panel\n  )\n```\n\nLo que resultará en:\n\n![App con un tema predefinido](imagenes/cerulean.png){fig-align=\"center\" width=\"370\"}\n\nHaciendo cambios en la fuente, color de fondo, estilo de los *inputs*, etc. Esta librería simplemente toma el nombre de clase de todos los elementos existentes y les aplica un formato en CSS, que aloja en un archivo separado que es llamado de forma condicional, dependiendo del tema elegido.\n\nUsar `shinythemes` es la vía más sencilla para cambiar el aspecto de nuestras apps, pero los temas predefinidos no siempre serán de nuestro agrado, por lo que podemos hacer uso de otros recursos más flexibles, como es el caso de `bslib`. Con esta librería la customización se basa en reemplazar elementos de nuestra interfaz con aquellos provenientes de ella, por ejemplo: Originalmente, el programa que construimos utilizaba un `sidebar` donde almacenábamos los *inputs*, el cual podemos eliminar y, en su lugar, almacenar todos estos elementos dentro de una página `page_sidebar()` de `bslib`, que ya cuenta por default con este elemento, siendo que únicamente debemos llamarlo de la siguiente manera:\n\n```{r, eval=FALSE}\nlibrary(bslib)\n\npage_sidebar(\n  title = \"Ejemplo de un reporte de ventas\",\n  sidebar = sidebar(\n    h3(\"Filtros\"),\n    checkboxGroupInput(\n      \"branch\", \"Branch\", choices = unique(walmart_data$Branch),\n      selected = unique(walmart_data$Branch),\n      inline = T\n    ),\n    checkboxGroupInput(\n      \"genero\", \"Genero\", choices = unique(walmart_data$Gender),\n      selected = unique(walmart_data$Gender),\n      inline = T\n    ),\n    ...\n  ),\n  ...\n)\n\n```\n\nDe igual forma, podemos cambiar las cajas de texto que usamos con anterioridad por otras provenientes de `bslib`, utilizando la función `value_box()` en su lugar, aplicando además un contenedor que les dará un `width` automático, mediante la función , siendo en este caso:\n\n```{r, eval=FALSE}\nlayout_column_wrap(\n    value_box(title = \"Margen promedio\",\n              min_height = \"100px\",\n              value = textOutput(\"margen\"),\n              showcase = icon(\"percent\"),\n              theme = \"text-success\", \n              width = \"300px\"\n    ),\n    ...\n  )\n```\n\nDonde los cambios en argumentos son mínimos, por lo que la transición entre una interfaz y otra es bastante sencilla. En este caso, el código completo de la interfaz ahora será:\n\n```{r, eval=FALSE}\npage_sidebar(\n  title = \"Ejemplo de un reporte de ventas\",\n  sidebar = sidebar(\n    h3(\"Filtros\"),\n    checkboxGroupInput(\n      \"branch\", \"Branch\", choices = unique(walmart_data$Branch),\n      selected = unique(walmart_data$Branch),\n      inline = T\n    ),\n    checkboxGroupInput(\n      \"genero\", \"Genero\", choices = unique(walmart_data$Gender),\n      selected = unique(walmart_data$Gender),\n      inline = T\n    ),\n    selectInput(\n      \"ciudad\", \"Ciudad\", choices = unique(walmart_data$City),\n      selected = unique(walmart_data$City), multiple = T\n    ),\n    selectInput(\n      \"tipo_cliente\", \"Tipo de cliente\", \n      choices = unique(walmart_data$Customer.type),\n      selected = unique(walmart_data$Customer.type), multiple = T\n    ),\n    selectInput(\n      \"tipo_pago\", \"Tipo de pago\", choices = unique(walmart_data$Payment),\n      selected = unique(walmart_data$Payment), multiple = T\n    ),\n    downloadButton(\"download\", \"Descargar reporte\", \n                   icon = icon(\"file-excel\"))\n  ),\n  layout_column_wrap(\n    value_box(title = \"Margen promedio\",\n              min_height = \"100px\",\n              value = textOutput(\"margen\"),\n              showcase = icon(\"percent\"),\n              theme = \"text-success\", \n              width = \"300px\"\n    ),\n    value_box(title = \"Ingreso bruto total\",\n              min_height = \"100px\",\n              value = textOutput(\"ingreso_bruto\"),\n              showcase = icon(\"dollar-sign\"),\n              theme = \"text-success\"),\n    value_box(title = \"Categoría de producto más vendida\",\n              min_height = \"100px\",\n              value = textOutput(\"categoria\"),\n              showcase = icon(\"list\"),\n              theme = \"text-success\")\n  ),\n  br(),\n  column(\n    width = 12,\n    fluidRow(\n      column(\n        width = 12,\n        align = \"center\",\n        plotOutput(\"historico\")\n      ),\n      column(\n        width = 5,\n        align = \"center\",\n        plotOutput(\"top\")\n      ),\n      column(\n        width = 7,\n        align = \"center\",\n        plotOutput(\"categorias_ventas\")\n      )\n    )\n  )\n)\n```\n\nResultando en:\n\n::: {#fig-sidebar layout-ncol=\"2\"}\n![Sidebar abierto](imagenes/sidebarabierto.png){#fig-sidebar1 align=\"center\" width=\"300\"}\n\n![Sidebar cerrado](imagenes/sidebarcerrado.png){#fig-sidebar2 align=\"center\" width=\"300\"}\n\nInterfaz actualizada con `bslib`\n:::\n\nUna interfaz mucho más minimalista y agradable a la vista (aunque claro, eso dependerá del gusto de cada persona). Como podrás darte cuenta, el código de esta interfaz es muy similar al código original, prácticamente cambiando las funciones a llamar para cada contenedor por aquellas provenientes de `bslib`, pero manteniendo prácticamente igual la estructura de nuestra aplicación.\n\nPor supuesto, encontrarás el código completo de esta versión en [el repositorio oficial del libro](https://github.com/Jorge-hercas/r-para-inteligencia-de-negocios/blob/main/Apps/App%20reporte%20excel%20bslib.R).\n\nActualmente, las librerías más populares para customizar de manera sencilla nuestras apps son:\n\n-   `shinythemes`: Temas genéricos para usar de manera rápida.\n-   `shiny.semantic`: Diseños más minimalistas de interfaces.\n-   `miniUI`: Diseños responsivos para pantallas pequeñas.\n-   `bslib`: Diseños más minimalistas de interfaces.\n-   `bsplus`: Funciones con elementos adicionales, como acordeones, textos flotantes, etc.\n-   `fresh`: Funciones para customizar las paletas de colores en dashboards (`shinydashboard` y `bs4Dash`).\n-   `designer`: App interactiva para generar customización en tiempo real y exportar el código.\n\nExisten otras librerías dedicadas a este aspecto, pero en la gran mayoría de los casos las librerías mencionadas anteriormente serán más que suficiente. Más adelante analizaremos librerías enfocadas a la creación de dashboards, como `bs4Dash`, `shinydashboard`, `argonDash`, entre otras.\n\n#### Plantillas (HTML templates)\n\nProbablemente en más de una ocasión te has encontrado con un sitio web y has pensado algo como *\"wow, este sitio tiene un diseño increíble, quisiera adaptarlo a un proyecto mío\"*. Bueno, esto es posible en shiny apps. Así como podemos llamar código CSS en la sección `head()` de una app, podemos referenciar una plantilla completa e introducir elementos (*inputs* y *outputs*) diréctamente en un archivo de código HTML, ¿Cómo hacemos esto? Esto es un proceso bastante sencillo: Generalmente, cuando descargamos una plantilla HTML de internet, esta viene con múltiples archivos conectados entre si (códigos javaScript, customización CSS, imágenes, etc). Para adaptar la plantilla a nuestro proyecto, todos los archivos referentes a este *template* serán guardados con la misma estructura con la que los descargamos dentro de la carpeta **www**, que ya hemos mencionado con anterioridad. Una vez guardados, nos vamos a centrar en uno en particular llamado `index.html`, el cual contiene la estructura de la plantilla descargada, por lo que será el que vamos a referenciar en nuestra app. Suponiendo que ya tenemos el proyecto en la carpeta **www**, el código de nuestra app se verá de la siguiente manera:\n\n```{r, eval=FALSE}\nshinyApp(\n  ui = htmlTemplate(\n    filename = \"index.html\"\n  ),\n  server = function(input, output, session){\n    ...\n  }\n)\n```\n\nAhora, del lado del código principal de la plantilla, supongamos la siguiente estructura en el documento `index.html`:\n\n```{html, eval=FALSE}\n<!doctype html>\n<html>\n<head>\n  <title>Ejemplo</title>\n  <meta name=\"descripcion\" content=\"Ejemplo\">\n  <meta name=\"ex\" content=\"Ejemplo\">\n</head>\n<body>\n   Ejemplo de un texto simple.\n</body>\n</html>\n```\n\nPara poder añadir los elementos de nuestra app, debemos comenzar en la sección `<head>`, añadiendo el código `headContent()` de la siguiente manera (una plantilla real será mucho, muchísimo más grande, pero la estructura será muy parecida):\n\n```{html, eval=FALSE}\n<!doctype html>\n<html>\n<head>\n  <title>Ejemplo</title>\n  <meta name=\"descripcion\" content=\"Ejemplo\">\n  <meta name=\"ex\" content=\"Ejemplo\">\n  {{headContent()}}\n</head>\n<body>\n   Ejemplo de un texto simple.\n</body>\n</html>\n```\n\nFinalmente, para añadir elementos de nuestra app a la plantilla, simplemente debemos guardarlos como objetos dentro del cuerpo `htmlTemplate()` y referenciarlos dentro del código de la plantilla, por ejemplo, del lado del código R:\n\n```{r, eval=FALSE}\nshinyApp(\n  ui = htmlTemplate(\n    filename = \"index.html\",\n    pagina_principal = fluidPage(\n      column(\n        width = 3,\n        selectInput(...),\n        ...\n      )\n    )\n  ),\n  server = function(input, output, session){\n    ...\n  }\n)\n```\n\nY, consecuentemente, del lado de la plantilla:\n\n```{html, eval=FALSE}\n<!doctype html>\n<html>\n<head>\n  <title>Ejemplo</title>\n  <meta name=\"descripcion\" content=\"Ejemplo\">\n  <meta name=\"ex\" content=\"Ejemplo\">\n  {{headContent()}}\n</head>\n<body>\n  {{pagina_principal}}\n</body>\n</html>\n```\n\nAdaptar plantillas a una app es bastante sencillo, además de que podemos tener un resultado bastante interesante, aunque esto ya dependerá de qué tan bien sepamos adaptar los diseños de los elementos de nuestra app (*inputs* y *outputs*) al diseño del template, procurando que estos dos sean compatibles en formas/paletas de colores.\n\nA continuación te presento un ejemplo de una app realizada con una plantilla ([con el código disponible en el siguiente enlace](https://github.com/socapal/dataton-tudinero)):\n\n::: {#fig-htmltemplate layout-ncol=\"2\"}\n![Primer panel de la app](imagenes/htmltemplate1.png){#fig-htmltemplate1 align=\"center\" width=\"300\"}\n\n![Segundo panel de la app](imagenes/htmltemplate2.png){align=\"center\" width=\"300\"}\n\nAplicación realizada con una HTML Template\n:::\n\n::: callout-warning\n## Cuidado\n\nNo todas las plantillas HTML existentes son compatibles con los *inputs*/*outputs* de una shiny app, por lo que debemos ser cuidadosos al escogerlas. Generalmente será una dinámica de prueba-error.\n:::\n\n### Dashboards con Shiny\n\nPor supuesto, una de las mejores formas de presentar nuestros datos será dentro de un dashboard (tablero), ya que podremos condensar mucha información en varios páneles, de una forma entendible para el usuario final, pero ¿cómo lo podemos hacer en shiny? De manera estricta **no** necesitamos librerías adicionales para crear una visualización más al estilo de un dashboard, ya que en realidad lo hemos aplicado en los ejemplos que estuvimos trabajado anteriormente, al ser programas que dan un monitoreo y visualización sencilla de interpretar a ciertos datos, para que el usuario final pueda sacar cálculos y conclusiones de manera rápida (así como exportar la información), sin embargo, estos ejemplos han sido basdante básicos en cuanto a la interfaz usada, por lo que el objetivo de esta sección será el de entender cómo funcionan las librerías que están enteramente enfocadas a la creación de dashboards, cómo podemos adaptarlas a nuestros proyectos y, lógicamente, optimizarlas para su escalabilidad.\n\nPara la creación de dashboards más especializados, las librerías más populares actualmente son `shinydashboard`, `shinydashboardPlus`, `argonDash` (en conjunto con `argonR`), `tablerDash`, `bs4Dash`, entre otras, paquetes que funcionan de manera muy similar, por lo que los siguientes ejemplos y desarrollos serán realizados con `bs4Dash`, haciendo uso del mismo set de datos `walmart_data`, que trabajamos en secciones anteriores.\n\nSí, los dashboards tienen como objetivo el centralizar los datos de un área del negocio o proyecto mediante resúmenes (gráficos, tablas, valores), concentrando una cierta cantidad de métricas en una sola vista. Sin embargo, eso no implica que debamos meter todos estos elementos en la misma página, ya que eso solo volvería confuso al tablero que estemos creando. Lo más óptimo siempre será dividir las vistas por tópicos. Además, existen distintos tipos de dashboards ---estratégicos, tácticos y operativos--- según el nivel de análisis y el público al que están dirigidos, aunque este tema no se abordará a fondo aquí. Por ejemplo: en un dashboard que esté destinado a mostrar los flujos de efectivo de una empresa, podemos dividir el tablero en: 1. Ingresos, 2. Costos y 3. Márgenes de ingresos, lo que evitará que el usuario que haga uso del dashboard se sature y se confunda con los números mostrados. Considerando el set de datos `walmart_data`, comencemos por crear una interfaz que estará dividida en dos ventanas: 1. Ingresos generales y 2. Márgenes. En este caso, para crear la interfaz, utilizaremos la función `dashboardPage()`:\n\n```{r, eval=FALSE}\nlibrary(bs4Dash)\n\ndashboardPage(\n  header = dashboardHeader(),\n  footer = dashboardFooter(),\n  sidebar = dashboardSidebar(),\n  controlbar = dashboardControlbar(),\n  body = dashboardBody()\n)\n\n```\n\nDonde `header`, `footer`, `sidebar`, `controlbar` y `body` son elementos necesarios para la creación de esta interfaz. Para crear el menú de secciones, tendremos que añadir los elementos dentro de la función `sidebarMenu()`, añadiéndoles un ID con el que podremos acceder a ellos más tarde, es decir:\n\n```{r, eval=FALSE}\ndashboardPage(\n  header = dashboardHeader(),\n  footer = dashboardFooter(),\n  sidebar = dashboardSidebar(\n    sidebarUserPanel(name = \"Dashboard de ventas\"),\n    sidebarMenu(\n      id = \"menu\",\n      menuItem(\n        text = \"Ingresos generales\",\n        tabName = \"ing_gen\",\n        icon = icon(\"money-bill\")\n      ),\n      menuItem(\n        text = \"Márgenes\",\n        tabName = \"marg\",\n        icon = icon(\"percent\")\n      )\n    )\n  ),\n  controlbar = dashboardControlbar(),\n  body = dashboardBody()\n)\n```\n\nResultando en:\n\n![Interfaz mediante bs4Dash](imagenes/bs4dashvista.png){fig-align=\"center\" width=\"370\"}\n\nCon esta primera vista podemos comenzar a notar algunas cosas, como el hecho de que las secciones `sidebar` y `controlbar` pueden ser ocultadas, lo que permite generar una visualización más limpia de los elementos que decidamos incluir en el cuerpo del dashboard. Cuando hablamos de la *no saturación* de elementos en la interfaz, eso también implica que no siempre vamos a querer visualizar los *inputs*, por lo que podemos hacer uso de la sección `controlbar` para almacenarlos y ocultarlos. Vamos a tomar los filtros que usamos para el reporte creado anteriormente, guardándolos en el `controlbar`:\n\n```{r, eval=FALSE}\ndashboardControlbar(\n      width = 350,\n      id = \"controlbar\",\n      controlbarMenu(\n        id = \"menu_controlbar\",\n        controlbarItem(\n          \"Filtros\",\n          \"Filtros generales:\",\n          column(width = 12, alig = \"center\",\n                 checkboxGroupInput(\n                   \"branch\", \"Branch\", \n                   choices = unique(walmart_data$Branch),\n                   selected = unique(walmart_data$Branch),\n                   inline = T\n                 ),\n                 checkboxGroupInput(\n                   \"genero\", \"Genero\", \n                   choices = unique(walmart_data$Gender),\n                   selected = unique(walmart_data$Gender),\n                   inline = T\n                 ),\n                 selectInput(\n                   \"ciudad\", \"Ciudad\", \n                   choices = unique(walmart_data$City),\n                   selected = unique(walmart_data$City), \n                   multiple = T\n                 ),\n                 selectInput(\n                   \"tipo_cliente\", \"Tipo de cliente\", \n                   choices = unique(walmart_data$Customer.type),\n                   selected = unique(walmart_data$Customer.type), \n                   multiple = T\n                 ),\n                 selectInput(\n                   \"tipo_pago\", \"Tipo de pago\", \n                   choices = unique(walmart_data$Payment),\n                   selected = unique(walmart_data$Payment), \n                   multiple = T\n                 ),\n                 downloadButton(\"download\", \"Descargar reporte\", \n                                icon = icon(\"file-excel\"))\n          )\n        )\n      )\n    )\n```\n\nLo que nos devolverá:\n\n![Panel para filtros](imagenes/bs4dashfiltros.png){fig-align=\"center\" width=\"160\"}\n\nElemento que se encontrará oculto por default. Esconder estos elementos permite que, durante la presentación de este dashboard, el contenido central no se preste a distracciones innecesarias, lo que lo hará más eficiente respecto a las métricas que se quieran abordar en un cierto momento.\n\n#### Gráficos, tablas y mapas basados en JavaScript\n\nYa que terminamos con los elementos generales del dashboard, vamos a continuar con la implementación del cuerpo, es decir, gráficos y tablas, pero en esta ocasión vamos a añadirle un toque extra. El uso de gráficos basados en la librería `ggplot2` es genial, ya que podemos customizarlos de forma casi ilimitada, sin embargo, aún siguen siendo imágenes estáticas, por lo que obtener información en tiempo real de ellos se vuelve complicado, por ejemplo: En un gráfico donde no todas las barras se encuentren etiquetadas con el valor del eje *y*, será sumamente complicado inferir estos valores de forma exacta. Para poder obtener información adicional, así como realizar ciertas acciones dentro del mismo gráfico o tabla (sin utilizar *inputs*), podemos hacer uso de librerías basadas en código JavaScript, las cuales contienen *Html Widgets*, como se les denomina a este tipo de elementos visuales. En esa sección nos limitaremos al uso de tres librerías específicas para generar estos elementos interactivos: `echarts4r`[^dashboards-2] [@R-echarts4r] para gráficos, `reactable`[^dashboards-3] para las tablas y `mapboxer`[^dashboards-4] [@R-mapboxer] para mapas.\n\n[^dashboards-2]: Librería que importa el código JavaScript del framework `Echarts` y lo transforma en código R fácilmente interpretable.\n\n[^dashboards-3]: Librería construida con el framework `React`, diseñada para crear tablas interactivas con código R fácilmente interpretable.\n\n[^dashboards-4]: Librería que llama a la API del servicio `mapbox`, para importar mapas interactivos de rápida carga, así como temas predefinidos/creados para personalizarlos.\n\n##### Gráficos\n\nRetomemos los gráficos del ejemplo anterior. Comencemos por el gráfico de series de tiempo que se encontraba en la parte superior de nuestra app, es decir:\n\n![Gráfico de área mediante `ggplot2`](imagenes/tseriesejemplo.png){fig-align=\"center\" width=\"400\"}\n\nSiendo el código de ese gráfico:\n\n```{r, eval=FALSE}\nhistorico_data() |> \n  ggplot(aes(x = Date, y = Ingreso)) +\n  geom_area_pattern(pattern = \"gradient\",\n                    pattern_fill = \"white\",\n                    pattern_fill2 = \"#74b47c\") +\n  theme_minimal() +\n  labs(title = \"Ingresos por fecha\") +\n  geom_text(aes(label=scales::comma(Ingreso)),\n            position=position_dodge(width=0.9), vjust=-0.25)\n```\n\nDonde `historico_data()` no es más que el objeto reactivo que usamos en la app para resumir (y filtrar) nuestros datos:\n\n```{r, eval=FALSE}\nwalmart_data |>\n  filter(Branch %in% input$branch & Gender %in% input$genero\n               & City %in% input$ciudad & Customer.type\n               %in% input$tipo_cliente \n               & Payment %in% input$tipo_pago ) |>\n  mutate(Ingreso = Unit.price*Quantity,\n         Date = floor_date(as_date(Date), \"week\")) |> \n  group_by(Date) |>\n  summarise(Ingreso = sum(Ingreso, na.rm = T))\n```\n\nVamos a convertir este gráfico a una versión más *interactiva* del mismo, utilizando `echarts4`. El uso de esta librería es bastante más sencillo que `ggplot2`, siendo que incialmente solo debemos establecer el eje **x** mediante el código `e_charts()` y, de forma automática, el eje **y**, basado en el tipo de gráfico que vayamos a utilizar, siendo en este caso un gráfico de área, por lo que la función a usar será `e_area()`. En este ejemplo, el código para generar el gráfico será:\n\n```{r, eval=FALSE}\nhistorico_data() |> \n  e_charts(Date) |> \n  e_area(Ingreso) |> \n  e_theme(\"infographic\") |> \n  e_color(color = \"#74b47c\") |> \n  e_tooltip(trigger = \"axis\", \n            backgroundColor = \"#fff\")\n```\n\nLo que resultará en:\n\n![Gráfico de área mediante `echarts4r`](imagenes/tseriesecharts.png){fig-align=\"center\" width=\"400\"}\n\nEn este caso, utilizamos la función `e_tooltip()` para generar un cuadro de texto flotante que aparece cuando posicionamos el puntero sobre algún punto, valga la redundancia, a lo largo del eje **x** en el área del gráfico. Tenemos diferentes opciones para que aparezca el `tooltip`, siendo en este caso `trigger = \"axis\"`, la que nos muestra el valor basado en el punto del eje **x** (independientemente del eje **y**), siendo que en otros casos utilizaremos `trigger = \"item\"`, en situaciones en las que no tengamos un eje **x** claro y necesariamente tengamos que posicionarnos en el punto graficado, opciones que veremos más adelante.\n\nObviamente, este objeto visual no es un gráfico generado como una imagen, por lo que sería incorrecto utilizar las funciones `renderPlot()` y `plotOutput()` para generarlo en una app. Para estos casos utilizaríamos las funciones `renderEcharts4r()` y `echarts4rOutput()` como sus respectivos equivalentes. Del lado de la interfaz, simplemente tendremos que asignarle un identificador, como en cualquier otro *output*, es decir:\n\n```{r, eval=FALSE}\necharts4rOutput(\"historico\")\n```\n\nY, de manera similar, generar la función de renderización en el lado del servidor:\n\n```{r, eval=FALSE}\noutput$historico <- renderEcharts4r({\n\n  historico_data() |>\n    e_charts(Date) |> \n    e_area(Ingreso) |> \n    e_theme(\"infographic\") |> \n    e_color(color = \"#74b47c\") |> \n    e_tooltip(trigger = \"axis\", \n            backgroundColor = \"#fff\") |> \n    e_title(\"Ingresos por fecha\")\n  \n  })\n```\n\nComo puedes ver, la lógica es exactamente la misma que con la generación de gráficos estáticos, únicamente cambiando las funciones de `ggplot2` por las de `echarts4r`, pero utilizando el mismo objeto reactivo con los datos en cuestión.\n\nVayamos ahora con el siguiente objeto gráfico, es decir:\n\n![Gráfico de segmentación mediante `ggplot2`](imagenes/trestopejemplo.png){fig-align=\"center\" width=\"350\"}\n\nSiendo en este caso el código respectivo:\n\n```{r, eval=FALSE}\ntop_data() |> \n        ggplot(aes(area = Ingreso, fill = City, \n                   label = scales::comma(round(Ingreso),\n                                         prefix = \"$\"))) +\n        geom_treemap() + \n        scale_fill_manual(\n          values = c(\"#7c8c84\", \"#74b47c\", \"#32a640\")\n        ) +\n        geom_treemap_text(\n          color = \"white\",\n          padding.y = unit(20, \"mm\"),\n          size = 15) +\n        theme_minimal() + \n        labs(title = \"Ingresos por region\")\n```\n\nDonde, al igual que en el caso anterior, `top_data()` es el objeto reactivo con el que resumimos (y filtramos) nuestros datos, es decir:\n\n```{r, eval=FALSE}\nwalmart_data |>\n  filter(Branch %in% input$branch & Gender %in% input$genero\n               & City %in% input$ciudad & Customer.type\n               %in% input$tipo_cliente \n               & Payment %in% input$tipo_pago ) |>\n  mutate(Ingreso = Unit.price*Quantity,\n         Date = floor_date(as_date(Date), \"week\")) |> \n  group_by(City) |> \n        summarise(Ingreso = sum(Ingreso)) |> \n        mutate(City = forcats::fct_reorder(City, Ingreso))\n```\n\nEn este caso, al tener solo tres grupos distintos, podemos optar por un gráfico de pastel o un gráfico de dona ahora que hagamos la transformación a `echarts4r`, los cuales son más intuitivos de leer en casos en los que no tenemos muchos elementos de segmentación de datos. La lógica para generar el gráfico es similar a la del caso anterior, únicamente cambiando el elemento del tipo de gráfico `e_area()` por `e_pie()`, es decir:\n\n```{r, eval=FALSE}\ntop_data() |> \n  e_charts(City) |> \n  e_pie(Ingreso, \n        radius = c(\"50%\", \"70%\")) |> \n  e_theme(\"infographic\") |> \n  e_tooltip(trigger = \"item\", \n            backgroundColor = \"#fff\") |> \n  e_title(\"Ingresos por ciudad\") |> \n  e_legend(orient = \"vertical\",\n           right = 0)\n```\n\nResultando en el gráfico @fig-pieecharts.\n\n![Gráfico de segmentación mediante `echarts4r`](imagenes/pieecharts.png){#fig-pieecharts fig-align=\"center\" width=\"400\"}\n\nComo puedes darte cuenta, en este caso no tenemos ejes **x** y **y** propiamente definidos, por lo que para añadir correctamente el *tooltip* es necesario utilizar el parámetro `item` en lugar de `axis` en la función `e_tooltip()`. Como única adición, se cambió la orientación y zona de la vista de la leyenda, para que concuerde con la versión gráfica que creamos con `ggplot2`.\n\nDe manera similar al caso anterior, simplemente debemos englobar el código dentro de las funciones `echarts4rOutput()`, para la salida del objeto, y `renderEcharts4r()`, para el elemento reactivo del lado del servidor.\n\nVamos a finalizar con el gráfico respectivo al top de categoriías de productos más vendidos, es decir:\n\n![Gráfico de barras mediante `ggplot2`](imagenes/topejemploggplot.png){fig-align=\"center\" width=\"350\"}\n\nEste caso es un poco más interesante de construir. La escala de colores en las barras se basa en los valores registrados de mayor a menor, siendo que para poder armar construir dicha escala hicimos uso de la función `scale_fill_viridis_b()` de `ggplot2`, la cual no tiene un equivalente en `echarts4r`. Directamente no tenemos forma de crear una escala de colores, pero lo que si podemos hacer es asignar un color a cada barra de forma manual, basado en una columna que contenga esa información en nuestro set de datos.\n\nComo recordarás, el código para generar el gráfico en cuestión mediante `ggplot2` es:\n\n```{r, eval=FALSE}\ncategorias_ventas_data() |> \n        ggplot(aes(y = forcats::fct_reorder(Product.line, \n                                            Ventas), \n                   x = Ventas,\n                   fill = Ventas)) + \n        geom_col(width = 0.5) + \n        scale_fill_viridis_b() + \n        theme_minimal() + \n        labs(y = \"Cateogoría de producto\")\n```\n\nDonde el objeto `categorias_ventas_data()`, al igual que en los casos anteriores, se refiere a:\n\n```{r, eval=FALSE}\nwalmart_data |>\n  filter(Branch %in% input$branch & Gender %in% input$genero\n               & City %in% input$ciudad & Customer.type\n               %in% input$tipo_cliente \n               & Payment %in% input$tipo_pago ) |>\n  mutate(Ingreso = Unit.price*Quantity,\n         Date = floor_date(as_date(Date), \"week\")) |> \n  group_by(Product.line) |> \n        summarise(Ventas = sum(Ingreso, na.rm = T)) |> \n        arrange(desc(Ventas))\n```\n\nA partir de los datos iniciales, podemos crear una columna adicional con la paleta de colores que utilizamos anteriormente, simplemente usando la función `col_numeric()` de la librería `scales`:\n\n```{r, eval=FALSE}\nwalmart_data |>\n  filter(Branch %in% input$branch & Gender %in% input$genero\n               & City %in% input$ciudad & Customer.type\n               %in% input$tipo_cliente \n               & Payment %in% input$tipo_pago ) |>\n  mutate(Ingreso = Unit.price*Quantity,\n         Date = floor_date(as_date(Date), \"week\")) |> \n  group_by(Product.line) |> \n  summarise(Ventas = sum(Ingreso, na.rm = T)) |> \n  mutate(color = scales::col_numeric(\"viridis\", Ventas)(Ventas)) |> \n  arrange(Ventas) \n```\n\nLógicamente, este paso debe ser posterior a que hayamos resumido la información, para poder realizar la asignación de color respecto a la información que estará representada en el gráfico de barras.\n\nPara asignar los colores a cada barra, simplemente debemos usar la función `e_add()`, usando como argumento para asignar el color a la columna `color`, siendo:\n\n```{r, eval=FALSE}\ncategorias_ventas_data() |> \n  e_charts(Product.line) |> \n  e_bar(Ventas) |> \n  e_add(\"itemStyle\", color) |> \n  e_flip_coords() |> \n  e_theme(\"infographic\") |> \n  e_tooltip(trigger = \"axis\", \n            backgroundColor = \"#fff\") |> \n  e_title(\"Top productos más vendidos\") |> \n  e_legend(show = F)\n```\n\nLo que resultará en el gráfico:\n\n![Gráfico de barras mediante `echarts4r`](imagenes/topecharts.png){fig-align=\"center\" width=\"350\"}\n\nEstos gráficos son geniales, si, pero más allá del *tooltip*, que nos ayuda a identificar números concretos al posicionarnos en cualquier sección del gráfico, también podemos obtener otros elementos a aprovechar para una mayor interactividad en nuestro dashboard, por ejemplo, exportar los valores que se nos devuelven de un gráfico al posicionarnos en algún punto o dar click en el mismo, para después actualizar los *inputs*, tal como lo haríamos en dashboards creados en PBI. Obtener esta información y exportarla es bastante sencillo, por ejemplo, el gráfico que creamos anteriormente está reemplazando al *output* estático `categorias_ventas`, siendo que en este caso su renderización sería:\n\n```{r, eval=FALSE}\n# UI\necharts4rBoxOutput(\"categorias_ventas\")\n\n# Server\noutput$categorias_ventas <- renderEcharts4r({\n  \n  categorias_ventas_data() |> \n    e_charts(Product.line) |> \n    e_bar(Ventas) |> \n    e_add(\"itemStyle\", color) |> \n    e_flip_coords() |> \n    e_theme(\"infographic\") |> \n    e_tooltip(trigger = \"axis\", \n            backgroundColor = \"#fff\") |> \n    e_title(\"Top productos más vendidos\") |> \n    e_legend(show = F)\n  \n})\n```\n\nEn este caso, para acceder a los elementos reactivos (interpretados como *inputs*) tales como la barra seleccionada, o el punto en el que el puntero se encuentra posicionado, utilizaremos el *id* de identificación del elemento `categorias_ventas` como un prefijo, seguido del elemento que vamos a extraer, el cual puede ser:\n\n-   `clicked_data`: Para datos seleccionados al hacer click en un elemento del gráfico. Se devolverá en un vector el valor de los ejes *x* y *y*.\n-   `clicked_row`: Para el número de fila del dato seleccionado, correspondiente al data frame usado reactivo usado para crear ese objeto.\n-   `clicked_serie`: Para el nombre de la serie seleccionada, si hay más de una.\n-   `mouseover_data`: Para datos seleccionados al posicionar el mouse sobre un elemento del gráfico. Se devolverá en un vector el valor de los ejes *x* y *y*.\n-   `mouseover_row`: Para el número de fila del dato en el que posicionamos el mouse, correspondiente al data frame usado reactivo usado para crear ese objeto.\n\nEstos son los principales elementos que podemos extraer como objetos reactivos, aunque también tenemos disponibles otros elementos (`brush`, `legend_change`, `legend_selected`, `clicked_data_value`, `mouseover_data_value` y `mouseover_serie`) que usaremos con menos frecuencia.\n\nPodemos utilizar, por ejemplo, `clicked_data` de la siguiente manera:\n\n```{r, eval=FALSE}\ndatos_seleccionados <- reactive({\n  input$categorias_ventas_clicked_data$value\n})\n```\n\nPor lo tanto, para acceder a, por ejemplo, el valor del eje *y*, podríamos aplicar indexación:\n\n```{r, eval=FALSE}\ndatos_seleccionados()[2]\n```\n\nSupongamos que en nuestro dashboard tenemos un filtro para las categorías de productos:\n\n```{r, eval=FALSE}\npickerInput(\n  inputId = \"categorias\",\n  label = \"Categoria de ventas\",\n  choices = unique(walmart_data$Product.line),\n  selected = unique(walmart_data$Product.line),\n  multiple = T\n)\n```\n\nEn este caso específico, podríamos actualizar las selecciones del filtro a partir de los valores que seleccionemos directamente del gráfico:\n\n```{r, eval=FALSE}\nobserve({\n  updatePickerInput(session = session, inputId = \"categorias\",\n                    selected = datos_seleccionados()[2])\n})\n```\n\nAfectando todos los elementos gráficos que se encuentren ligados a ese mismo filtro. De esta forma podemos generar una vista mucho más agradable al usuario final, ya que ahora puede interactuar con los elementos del gráfico y recibir una respuesta del servidor, de una forma similar a lo que hacen otros visualizadores (PBI, Tableau, etc), teniendo además la ventaja de que podemos acceder a esta información mediante reactivos, lo que nos permite tener un manejo más profundo de nuestro programa.\n\n##### Mapas\n\nYa que reemplazamos los gráficos estáticos con los gráficos interactivos (y sacamos provecho de ellos), podemos comenzar a explorar otro tipo de elementos, como lo son los mapas y las tablas.\n\nEmpecemos por la creación de un mapa mediante la librería `mapboxer`. Esta librería se conecta a la API de *mapbox*, por lo que para llamar a un mapa genérico bastará con la función `mapboxer()`:\n\n```{r, eval=FALSE}\nlibrary(mapboxer)\n\nmapboxer()\n```\n\nResultando en:\n\n![Mapa interactivo con `mapboxer`](imagenes/mapboxer.png){fig-align=\"center\" width=\"350\"}\n\nAhora, utilicemos los datos de `walmart_data` para generar una visualización dentro de un mapa. Podemos partir de las coordenadas de las ciudades en cuestión, las cuales no tenemos en el data frame, pero podemos obtener sencillamente con, por ejemplo, la API de Google Maps, usando la librería `ggmap`:\n\n```{r, eval=FALSE}\nlibrary(ggmap)\nregister_google(\"TU_API_KEY\")\n\n# Proceso para extraer direcciones\ndirecciones <- unique(walmart_data$City)\ndir_contenedor <- tibble(\n  Ciudad = c(),\n  lat = c(),\n  lng = c()\n)\n\nfor (row in 1:length(direcciones)){\n  \n  dir <- geocode(direcciones[row])\n  \n  dir_contenedor <- dir_contenedor |> \n    bind_rows(\n      tibble(\n        Ciudad = direcciones[row],\n        lat = dir$lat,\n        lng = dir$lon\n      )\n    )\n  \n}\n\n# Inserción en data frame\nwalmart_data <- walmart_data |> \n  left_join(dir_contenedor, \n            by = c(\"City\" = \"Ciudad\"))\n\n```\n\nYa que tenemos esa información, podemos proceder a resumir nuestros datos a nivel ciudad:\n\n```{r, eval=FALSE}\nresumen <-\n  walmart_data |> \n  mutate(Ingreso = Unit.price*Quantity) |> \n  group_by(City, lat, lng) |> \n  summarise(Ingreso = sum(Ingreso, na.rm = T)) |> \n  ungroup() |> \n  mutate(\n    percent_ing = Ingreso/sum(Ingreso)*100,\n    color = scales::col_numeric(\"viridis\", Ingreso)(Ingreso),\n    Ingreso = scales::comma(Ingreso, prefix = \"$\")\n  )\n```\n\nY, finalmente, generar una vista de burbuja simple en el mapa:\n\n```{r, eval=FALSE}\nresumen |> \n  as_mapbox_source() |> \n  mapboxer() |> \n  add_circle_layer(\n    circle_color = c(\"get\", \"color\"),\n    circle_opacity = 0.5,\n    circle_radius = c(\"get\", \"percent_ing\"),\n    popup = \"<p>\n             Ingreso: {{Ingreso}}\n             <p>\"\n  ) \n```\n\nResultando en:\n\n![Mapa de burbuja con `mapboxer`](imagenes/mapboxercircle.png){fig-align=\"center\" width=\"350\"}\n\nNótese que, para este ejemplo, además de resumir nuestros datos también creamos dos columnas adicionales: Una para generar la paleta de colores de los círculos y otra para normalizar el valor de nuestros datos y poder mantener un un tamaño razonable en el radio de los círculos.\n\nYa que tenemos un mejor entendimiento del uso básico de esta librería, ahora podemos comenzar a sacarle provecho dentro de una app web. Comencemos por integrar este widget en la interfaz de la app, siendo:\n\n```{r, eval=FALSE}\nmapboxerOutput(\"mapa\")\n```\n\nY:\n\n```{r, eval=FALSE}\n# Datos\nresumen <- reactive({\n  walmart_data |> \n  mutate(Ingreso = Unit.price*Quantity) |> \n  group_by(City, lat, lng) |> \n  summarise(Ingreso = sum(Ingreso, na.rm = T)) |> \n  ungroup() |> \n  mutate(\n    percent_ing = Ingreso/sum(Ingreso)*100,\n    color = scales::col_numeric(\"viridis\", Ingreso)(Ingreso),\n    Ingreso = scales::comma(Ingreso, prefix = \"$\")\n  )\n})\n\n# Mapa\noutput$mapa <- renderMapboxer({\n  resumen() |> \n    as_mapbox_source() |> \n    mapboxer() |> \n    add_circle_layer(\n    circle_color = c(\"get\", \"color\"),\n    circle_opacity = 0.5,\n    circle_radius = c(\"get\", \"percent_ing\"),\n    popup = \"<p>\n             Ingreso: {{Ingreso}}\n             <p>\"\n  ) \n})\n```\n\nPara la renderización del mapa desde el servidor, similar a cualquier otro elemento gráfico que hemos trabajado hasta ahora.\n\nAl igual que con los gráficos interactivos de `echarts4r`, podemos aprovechar la información obtenida del mapa cuando nosotros demos click en el. En este caso usamos el nombre *mapa* como ID del elemento, por lo que para acceder a la información de nuestros clicks, simplemente debemos añadir un sufijo `onclick` al mismo, usándolo dentro de un objeto reactivo, es decir:\n\n```{r, eval=FALSE}\n# Elementos devueltos tras el evento `click`\nobserveEvent(input$mapa_onclick,{\n      print(input$mapa_onclick)\n    })\n```\n\nCon este evento, rápidamente podemos identificar que, al dar click en alguna capa[^dashboards-5] de nuestro mapa, se nos devolverán tres clases de objetos:\n\n[^dashboards-5]: Representación de nuestros datos mediante un elemento visual, como burbujas, escalas de colores, etc.\n\n1.  Coordenadas (latitud y longitud), mediante `$coords$lng` y `$coords$lat`\n2.  Propiedades de la capa, propias del data frame utilizado, mediante `$props$_elemento_`\n3.  ID's del mapa y capa seleccionada, mediante `$layer_id` y `$widget_id`\n\nLógicamente, podemos aprovechar estos elementos para, por ejemplo, actualizar los filtros de nuestra app de una manera más interesante, tal cual lo hicimos con el gráfico anterior, por ejemplo, suponiendo que tuviéramos un filtro por ciudad:\n\n```{r, eval=FALSE}\npickerInput(\n  inputId = \"ciudad\",\n  label = \"Ciudad a escoger\",\n  choices = unique(walmart_data$City),\n  selected = unique(walmart_data$City),\n  multiple = T\n)\n```\n\nPodemos actualizarlo muy sencillamente con:\n\n```{r, eval=FALSE}\nobserveEvent(input$mapa_onclick, {\n  updatePickerInput(session = session, inputId = \"ciudad\",\n                    selected = input$mapa_onclick$props$City)\n})\n```\n\nExisten otro tipo de capas que podemos visualizar, como las líneas (ej. Rutas de punto *A* a punto *b*) y las delimitaciones de regiones (ciudades, estados, países), para las cuales tendremos que usar `add_line_layer()` y `add_fill_layer()`, respectivamente. Estas funciones tienen un uso bastante similar, aunque para `add_fill_layer()` no utilizaremos data frames que contengan únicamente una coordenada, si no archivos de tipo `.shp` o *shapefiles*, que contienen en cada observación una lista con todos los puntos necesarios para dibujar el relieve en el mapa. Podemos leer estos archivos con la librería `sf`, utilizando la función `read_sf()`, la cual nos devolverá esta información en un formato similar a un data frame. Veamos un rápido ejemplo con los datos [*dest_2015gw.shp*, disponibles en este enlace al repositorio](https://github.com/Jorge-hercas/r-para-inteligencia-de-negocios/tree/main/SHP/mexico). La lectura del *shapefile* es bastante simple:\n\n```{r}\nlibrary(sf)\nshapefile_mexico <- read_sf(\"shapefile/dest_2015gw.shp\") |> \n  dplyr::select(CAPITAL,geometry)\n\nshapefile_mexico\n```\n\nLa información que nos interesa de este data frame es la geometría, la cual podemos plasmar rápidamente en un mapa:\n\n```{r, eval=FALSE}\nshapefile_mexico |> \n  as_mapbox_source() |> \n  mapboxer() |> \n  add_fill_layer(\n    fill_color = \"darkgray\",\n    fill_outline_color = \"#000\",\n    fill_opacity = 0.5\n  )\n```\n\nResultando en el gráfico @fig-mapfill.\n\n![Mapa de geometría con `mapboxer`](imagenes/mapboxerfill.png){#fig-mapfill align=\"center\" width=\"350\"}\n\n##### Tablas\n\nComo último *widget* a analizar, vamos a trabajar con algunas tablas interactivas mediante la librería `reactable`. Su uso es bastante sencillo, ya que solo tenemos que llamar a la función `reactable()` y usar como argumento un data frame. Usemos como ejemplo los datos `walmart_data` que hemos venido trabajando hasta ahora, y vamos a crear una tabla resumen de las ventas por zona:\n\n```{r, eval=FALSE}\nlibrary(reactable)\n\nwalmart_data |> \n  group_by(City) |> \n  summarise(Ingreso_total = sum(Total, na.rm = T)) |> \n  arrange(desc(Ingreso_total)) |> \n  reactable(\n    columns = list(\n      Ingreso_total = colDef(\n        format = colFormat(\n          prefix = \"$\",\n          separators = T,\n          digits = 0\n        )\n      )\n    )\n  )\n```\n\nLo que resultará en:\n\n![Tabla interactiva con `reactable`](imagenes/reactableexample.png){#fig-reactable align=\"center\" width=\"350\"}\n\nNótese que, para una mejor presentación de los datos, arreglamos el formato de la columna numérica, lo que lo hará más sencillo de leer para el usuario final.\n\nEsta tabla se ve genial a simple vista, si, pero ¿Qué ventaja existe realmente entre usar una tabla y usar un gráfico que exprese los mismos datos? En realidad, no existe una regla exacta que defina en qué situaciones usar cada tipo de elemento visual, en estas situaciones debemos ser más analíticos respecto a: 1. El tipo de público al que va dirigido (es decir, si el usuario final prefiere ver números o gráficos), y 2. La homogeneidad de los datos, ya que en situaciones donde cada número es muy similar al otro, el uso de un gráfico puede no ser tan intuitivo como si lo será una tabla. Existen otros elementos que podemos tomar en cuenta al decidir qué tipo de *widget* vamos a utilizar, como la cantidad de datos a mostrar, pero los dos anteriores serán los que tomaremos principalmente en cuenta.\n\nAhora, la renderización de tablas en `shiny` es similar a la de los otros elementos que hemos analizado hasta ahora:\n\n```{r, eval=FALSE}\n# Interfaz\nreactableOutput(\"ingresos_zona\")\n\n# Servidor\ningresos_por_zona <- reactive({\n  \n  walmart_data |> \n  group_by(City) |> \n  summarise(Ingreso_total = sum(Total, na.rm = T)) |> \n  arrange(desc(Ingreso_total))\n  \n})\n\noutput$ingresos_zona <- renderReactable({\n  \n  ingresos_por_zona() |> \n    reactable(\n    columns = list(\n      Ingreso_total = colDef(\n        format = colFormat(\n          prefix = \"$\",\n          separators = T,\n          digits = 0\n        )\n      )\n    )\n  )\n  \n})\n```\n\nComo era de esperarse, también podemos extraer información de la tabla renderizada a partir de los clicks que hagamos en ella, mediante `getReactableState(\"ID_DE_LA_TABLA\")`, por ejemplo:\n\n```{r, eval=FALSE}\ngetReactableState(\"ingresos_zona\")\n```\n\nLo que nos devolverá en formato de lista la siguiente información:\n\n-   `page`: Página actual visualizada.\n-   `pageSize`: Número de filas que tiene la vista de la tabla.\n-   `pages`: Páginas totales de la tabla.\n-   `selected`: Número de fila que estamos seleccionando de la tabla.\n-   `sorted`: Valor `asc` o `desc`, si la tabla fue reordenada en alguna columna.\n\nEn la mayoría de los casos, la opción que más vamos a utilizar será `selected`, sin embargo, para que podamos seleccionar una fila, tendremos que usar las siguientes opciones en la función `reactable()`:\n\n```{r, eval=FALSE}\nreactable(\n  selection = \"single\",\n  onClick = \"select\"\n)\n```\n\nDonde también podemos establecer el argumento `selection = \"multiple\"`, si queremos seleccionar más de una fila a la vez. Lógicamente, si seleccionamos más de una fila, el resultado será un vector que incluya el número de filas seleccionadas.\n\nAsí como en los casos anteriores, podemos aprovechar los elementos extraídos de la sesión para actualizar el resto de elementos visuales que tengamos en nuestro tablero, así como los selectores y operaciones realizadas, por ejemplo, el selector `pickerInput()` que vimos en el caso anterior del mapa, puede ser actualizado de una forma similar:\n\n```{r, eval=FALSE}\n\nobserveEvent(input$ingresos_zona, {\n  # Extracción de las filas seleccionadas\n  estado_de_seleccion <- getReactableState(\"ingresos_zona\", \"selected\")\n  \n  # Actualización de los estados seleccionados mediante indexación\n  # a la tabla reactiva\n  updatePickerInput(session = session, inputId = \"ciudad\",\n                    selected = unique(ingresos_por_zona()[1:2,]$City))\n})\n```\n\nDe esta forma, podemos mantener una conexión bidireccional en el tablero generado, de tal forma que si quisiéramos filtrar a partir de un elemento visual a los demás, entonces podremos hacerlo de una forma muy agradable visualmente.\n\n##### Una pequeña reflexión\n\nEstos tres ejemplos que analizamos son solamente tres librerías de cientos existentes con el mismo propósito, el de generar visualizaciones interactivas a partir de código JavaScript. Estos ejemplos únicamente tienen como propósito el mostrarte un funcionamiento general de los *HTML Widgets*, pero ¿por qué usarlos en lugar de simplemente imágenes estáticas que sean actualizadas mediante *inputs*? Realmente no es un requisito indispensable el usar este tipo de elementos en el tablero que estemos desarrollando, sin embargo, es preferible hacerlo en ciertos casos ya que la experiencia hacia el usuario final será mucho más agradable y le facilitará su uso. Cuando creamos un dashboard mediante **shiny**, debemos estar concientes en todo momento de que la información que vamos a presentar siempre debe ser **concisa y fácil de entender para el usuario al que va a ir dirigida**. Para nosotros como los desarrolladores del tablero, será sencillo el interpretar los datos que estamos expresando en este mismo, ¿Pero lo será para un usuario no técnico? Esa es la pregunta que siempre debemos tener en cuenta, ya que de esta forma nos será más sencillo el buscar soluciones visuales o interactivas que le permitan al usuario final el poder aprovechar la información del tablero de una mejor manera. Personalmente, me he encontrado en múltiples situaciones en las que hice un dashboard increíble, con prácticamente todas las métricas que mi compañero de trabajo necesitaba en ese momento, pero al presentárselo, su primer comentario se resumía en *\"¿Puedo exportar esto a Excel?\"*, ya que el dashboard era demasiado técnico, o no tenía el nivel de manipulación que mis colaboradores buscaban. Generalmente, cuando una persona no técnica utiliza un programa que cuenta con múltiples gráficos y mapas, casi siempre su primer acción será el dar click en algún punto del mapa, una barra de un gráfico o la columna de una tabla, esperando que algo pase con el resto de elementos del tablero, por lo que utilizar *HTML Widgets* nos será de gran utilidad, al poder satisfacer este tipo de situaciones, mejorando así la experiencia de uso.\n\nEsta pequeña reflexión no es para que te desanimes respecto a la creación de tus tableros, si no para que entiendas y siempre tengas presente la razón por la que los hacemos, y como podemos crear un programa que sea de verdadera utilidad para la persona a la que va dirigido, sin que esta última piense en utilizar otra opción porque se le complicó utilizar el programa, lo que lo convertiría en un desarrollo sin ningún valor agregado.\n\n#### Seguridad en mis dashboards\n\nEste tema en particular es de vital importancia para los desarrollos que harás en el futuro. Probablemente tus primeros dashboards y/o programas serán elaborados con datos públicos o datos de muestra, ya que serán proyectos personales para tu portafolio, pero cuando trabajes en un dashboard para una empresa, por obvias razones deberás mantener la seguridad de este mismo como tu prioridad, de tal forma que puedas asegurarte que solamente las personas autorizadas tengan acceso a la información en este. Obviamente, no queremos que la información privada de una empresa u organización este expuesta al público o sea filtrada. En *shiny* tenemos algunas opciones para brindarle seguridad a nuestros tableros, siendo que en el presente libro nos vamos a enfocar en las *Capas de Contraseña*.\n\n##### Capas de contraseñas\n\nComencemos por la opción más sencilla, siendo que actualmente ya existen librerías dedicadas a este aspecto, como lo es `shinymanager` [@R-shinymanager]. Una capa de contraseñas no es más que un panel inicial que te solicita un usuario y una contraseña, tal como lo harías en cualquier página de uso cotidiano, como Facebook, Twitter, etc. Lo que hará esta capa de contraseñas es *poner en pausa* la renderización de todos los elementos de nuestro dashboard, y finalizar este proceso hasta que hayamos ingresado las claves correspondientes, dicho de otra manera, nuestro dashboard no va a iniciar ningún elemento hasta que se verifique que un usuario autorizado haya ingresado.\n\nSupongamos la siguiente aplicación:\n\n```{r, eval=FALSE}\nshinyApp(\n  ui = fluidPage(\n    h1(\"Título del programa\"),\n    reactableOutput(\"ingresos_zona\")\n  ),\n  server = function(input, output, session){\n    \n    ingresos_por_zona <- reactive({\n      \n      walmart_data |> \n        group_by(City) |> \n        summarise(Ingreso_total = sum(Total, na.rm = T)) |> \n        arrange(desc(Ingreso_total))\n      \n    })\n    \n    output$ingresos_zona <- renderReactable({\n      \n      ingresos_por_zona() |> \n        reactable(\n          columns = list(\n            Ingreso_total = colDef(\n              format = colFormat(\n                prefix = \"$\",\n                separators = T,\n                digits = 0\n              )\n            )\n          )\n        )\n    })\n  }\n)\n```\n\nEste programa te muestra inmediatamente los datos de ese cálculo, por lo que si se encuentra en la nube, cualquiera podría acceder a el y robar dicha información. Protegerlo utilizando `shinymanager` es bastante sencillo. Como primer paso, debemos contar con una tabla que incluya los accesos al programa, como el siguiente data frame:\n\n```{r, eval=FALSE}\nusuarios <- tibble(\n  user = \"Nombre_usuario\",\n  password = \"ejemplo_123\"\n)\n```\n\nDespués, únicamente deberemos englobar nuestra interfaz en la función `secure_app()`, para que esta se encuentre ahora detrás de un panel condicional que únicamente desaparecerá hasta que hayamos ingresado nuestras credenciales, es decir:\n\n```{r, eval=FALSE}\nui <- secure_app(\n  fluidPage(\n    h1(\"Título del programa\"),\n    reactableOutput(\"ingresos_zona\")\n  )\n)\n```\n\nMientras que, del lado del servidor, vamos a utilizar la función `secure_server()`, la cual será la encargada de revisar las credenciales y darnos acceso a los demás páneles, es decir:\n\n```{r, eval=FALSE}\nserver <- function(input, output, session){\n  \n    autenticacion <- secure_server(\n      check_credentials = check_credentials(usuarios)\n    )\n    \n    ingresos_por_zona <- reactive({\n      \n      walmart_data |> \n        group_by(City) |> \n        summarise(Ingreso_total = sum(Total, na.rm = T)) |> \n        arrange(desc(Ingreso_total))\n      \n    })\n    \n    output$ingresos_zona <- renderReactable({\n      \n      ingresos_por_zona() |> \n        reactable(\n          columns = list(\n            Ingreso_total = colDef(\n              format = colFormat(\n                prefix = \"$\",\n                separators = T,\n                digits = 0\n              )\n            )\n          )\n        )\n    })\n  }\n```\n\nPor lo tanto, cuando iniciemos la aplicación, en primera instancia tendremos la siguiente vista:\n\n![Capa de contraseñas](imagenes/password.png){align=\"center\" width=\"390\"}\n\nVista que podemos customizar de múltiples maneras, por ejemplo, utilizando temas de `shinythemes` mediante el argumento `secure_app(theme = \"...\")`, o estableciendo una imagen como fondo de pantalla mediante código CSS, por ejemplo:\n\n```{r, eval=FALSE}\nui <- secure_app(\n  fluidPage(\n    h1(\"Título del programa\"),\n    reactableOutput(\"ingresos_zona\")\n  ),\n  theme = \"cosmo\",\n  background  = \"linear-gradient(rgba(0, 0, 0, 0.5),\n                    rgba(0, 0, 0, 0.5)),\n                    url('https://cdn.pixabay.com/photo/2018/08/21/23/29/\n                    forest-3622519_1280.jpg')\n                    no-repeat center fixed;\n                    -webkit-background-size: cover;\n                    -moz-background-size: cover;\n                    -o-background-size: cover;\n                    background-size: cover;\"\n)\n```\n\nResultando en:\n\n![Capa de contraseñas](imagenes/fondopantalla.png){align=\"center\" width=\"390\"}\n\nE incluso agregar un logotigo:\n\n```{r, eval=FALSE}\nui <- secure_app(\n  fluidPage(\n    h1(\"Título del programa\"),\n    reactableOutput(\"ingresos_zona\")\n  ),\n  theme = \"cosmo\",\n  tags_top = tags$div(\n               tags$img(\n                 src = \"https://www.logoai.com/oss/icons/2021/\n                        10/27/rA73APprj8wskQ0.png\", \n                 width = 100\n               )\n             ),\n  background  = \"linear-gradient(rgba(0, 0, 0, 0.5),\n                    rgba(0, 0, 0, 0.5)),\n                    url('https://cdn.pixabay.com/photo/2018/08\n                    /21/23/29/forest-3622519_1280.jpg')\n                    no-repeat center fixed;\n                    -webkit-background-size: cover;\n                    -moz-background-size: cover;\n                    -o-background-size: cover;\n                    background-size: cover;\"\n)\n```\n\nLo que nos dará:\n\n![Capa de contraseñas](imagenes/logotipocontra.png){align=\"center\" width=\"390\"}\n\nVamos a sacarle un poco más de provecho al uso de esta capa de contraseñas, ya que con ello podemos crear un perfil de usuario. Tomemos como ejemplo una interfaz creada con `bs4Dash`:\n\n```{r, eval=FALSE}\nui <-\ndashboardPage(\n    header = dashboardHeader(rightUi = userOutput(\"user\")),\n    footer = dashboardFooter(),\n    sidebar = dashboardSidebar(\n      sidebarUserPanel(name = \"Dashboard de ventas\"),\n      sidebarMenu(\n        id = \"menu\",\n        menuItem(\n          text = \"Ingresos generales\",\n          tabName = \"ing_gen\",\n          icon = icon(\"money-bill\")\n        ),\n        menuItem(\n          text = \"Márgenes\",\n          tabName = \"marg\",\n          icon = icon(\"percent\")\n        )\n      )\n    ),\n    controlbar = dashboardControlbar(\n      skin = \"light\",\n      collapsed = TRUE,\n      width = 350,\n      id = \"controlbar\",\n      controlbarMenu(\n        id = \"menu_controlbar\"\n      )\n    ),\n    body = dashboardBody(\n      ...\n    )\n)\n```\n\nComo puedes notar, esta interfaz tiene un *output* dedicado a un elemento de usuario `userOutput(\"user\")`, el cual podemos aprovechar, agregando algunos parámetros al data frame que contiene la información de acceso al dashboard, por ejemplo:\n\n```{r, eval=FALSE}\nusuarios <- tibble(\n  user = \"Nombre_usuario\",\n  password = \"ejemplo_123\",\n  rol = \"Jefe de ventas\",\n  Area = \"Ventas\",\n  Nombre = \"Prison Mike\",\n  foto = \"https://upload.wikimedia.org/wikipedia/en/5/56/PrisonMike.png\"\n)\n```\n\nPor tanto, la interfaz será:\n\n```{r, eval=FALSE}\nui <- secure_app(\n  ui,\n  theme = \"cosmo\",\n  tags_top = tags$div(\n               tags$img(\n                 src = \"https://www.logoai.com/oss/icons/2021/\n                        10/27/rA73APprj8wskQ0.png\", \n                 width = 100\n               )\n             ),\n  background  = \"linear-gradient(rgba(0, 0, 0, 0.5),\n                    rgba(0, 0, 0, 0.5)),\n                    url('https://cdn.pixabay.com/photo/2018/08\n                    /21/23/29/forest-3622519_1280.jpg')\n                    no-repeat center fixed;\n                    -webkit-background-size: cover;\n                    -moz-background-size: cover;\n                    -o-background-size: cover;\n                    background-size: cover;\"\n)\n```\n\nY, del lado del servidor, tendremos que crear el perfil de usuario, tal que:\n\n```{r, eval=FALSE}\noutput$user <- renderUser({\n    \n    dashboardUser(\n      name = autenticacion$Nombre,\n      image = autenticacion$foto,\n      title = autenticacion$rol,\n      subtitle = autenticacion$Area\n    )\n    \n  })\n\n```\n\nLo que resultará en una interfaz dinámica que devolverá el perfil del usuario loggeado:\n\n![Capa de contraseñas](imagenes/mike.png){align=\"center\" width=\"390\"}\n\nDe esta forma podemos ir jugando con la seguridad de nuestros tableros, sacándole provecho visualmente. Claro que, el cargar el nombre de usuario en la interfaz realmente no es algo a lo que le podamos sacar provecho por si solo, pero si nos puede ser de utilidad a nivel *backend* para, por ejemplo, llevar registros de accesos al programa, o generar niveles de acceso a secciones de nuestro dashboard. Vamos a desarrollar estas dos ideas en la siguiente sección.\n\n##### Control de accesos mediante bases de datos\n\nUna forma de tener un mayor control respecto a los accesos que brindamos a nuestro dashboard es mediante un registro constante de los usuarios que acceden a este programa, pero ¿Cómo podemos obtener y guardar dicha información para poder explotarla después? Esto puede ser tan sencillo como enviar el registro de un acceso a una base de datos SQL, esto a partir de un evento en el servidor del dashboard. Comencemos por crear en la base de datos las tablas que contendrán la información de los usuarios y el registro de cada uno de ellos:\n\n```{r, eval=FALSE}\n# Tabla de usuarios\nCREATE TABLE dashboard_users(\n  ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,\n  user VARCHAR(30),\n  nombre VARCHAR(30),\n  password VARCHAR(30),\n  rol VARCHAR(15),\n  area VARCHAR(15),\n  foto VARCHAR(30),\n  status INT\n);\n\n# Tabla de registros (logs)\nCREATE TABLE dashboard_logs (\n  log_id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,\n  user_id INT NOT NULL,\n  log_timestamp TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,\n  FOREIGN KEY (user_id) REFERENCES dashboard_users(ID)\n)\n```\n\nDonde la tabla `dashboard_logs` tendrá una llave foránea ligada al *ID* del usuario. Vamos a añadir un usuario de ejemplo:\n\n```{r, eval=FALSE}\ninsert into dashboard_users (user, nombre, password, rol, area, foto, status) \nvalues (\"michael_01\", \"Michael Scott\", \"contra123\", \"Gerente\", \n\"Ventas\", \n\"https://cdn.pixabay.com/photo/2020/06/09/10/52/man-5277903_1280.jpg\", 1)\n```\n\nYa que tenemos esto, volvamos a nuestro dashboard, pero tomando ahora los usuarios directamente de nuestra base de datos:\n\n```{r, eval=FALSE}\n# Conexión a la BD\nconn <- DBI::dbConnect(RMySQL::MySQL(), \n                       dbname = \"ventas\", \n                       host = \"localhost\", \n                       port = 3306, \n                       user = \"master\", \n                       password = \"xxxxxxx\")\n\n# Carga de credenciales\nusuarios <- dbGetQuery(\n  conn, \"SELECT * FROM dashboard_users\n         WHERE status = 1\"\n)\n```\n\nAhora, ¿cómo guardamos la información de acceso de los usuarios a nuestro dashboard? Muy sencillo. Nosotros ya aplicamos dos reglas en la tabla de registros para que, tanto el `log_id` como la hora/fecha de acceso `log_timestamp`, se generen automáticamente, siendo que el `log_id` es un valor único incremental y la hora de registro es registrada como la hora actual en la que se crea la fila, por lo que bastará con mandar un query con el id del usuario que se registró en el servidor de nuestro programa, es decir:\n\n```{r, eval=FALSE}\nserver <- function(input, output, session){\n  \n  autenticacion <- secure_server(\n    check_credentials = check_credentials(usuarios)\n  )\n  \n  observe({\n    if (!is.null(autenticacion$user)){\n      dbSendQuery(conn, paste0(\"INSERT INTO dashboard_logs \n                                 (user_id) VALUES (\",autenticacion$ID,\")\"))\n    }\n  })\n  \n}\n```\n\nDe esta forma, cuando veamos la tabla de registros, podremos notar que se generará información como la siguiente:\n\n```{r, eval=FALSE}\ndbGetQuery(\n  conn, \"SELECT * \n         FROM dashboard_logs \n         WHERE CAST(log_timestamp AS DATE) = CURRENT_DATE\"\n)\n```\n\n| log_id | user_id | log_timestamp       |\n|--------|---------|---------------------|\n| 1      | 1       | 2025-01-01 20:11:19 |\n| 2      | 2       | 2025-01-01 20:11:57 |\n\n: Ejemplo de tabla de registros\n\n##### Niveles de acceso\n\nCuando comiences a crear tus primeros tableros te darás cuenta de que en un mismo equipo no todas las personas pueden (o deben) tener acceso a la información, por ejemplo, si desarrollaramos un dashboard de ventas, probablemente solo nos interese mostrar el ingreso generado por cuentas a los agentes de ventas que accedan a este tablero, mientras que información financiera más sensible solo podrá ser visualizada por el gerente de ventas o su superior, tal como ocurre en tableros creados en Power BI, cuando asignamos roles de usuario. Por supuesto, no existe una librería o función que nos permita hacer esto directamente, por lo que, con lo que ya hemos armado hasta ahora, podemos plantear una solución bastante sencilla a este problema. Tomemos como ejemplo la interfaz de `bs4Dash` que utilizamos en el caso anterior, con la que generamos un perfil dinámico:\n\n```{r, eval=FALSE}\nui <-\ndashboardPage(\n    header = dashboardHeader(rightUi = userOutput(\"user\")),\n    footer = dashboardFooter(),\n    sidebar = dashboardSidebar(\n      sidebarUserPanel(name = \"Dashboard de ventas\"),\n      sidebarMenu(\n        id = \"menu\",\n        menuItem(\n          text = \"Ingresos generales\",\n          tabName = \"ing_gen\",\n          icon = icon(\"money-bill\")\n        ),\n        menuItem(\n          text = \"Márgenes\",\n          tabName = \"marg\",\n          icon = icon(\"percent\")\n        )\n      )\n    ),\n    controlbar = dashboardControlbar(\n      skin = \"light\",\n      collapsed = TRUE,\n      width = 350,\n      id = \"controlbar\",\n      controlbarMenu(\n        id = \"menu_controlbar\"\n      )\n    ),\n    body = dashboardBody(\n      ...\n    )\n)\n```\n\nEsta interfaz tiene dos ventanas a las que podemos acceder: `Ingresos generales` y `Márgenes`. Supongamos que la información de `Márgenes` es más sensible y, por ende, necesita un nivel de autorización mayor, ¿Cómo podemos evitar que otros usuarios accedan a este panel? Lógicamente, el primer paso será asignar los roles de acceso a los usuarios, lo que podemos hacer creando una columna adicional en nuestra base de datos con esta información:\n\n```{r, eval=FALSE}\nALTER TABLE dashboard_users\nADD access_level INT;\n\nUPDATE dashboard_users \nSET access_level = 1\nWHERE ID = 1;\n```\n\nCon esto, estamos suponiendo que nuestro usuario de ejemplo tiene un nivel de acceso igual a *1*, pero para acceder al panel `Márgenes` se necesita un nivel de acceso igual a *2*, por lo que el siguiente paso será dinamizar esta vista, de tal forma que solo se muestre a usuarios con los permisos requeridos. La forma más sencilla es plantear una función que englobe nuestra interfaz:\n\n```{r}\nvista <- function(access_level,access_required,...){\n  \n  if (access_level < access_required){\n    div(\n      width = 12, align = \"ceter\",\n      p(\"Sin permiso\")\n    )\n  }else{\n    tagList(...)\n  }\n  \n}\n```\n\nDonde `access_level` será una variable que obtendremos de la tabla de usuarios, mientras que `access_required` será una variable que nosotros asignaremos, como el nivel mínimo requerido para acceder a dicho panel. Obviamente, si el usuario tiene un nivel de acceso menor al requerido, simplemente la función devolverá el mensaje *\"Sin permiso\"*. El argumento `...` será un objeto que contenga la interfaz que vamos a dinamizar. Tonemos como ejemplo una interfaz en la que generemos un mapa que solo pueda ser visualizado por usuarios con un nivel de acceso igual o superior a 2, siendo el panel:\n\n```{r, eval=FALSE}\npanel_condicional <-\n  tagList(\n    box(\n      width = 12,\n      align = \"center\",\n      mapboxerOutput(\"mapa\")\n    )\n  )\n```\n\nEl cual será mostrado en la siguiente interfaz:\n\n```{r, eval=FALSE}\ntabItems(\n        tabItem(\n          tabName = \"ing_gen\",\n          uiOutput(\"panel_inicial\")\n        )\n      )\n```\n\nPodemos guardar el objeto `panel_condicional` como una variable global, a la cual accederemos después mediante:\n\n```{r, eval=FALSE}\noutput$panel_inicial <- renderUI({\n      \n      vista(access_level = autenticacion$access_level, \n            access_required = 2, \n            panel_condicional)\n      \n    })\n```\n\nObviamente, la tabla que contiene la información de acceso del perfil necesarioamente debe tener una columna llamada `access_level`, siendo esta la que estamos llamando en la función `vista()` para identificar el nivel de autorización del usuario que acceda, por ejemplo:\n\n```{r, echo=FALSE}\nusuarios <- tibble(\n  user = \"Nombre_usuario\",\n  password = \"ejemplo_123\",\n  rol = \"Jefe de ventas\",\n  Area = \"Ventas\",\n  Nombre = \"Prison Mike\",\n  foto = \"https://upload.wikimedia.org/wikipedia/en/5/56/PrisonMike.png\",\n  access_level = 2\n)\n\nusuarios\n```\n\nPor tanto, si el usuario *Nombre_usuario* ingresara al *dashboard*, sería capaz de visualizar los objetos dentro de este panel, debido a su nivel de autorización suficiente.\n\nAhora, manejar los accesos a los páneles bajo niveles de autorización puede no ser siempre la mejor opción, ya que si, es un método de seguridad confiable, sin embargo, al ser un método ambiguo respecto a los usuarios que tendrán acceso a cada panel, podemos dar pie a situaciones en las que un panel requiera un nivel de acceso elevado, pero un usuario con los niveles de acceso mínimos requeridos, pero perteneciente a **otra área** sea capaz de visualizar esta información. Para este tipo de situaciones podemos modificar la lógica de la función, agregando también otro tipo de condiciones más específicas, como el área a la que pertenece el usuario o el nombre del panel/páneles en específico al/los que tendrá acceso cada usuario, por ejemplo:\n\n```{r}\nvista <- function(access_level,access_required,actual_department,\n                  required_department,...){\n  \n  if (access_level < access_required \n      & required_department == actual_department){\n    div(\n      width = 12, align = \"ceter\",\n      p(\"Sin permiso\")\n    )\n  }else{\n    tagList(...)\n  }\n  \n}\n```\n\nDonde `required_department` será una constante en el servidor de nuestra aplicación que nos indicará a qué área pertenece el panel, mientras que `actual_department` será el valor obtenido a partir del usuario que esté ingresando, agarantizando que solo los destinatarios de la información puedan verla.\n\n### Shiny para (semi) automatizar tareas\n\nAlgo interesante de las *Shiny Apps* es el hecho de que no son específicamente dashboards, son **aplicaciones web**, lo que significa que la cantidad de cosas que podemos crear con ellas es ilimitada, estando únicamente sujetos a nuestra imaginación. Precisamente, en esta sección discutiremos como podemos implementar la lógica detrás de la semi automatización de procesos, así como ajustarla a cada caso particular.\n\nUsualmente, solemos escribir códigos que siguen una cierta secuencia, por ejemplo, para generar un gráfico sencillo:\n\n```{r, eval=FALSE}\n# Paso 1: leer los datos\ndata <- read.csv(\"ejemplo.csv\")\n\n# Paso 2: Transformar los datos\ndatos <- datos |> \n  group_by(x) |> \n  summarise(\n    total = sum(y)\n  )\n\n# Paso 3: Generar el gráfico\ngrafico <- \ndatos |> \n  ggplot(aes(x = x, y = total)) + \n  geom_col()\n\n# Paso 4: Guardar el gráfico\nggsave(\"ruta_del_proyecto/grafico.pdf\", plot = grafico)\n```\n\nEste código sigue una secuencia lógica que va a aplicar de la misma manera, cada vez que lo ejecutemos, sin embargo, no siempre tendremos abierta una sesión de R para correr el código, por lo que podemos contenerlo dentro de una Shiny App, ¿Cómo podemos hacer esto? Básicamente, podemos hacerlo de dos formas: Escribiendo directamente el código en el servidor de la app, o guardando el mismo en un script por separado y simplemente llamándolo desde el servidor. Ahora, obviamente ese código no se va a ejecutar solo por tenerlo en el servidor, debemos llamarlo de alguna manera, utilizando un **evento**, como es el caso de los botones. Veamos un ejemplo sencillo:\n\n```{r, eval=FALSE}\nshinyApp(\n  ui = fluidPage(\n    actionButton(\"generar_grafico\", \"Generar gráfico\")\n  ),\n  server = function(input, output, session){\n    \n    observeEvent(input$generar_grafico, {\n      # Paso 1: leer los datos\n      data <- read.csv(\"ejemplo.csv\")\n      \n      # Paso 2: Transformar los datos\n      datos <- datos |> \n        group_by(x) |> \n        summarise(\n        total = sum(y))\n      \n      #Paso 3: Generar el gráfico\n      grafico <- \n        datos |> \n        ggplot(aes(x = x, y = total)) + \n        geom_col()\n\n      # Paso 4: Guardar el gráfico\n      ggsave(\"ruta_del_proyecto/grafico.pdf\", plot = grafico)\n      \n    })\n    \n  }\n)\n```\n\nEn este caso, estamos escribiendo directamente el código en el servidor, pero también podríamos guardarlo en un archivo separado llamado, por ejemplo, `proceso.R`, y llamarlo mediante el mismo evento:\n\n```{r, eval=FALSE}\nshinyApp(\n  ui = fluidPage(\n    actionButton(\"generar_grafico\", \"Generar gráfico\")\n  ),\n  server = function(input, output, session){\n    \n    observeEvent(input$generar_grafico, {\n      \n      source(\"proceso.R\")\n      \n    })\n    \n  }\n)\n```\n\nAhora, esta muestra de código simplemente ejecuta un proceso estático, por lo que probablemente puedas pensar que esto no es útil en un uso práctico, ya que como programadores casi siempre tendremos nuestros códigos a la mano para ejecutarlos, lo que de hecho es cierto, por lo que ahora vamos a abordar un ejemplo mucho más completo que nos permita entender el potencial completo de los procesos que podemos ejecutar. Supongamos que los usuarios finales a quienes van dirigidos los análisis (stakeholders) están consumiendo constantemente un reporte, aunque de manera irregular, por lo que hacer uso de un *JOB* en la nube podría no ser la mejor solución en este caso, ya que no tenemos claro cada cuando tendremos que actualizar los datos de dicho reporte; Podemos aprovechar esta situación para generar un programa que le permita a los mismos usuarios finales el poder actualizar los datos que necesitan ellos mismos; Comencemos por establecer el proceso para extraer los datos que necesitamos, por ejemplo, si los datos vienen de una base de datos en SQL:\n\n```{r, eval=FALSE}\nlibrary(DBI)\nlibrary(googlesheets4)\n\n# Conexión\nconexion <- DBI::dbConnect(RMySQL::MySQL(), dbname = \"db_name\", \n        host = \"host_name.com\", \n        port = 3306, user = \"usuario\", password = \"contraseña\")\n\n# Query para obtener datos al día actual\ndatos <- dbGetQuery(\"select ...\n                    from tabla_ejemplo\n                    where fecha_tabla <= curdate()\")\n```\n\nSupongamos ahora que estaremos enviando estos datos a un reporte en un documento colaborativo en Google Sheets, por lo que el siguiente paso lógico será:\n\n```{r, eval=FALSE}\n# Carga de datos en un reporte en la nube\nwrite_sheet(data = datos, ss = \"xxxxxxxxx\", sheet = \"Reporte\")\n```\n\nEste proceso se puede volver aún más flexible, al dinamizar parámetros de la consulta de nuestro reporte. Vamos a convertir el código anterior en una función para poder añadir filtros más específicos en la consulta:\n\n```{r, eval=FALSE}\nlibrary(DBI)\nlibrary(googlesheets4)\n\n# Conexión\n  conexion <- DBI::dbConnect(RMySQL::MySQL(), dbname = \"db_name\", \n          host = \"host_name.com\", \n          port = 3306, user = \"usuario\", password = \"contraseña\")\n\n# Función\ncargar_datos <- function(fecha_inicial, fecha_final, conexion){\n\n  # Query para obtener datos al día actual\n  datos <- dbGetQuery(paste0(\"select ...\n                        from tabla_ejemplo\n                        where fecha_tabla between '\",fecha_inicial,\"' \n                                          and '\",fecha_final,\"' \"))\n  \n  # Carga de datos en un reporte en la nube\n  write_sheet(data = datos, ss = \"xxxxxxxxx\", sheet = \"Reporte\")\n  \n}\n```\n\nAhora, ya que tenemos el proceso dentro de una función, podemos añadir estos parámetros en una shiny app como *Inputs*, por ejemplo:\n\n```{r, eval=FALSE}\nshinyApp(\n  ui = fluidPage(\n    dateInput(\"fecha_inicial\", \"Fecha inicial\"),\n    dateInput(\"fecha_final\", \"Fecha final\"),\n    actionButton(\"generar_reporte\", \"Generar Reporte\")\n  ),\n  server = function(input, output, session){\n    observeEvent(input$generar_reporte,{\n      \n      cargar_datos(fecha_inicial = input$fecha_inicial, \n                   fecha_final = input$fecha_final,\n                   conexion = conexion)\n      \n    })\n  }\n)\n```\n\nDe esta forma, el usuario final ahora tiene la capacidad de actualizar el reporte que necesita siempre que lo quiera, en los rangos de fechas que necesite, simplemente utilizando una shiny app.\n\nLlevemos aún más lejos este ejercicio. Supongamos ahora que tenemos múltiples reportes, los cuales funcionan de una manera similar, por lo que las funciones que armamos para cargarlos en google sheets mantienen la misma estructura, por ejemplo:\n\n```{r, eval=FALSE}\n# Función\nsegundo_reporte <- function(fecha_inicial, fecha_final, conexion){\n\n  # Query para obtener datos al día actual\n  datos <- dbGetQuery(paste0(\"select ...\n                        from tabla_ejemplo_2\n                        where fecha_tabla between '\",fecha_inicial,\"' \n                                          and '\",fecha_final,\"' \"))\n  \n  # Carga de datos en un reporte en la nube\n  write_sheet(data = datos, ss = \"xxxxxxxxx\", sheet = \"Reporte 2\")\n  \n}\n```\n\nEn tal caso, podemos agregar un `selectInput()` para cada reporte. Primero, vamos a englobar ambas funciones en una lista, para poder acceder a ellas después:\n\n```{r, eval=FALSE}\nreportes <- list(\n  \"Reporte 1\" = cargar_datos,\n  \"Reporte 2\" = segundo_reporte\n)\n```\n\nAhora, vamos a agregar las opciones en el *UI*:\n\n```{r, eval=FALSE}\nfluidPage(\n    dateInput(\"fecha_inicial\", \"Fecha inicial\"),\n    dateInput(\"fecha_final\", \"Fecha final\"),\n    selectInput(\"tipo_reporte\", \"Selecciona el reporte\", choices = names(reportes)),\n    actionButton(\"generar_reporte\", \"Generar Reporte\")\n  )\n```\n\nFinalmente, vamos a agregar el proceso al servidor:\n\n```{r, eval=FALSE}\nobserveEvent(input$generar_reporte, {\n  # Obtener la función seleccionada\n  funcion_reporte <- reportes[[input$tipo_reporte]]\n      \n  # Ejecutar la función\n  funcion_reporte(\n    fecha_inicial = input$fecha_inicial,\n    fecha_final = input$fecha_final,\n    conexion = conexion\n  )\n})\n```\n\nFinalmente, la aplicación completa sería:\n\n```{r, eval=FALSE}\nlibrary(DBI)\nlibrary(googlesheets4)\n\ncargar_datos <- function(fecha_inicial, fecha_final, conexion){\n  # Query para obtener datos al día actual\n  datos <- dbGetQuery(paste0(\"select ...\n                        from tabla_ejemplo\n                        where fecha_tabla between '\",fecha_inicial,\"' \n                                          and '\",fecha_final,\"' \"))\n  \n  # Carga de datos en un reporte en la nube\n  write_sheet(data = datos, ss = \"xxxxxxxxx\", sheet = \"Reporte\")\n}\n\nsegundo_reporte <- function(fecha_inicial, fecha_final, conexion){\n  # Query para obtener datos al día actual\n  datos <- dbGetQuery(paste0(\"select ...\n                        from tabla_ejemplo_2\n                        where fecha_tabla between '\",fecha_inicial,\"' \n                                          and '\",fecha_final,\"' \"))\n  \n  # Carga de datos en un reporte en la nube\n  write_sheet(data = datos, ss = \"xxxxxxxxx\", sheet = \"Reporte 2\")\n}\n\nreportes <- list(\n  \"Reporte 1\" = cargar_datos,\n  \"Reporte 2\" = segundo_reporte\n)\n\nshinyApp(\n  ui = fluidPage(\n    dateInput(\"fecha_inicial\", \"Fecha inicial\"),\n    dateInput(\"fecha_final\", \"Fecha final\"),\n    selectInput(\"tipo_reporte\", \"Selecciona el reporte\", \n                choices = names(reportes)),\n    actionButton(\"generar_reporte\", \"Generar Reporte\")\n  ),\n  server = function(input, output, session){\n    observeEvent(input$generar_reporte,{\n      \n      funcion_reporte <- reportes[[input$tipo_reporte]]\n      \n      funcion_reporte(\n          fecha_inicial = input$fecha_inicial,\n          fecha_final = input$fecha_final,\n          conexion = conexion\n        )\n      \n    })\n  }\n)\n```\n\nComo puedes ver, mediante una Shiny App podemos ejecutar cualquier clase de procesos sin tener que tener abierta una sesión en un IDE, lo que lo hace perfecto para simplificar tareas, y que estas mismas puedan ser ejecutadas por los mismos usuarios finales siempre que las necesiten. En este caso utilizamos como ejemplo un sencillo proceso ETL, pero en el mundo laboral podemos crear este tipo de aplicaciones para cualquier situación que requeramos, como extraer información de correos, generar archivos PDF, hacer ciertos cálculos, y un largo etcetera.\n\nAntes de pasar a la siguiente sección, vamos a añadirle una cosa más a nuestra app. Cuando los procesos que ejecutemos toman demasiado tiempo, puede volverse un poco ambiguo el hecho de que realmente se esten ejecutando, por lo que podemos apoyarnos de ciertos visuales para poder confirmar esto, como las típicas barras de carga de que vemos en los programas, por ejemplo, en el caso anterior podemos englobar la sección del código a la que llamamos mediante `input$generar_reporte`, dentro de una función `withProgress()`, que mostrará una barra de carga, la cual podemos ajustar respecto a los saltos que tome para llegar al *100%*:\n\n```{r, eval=FALSE}\nwithProgress(message = \"Iniciando proceso\",{\n  observeEvent(input$generar_reporte,{\n    \n      funcion_reporte <- reportes[[input$tipo_reporte]]\n      \n      incProgress(0.5, message = \"50% terminado\")\n      \n      funcion_reporte(\n          fecha_inicial = input$fecha_inicial,\n          fecha_final = input$fecha_final,\n          conexion = conexion\n        )\n      \n      incProgress(0.5, message = \"Proceso completado\")\n      \n    })\n})\n```\n\nResultando la figura 9.35.\n\n![Barra de carga en un proceso](imagenes/incprogress.png){fig-align=\"center\" width=\"420\"}\n\nExisten muchas otras dependencias basadas en JavaScript que podemos usar para apoyarnos visualmente al momento de ejecutar procesos en shiny apps, como pueden ser `shinyalert`, `shinytoastr`, entre otras más, dependiendo de cuál sea nuestro interés en particular (e inclusive el tema que estemos usando en nuestra app).\n\n### Buenas prácticas para estructurar una aplicación\n\nAhora que ya conocemos como crear aplicaciones con Shiny y todo lo que podemos hacer con ellas, vamos a terminar este capítulo aprendiendo las mejores prácticas para poder estructurar nuestras aplicaciones, haciéndolas escalables y sencillas de leer.\n\nAlgo común cuando comenzamos a crear nuestras primeras apps es que solemos mantener el código completo en un mismo archivo (casi siempre llamado `app.R`). Esto está bien cuando se trata de programas pequeños o prototipos simples. Sin embargo, a medida que nuestras aplicaciones crecen en complejidad (incorporando múltiples módulos, funciones, y elementos de *UI*) mantener todo el código en un solo archivo se vuelve inviable. Encontrar y modificar un componente entre cientos o miles de líneas de código se convierte en una tarea titánica y propensa a errores.\n\nComo primera buena práctica, se recomienda dividir nuestro código en tres archivos principales:\n\n-   `global.R`: Aquí se colocan todas las variables, funciones, conexiones a bases de datos, paquetes, y cualquier otro recurso que será compartido tanto por el servidor como por la interfaz de usuario. Este archivo se ejecuta una sola vez cuando se lanza la app, por lo que es ideal para tareas pesadas o costosas.\n\n-   `ui.R`: Contiene exclusivamente el código relacionado con la interfaz gráfica del usuario. Aquí se definen los paneles, *inputs*, *outputs*, menús, estilos, temas, etc. Esta separación facilita que alguien con conocimiento en diseño o UX pueda trabajar directamente sobre este archivo sin alterar la lógica del servidor.\n\n-   `server.R`: Aquí se define la lógica de la aplicación: los cálculos, transformaciones, llamados a la base de datos, generación de gráficos y respuestas a eventos. Este archivo contiene el *server* como función reactiva.\n\nLa estructura de la carpeta de nuestro proyecto se verá:\n\n```{r, eval=FALSE}\n# /folder_de_mi_app/\n# │\n# ├── global.R\n# ├── ui.R\n# ├── server.R\n```\n\nQueda implícito el hecho de que es áltamente recomendable crear un proyecto que esté meramente destinado a nuestra aplicación, ya que tener guardados otros archivos en la misma ruta puede dar pie a confusiones al revisar/depurar el código, además de que será más dificil de mantener a largo plazo. Ahora, además de nuestros archivos principales, como ya mencioné en secciones anteriores, podemos incluir una carpeta `www`, en la cual guardaremos todos los objetos/archivos referentes a la customización de nuestra app, como pueden ser códigos CSS, imágenes, entre otros elementos. En el caso de incluir esta carpeta, la estructura de nuestra app será:\n\n```{r, eval=FALSE}\n# /folder_de_mi_app/\n# │\n# ├── global.R           \n# ├── ui.R               \n# ├── server.R           \n# │\n# ├── www/               \n# │   ├── styles.css     \n# │   ├── logo.png \n```\n\nDe esta forma, nuestro código es mucho más limpio y se vuelve sencillo el encontrar las secciones de la app que estamos buscando. Para referenciar estos archivos, de manera automática nuestro proyecto ya va a detectar que se encuentran dentro de la carpeta `www`, por lo que solo tendremos que nombrarlos sin ningún prefijo, por ejemplo:\n\n```{r, eval=FALSE}\ntags$head(\n  tags$link(rel = \"stylesheet\", \n            type = \"text/css\", \n            href = \"styles.css\")\n)\n```\n\nPara llamar a un archivo CSS.\n\nSi quisiéramos, podemos dividir las secciones de nuestra app aún más, para que incluso secciones pequeñas o más específicas se encuentren encapsuladas dentro de otros archivos separados, por ejemplo, imaginemos que tenemos un proceso de extracción de datos relativamente complejo y largo, por lo que escribirlo en el archivo `global.R` se vuelve poco práctico; En lugar de escribir ese código en la sección global, podemos escribirlo en un archivo separado llamado `data.R` y simplemente llamarlo desde el archivo `global.R`, por lo que el código de este último archivo se verá de la siguiente manera:\n\n```{r, eval=FALSE}\nlibrary(shiny)\n...\n...\n\nsource(\"data.R\")\n```\n\nVolviéndose bastante sencillo de leer para otros usuarios, ya que ahora podemos identificar en qué archivo se encuentran cosas más específicas, como funciones, pipelines, e inclusive secciones del `UI`.\n\nLa contenerización de archivos en una shiny app no solo se limita a los aspectos que vimos anteriormente, si no también a secciones específicas de la app en si, como ventanas o páginas de la misma, cosa que podemos hacer separándola en **módulos**, tema que veremos a continuación.\n\n#### Módulos\n\nEn una shiny app, los módulos permiten organizar el código en componentes reutilizables y escalables, lo que es ideal cuando la app empieza a crecer en complejidad y componentes. Básicamente, un módulo encapsula una parte de la interfaz y su lógica del servidor, para que puedas reutilizarla varias veces o mantener el código ordenado. El uso de módulos en shiny es especialmente útil cuando trabajamos colaboratibamente en un proyecto a gran escala, ya que cada usuario estará enfocado en desarrollar el código de su propio módulo, sin preocuparse por afectar la funcionalidad de los demás.\n\nLa estructura de los módulos es bastante sencilla, ya que simplemente vamos a englobar los componentes en dos funciones referentes al `UI` y el `Server`:\n\n-   `mod_funcion_UI(id)`: Para definir la parte de la interfaz.\n-   `mod_funcion_Server(id)`: Para definir la lógica del servidor.\n\nPara ver cómo estructurar una aplicación mediante módulos, podemos retomar el ejemplo que vimos en la sección anterior. Para esta aplicación vamos a crear los siguientes archivos:\n\n-   `Global.R`\n-   `UI.R`\n-   `Server.R`\n-   `Funciones.R`\n-   `Modulo_panel_funciones.R`\n\nPrimero, comenzaremos por crear el archivo `Funciones.R`, para las funciones globales:\n\n```{r, eval=FALSE}\ncargar_datos <- function(fecha_inicial, fecha_final, conexion){\n  datos <- dbGetQuery(conexion, paste0(\"\n    SELECT ... \n    FROM tabla_ejemplo \n    WHERE fecha_tabla BETWEEN '\", fecha_inicial, \"' AND '\", \n    fecha_final, \"'\"\n  ))\n  \n  write_sheet(data = datos, ss = \"xxxxxxxxx\", sheet = \"Reporte\")\n}\n\nsegundo_reporte <- function(fecha_inicial, fecha_final, conexion){\n  datos <- dbGetQuery(conexion, paste0(\"\n    SELECT ... \n    FROM tabla_ejemplo_2 \n    WHERE fecha_tabla BETWEEN '\", fecha_inicial, \"' AND '\", \n    fecha_final, \"'\"\n  ))\n  \n  write_sheet(data = datos, ss = \"xxxxxxxxx\", sheet = \"Reporte 2\")\n}\n\nreportes <- list(\n  \"Reporte 1\" = cargar_datos,\n  \"Reporte 2\" = segundo_reporte\n)\n```\n\nAhora, vamos a crear el archivo `Modulo_panel_funciones.R`, para comenzar a entender la estructura de estos componentes:\n\n```{r, eval=FALSE}\n# UI\nmod_reporte_ui <- function(id){\n  ns <- NS(id)\n  tagList(\n    dateInput(ns(\"fecha_inicial\"), \"Fecha inicial\"),\n    dateInput(ns(\"fecha_final\"), \"Fecha final\"),\n    selectInput(ns(\"tipo_reporte\"), \"Selecciona el reporte\", \n                choices = NULL),\n    actionButton(ns(\"generar_reporte\"), \"Generar Reporte\")\n  )\n}\n\n# Servidor\nmod_reporte_server <- function(id, reportes, conexion){\n  moduleServer(id, function(input, output, session){\n    \n    updateSelectInput(session, \"tipo_reporte\", choices = names(reportes))\n      \n      withProgress(message = \"Iniciando proceso\",{\n        observeEvent(input$generar_reporte,{\n          req(input$tipo_reporte, input$fecha_inicial, input$fecha_final)\n          \n          funcion_reporte <- reportes[[input$tipo_reporte]]\n          \n          incProgress(0.5, message = \"50% terminado\")\n          \n          funcion_reporte(\n            fecha_inicial = input$fecha_inicial,\n            fecha_final = input$fecha_final,\n            conexion = conexion\n          )\n          \n          incProgress(0.5, message = \"Proceso completado\")\n          \n        })\n      })\n    \n  })\n}\n\n\n```\n\nVamos ahora a analizar el código de los módulos. Del lado del `UI` es bastante sencillo, ya que simplemente creamos una función que llame a esa sección de la interfaz. Obviamente, como los elementos de HTML no se encuentran dentro de una función `fluidPage()`, tenemos que hacer uso de `tagList()`, para que puedan ser englobados y luego exportados a una sección de nuestra shiny app. Como puedes darte cuenta, además agregamos una línea de código adicional:\n\n```{r, eval=FALSE}\nns <- NS(id)\n```\n\n¿Qué significa? Este pequeño código hace referencia al *NameSpace* o *espacio de nombre*, y sirve para encapsular todo lo que se encuentre dentro de este módulo, evitando problemas de ambigüedad, como en situaciones en las que dos *inputs* de módulos diferentes tienen el mismo ID, lo que al mantenerlos separados mediante un *NameSpace* diferente, aunque ambos elementos tengan un mismo ID, este no se confundirá con el otro, permitiendo que nuestras apps sean mucho más escalables. Tanto del lado del `UI` como del `Server` tenemos como argumento la variable `id`, esta variable nos ayudará a asignarle el *NameSpace* a nuestro módulo para que sea identificable a partir de ella. Finalmente, puedes darte cuenta de que todos los *inputs* del módulo referente al `UI` se encuentran dentro de una función `ns()`, y esto es ocurre para que podamos mantenerlas única y exclusivamente dentro del *NameSpace*, siendo que todo lo que hagamos afuera de este espacio no lo va a afectar.\n\nVolviendo a la estructura de la app, ahora vamos a crear el archivo `Global.R`:\n\n```{r, eval=FALSE}\nlibrary(shiny)\nlibrary(DBI)\n...\n...\n\nconnexion <- dbGetQuery(...)\n\nsource(\"Funciones.R\")\nsource(\"Modulo_panel_funciones.R\")\n```\n\nEl archivo `Global.R` únicamente cuenta con unas pocas líneas de código, ya que las funciones y módulos adicionales se encuentran en archivos separados que solo son llamados aquí, lo que lo convierte en un archivo sumamente fácil de leer.\n\nVayamos ahora con el archivo `UI.R`:\n\n```{r, eval=FALSE}\nfluidPage(\n  mod_reporte_ui(\"reporte_ui\")\n)\n```\n\nEn donde llamamos a la función referente a los componentes de la interfaz.\n\nFinalmente, vamos a crear el archivo `Server.R`:\n\n```{r, eval=FALSE}\nfunction(input, output, session){\n  mod_reporte_server(\"reporte_ui\", reportes, conexion)\n}\n```\n\nDe esta forma, podemos construir completamente una app modularizada que sea más escalable. Si quisiéramos ordenar aún más el código, podríamos guardar todos los archivos referentes a los módulos en una subcarpeta llamada `modulos`, siendo ahora el archivo `Global.R`:\n\n```{r, eval=FALSE}\nlibrary(shiny)\nlibrary(DBI)\n...\n...\n\nconnexion <- dbGetQuery(...)\n\nsource(\"modulos/Funciones.R\")\nsource(\"modulos/Modulo_panel_funciones.R\")\n```\n\n#### Otras funcionalidades de los módulos\n\nYa que trabajamos con la estructura general de los módulos, ahora vamos a exprimir sus capacidades un poco más. A pesar de que estos son independientes entre si, es posible que nos encontremos en situaciones en las que necesitemos exportar un cierto valor o `input` de un módulo a otro. El aislamiento entre estos objetos impide que utilicemos directamente el `input$id_objeto` de otro módulo, ya que esto rompería el funcionamiento de los módulos, por lo que la forma correcta de acceder a estos objetos es a través del uso de objetos reactivos. En lugar de acceder directamente al `input`, podemos hacer que un módulo devuelva un objeto `reactive()` o una `reactiveValues()` que luego se pasa como argumento a otro módulo, siendo este ya un objeto independiente. Veamos un ejemplo sencillo de esto, considerando la aplicación anterior con la que estuvimos trabajando: Imaginemos que ahora queremos crear un reporte adicional en otra sección de nuestra app, a partir de los parámetros del primer módulo, por lo que podemos hacer una modificación a este último para que exporte los valores reactivos en una lista:\n\n```{r, eval=FALSE}\nmod_reporte_server <- function(id, reportes, conexion){\n  moduleServer(id, function(input, output, session){\n    \n    updateSelectInput(session, \"tipo_reporte\", choices = names(reportes))\n    \n    valores <- reactiveValues()\n\n    observeEvent(input$generar_reporte, {\n      req(input$tipo_reporte, input$fecha_inicial, input$fecha_final)\n\n      withProgress(message = \"Iniciando proceso\", {\n        \n        funcion_reporte <- reportes[[input$tipo_reporte]]\n        \n        incProgress(0.5, message = \"50% terminado\")\n\n        funcion_reporte(\n          fecha_inicial = input$fecha_inicial,\n          fecha_final = input$fecha_final,\n          conexion = conexion\n        )\n\n        incProgress(0.5, message = \"Proceso completado\")\n        \n        valores$fecha_inicial <- input$fecha_inicial\n        valores$fecha_final <- input$fecha_final\n        valores$tipo_reporte <- input$tipo_reporte\n        valores$listo <- TRUE\n      })\n    })\n\n    return(valores)\n  })\n}\n```\n\nDe esta forma, con la ayuda del objeto `valores <- reactiveValues()`, podemos almacenar toda la información que necesitemos de forma reactiva. Ahora, vamos a crear el segundo módulo, el cual tomará estos parámetros y creará el reporte:\n\n```{r, eval=FALSE}\n# UI\nmod_segundo_reporte <- function(id){\n  ns <- NS(id)\n  tagList(\n    actionButton(ns(\"generar_segundo_reporte\"), \"Generar Reporte\"),\n    verbatimTextOutput(ns(\"resultado_segundo_reporte\"))\n  )\n}\n\n# Server\nmod_ejecutar_reporte_server <- function(id, parametros){\n  moduleServer(id, function(input, output, session){\n    \n    output$resultado <- renderPrint({\n      req(parametros$listo)\n      paste(\"Parámetros recibidos:\",\n            \"\\nTipo:\", parametros$tipo_reporte,\n            \"\\nDesde:\", parametros$fecha_inicial,\n            \"\\nHasta:\", parametros$fecha_final)\n    })\n    \n  })\n}\n```\n\nPor lo tanto, ahora el `UI` de nuestra app será:\n\n```{r, eval=FALSE}\nfluidPage(\n  mod_reporte_ui(\"reporte_ui\"),\n  mod_segundo_reporte(\"segundo_reporte\")\n)\n```\n\nY el servidor:\n\n```{r, eval=FALSE}\nfunction(input, output, session){\n  mod_reporte_server(\"reporte_ui\", reportes, conexion),\n  mod_ejecutar_reporte_server(\"segundo_reporte\", parametros)\n}\n```\n\nPodemos guardar el código de este segundo módulo en un archivo llamado `Modulo_panel_reporte.R`, por lo que ahora nuestro archivo `Global.R` se verá de la siguiente manera:\n\n```{r, eval=FALSE}\nlibrary(shiny)\nlibrary(DBI)\n...\n...\n\nconnexion <- dbGetQuery(...)\n\nsource(\"modulos/Funciones.R\")\nsource(\"modulos/Modulo_panel_funciones.R\")\nsource(\"modulos/Modulo_panel_reporte.R\")\n```\n\nA pesar de que el objeto `valores` y el parámetro `parametros` no tienen el mismo nombre, estos se pueden comunicar gracias a que utilizamos un objeto `reactiveValues()`, el cual funciona como una referencia que comparten ambos en memoria, lo que les permite identificar que dichos parámetros son provenientes de ese objeto reactivo.\n\nY así, tan sencillamente (aunque no tan intuitivo a nivel código) podemos trabajar con módulos en shiny de casi todas las maneras imaginables. Esta estructura modular no solo nos permite organizar mejor nuestras aplicaciones, sino también construir componentes reutilizables, escalables y más fáciles de mantener. A medida que nuestras apps crecen en complejidad, dominar los módulos se vuelve una herramienta esencial para escribir código más limpio, robusto y flexible.\n\n### Culminando con dos proyectos\n\nFinalmente, después de toda esta travesía que pasamos para poder entender cómo funcionan nuestros datos, como extraerlos, manipularlos, arreglarlos y hacer estadística y análisis con ellos, vamos a darle un cierre a este libro aplicando **todo** (o casi todo) lo que hemos aprendido en dos proyectos finales: *Una aplicación para visualizar datos del clima* y *un dashboard de ventas interactivo*, cada uno de ellos con un propósito diferente que iremos viendo poco a poco.\n\nEstos proyectos no son solo ejercicios prácticos: son oportunidades reales para consolidar conocimientos, tomar decisiones de diseño, y pensar en la experiencia del usuario final. Pondremos en práctica estructuras reactivas, uso de módulos, visualizaciones dinámicas, limpieza de datos y algunas técnicas avanzadas que ya has visto, ahora en un contexto más completo y realista.\n\nAdemás, estas aplicaciones están pensadas no solo como ejercicios académicos, sino como soluciones que podrían funcionar en escenarios del mundo real. Son el cierre perfecto para esta etapa de tu formación, antes de seguir profundizando en nuevas herramientas y desafíos.\n\n#### Visualizador de datos climáticos\n\nNuestro primer proyecto final consistirá en una aplicación web diseñada para mostrar los datos climáticos actuales en cualquier parte del mundo, además de ofrecer pronósticos para los días siguientes. Este proyecto es especialmente interesante porque no solo trabajaremos con datos en tiempo real, sino que también vamos a extraerlos directamente mediante la API de OpenWeather, integrando así una fuente externa dinámica en nuestra aplicación.\n\nComo recordarás, en el proyecto final del capítulo 5 nos enfocamos en crear un *pipeline* para enviar datos desde la API de `Open Weather` hacia una base de datos relacional, y en ese mismo capítulo hicimos uso de la librería `OweatherR`, desarrollada específicamente para facilitar ese flujo de trabajo. En este nuevo proyecto, volveremos a utilizar esa librería para simplificar el código de nuestra aplicación. Sin embargo, también explicaremos cada una de las funciones de la librería `OweatherR` de forma individual, con el objetivo de comprender mejor su estructura interna y tener un mayor control sobre lo que *se ejecuta detrás*.\n\n##### Archivo Global\n\nComencemos por el primer paso lógico, como en cualquier shiny app: Cargar todas las librerías necesarias en un archivo `Global.R`:\n\n```{r, eval=FALSE}\nlibrary(shiny)\nlibrary(globe4r)\nlibrary(dplyr)\nlibrary(stringr)\nlibrary(shinyWidgets)\nlibrary(globe4r)\nlibrary(GoogleTooltip)\nlibrary(mapboxer)\nlibrary(ggmap)\nlibrary(reactable)\nlibrary(OweatherR)\nlibrary(echarts4r)\n```\n\nSiendo `globe4r`, `reactable`, `mapboxer`, `echarts4r` y `shinyWidgets` librerías para los objetos visuales que vamos a trabajar, `dplyr` y `stringr` para el manejo de datos y cadenas específicas, `ggmap` para obtener coordenadas a partir de direcciones, y `GoogleTooltip` (librería también escrita por tu servidor) para generar una barra de búsqueda que nos de sugerencias de direcciones, tal como lo hace el portal de Google Maps.\n\nSiguiendo con el archivo `Global.R`, para poder extraer coordenadas a partir de direcciones, tendremos que usar una *API KEY* de Google Maps, la cual puedes obtener en la consola de Google, tal como vimos en secciones anteriores de este capítulo cuando discutimos el uso de `mapboxer`. De igual forma, necesitaremos una *API KEY* del sitio de Open Weather para poder extraer la información climática que necesitamos. Una vez tenemos estos dos claves, el código en el archivo `Global.R` será:\n\n```{r, eval=FALSE}\n# API de Google Maps\ngoogle_key <- \"XXXXXXXXXXXXXXXXXXXXXXXX\"\nregister_google(google_key)\n# API de Open Weather\nset_weather_key(\"XXXXXXXXXXXXXXXXXXXXXXXX\")\n```\n\nDonde la función `set_weather_key()` únicamente guarda la cadena de texto de la *API KEY* como una variable de entorno disponible mientras la sesión esté activa, siendo la estructura de la función:\n\n```{r, eval=FALSE}\nset_weather_key <- function(api_key){\n  Sys.setenv(weather_key = api_key)\n}\n```\n\n##### Archivo UI\n\nSiguiendo con la interfaz de nuestra app (en el archivo `ui.R`), antes de comenzar con el código, una buena práctica siempre será el plasmar en un diagrama o un visual sencillo el como queremos que luzca nuestro programa, para después escribir el código en base a ello, siendo en este caso el siguiente:\n\n![Diagrama general de la app](imagenes/diagrama%20app%20clima.pdf){fig-align=\"center\" width=\"540\"}\n\nBásicamente, vamos a dividir la interfaz en dos secciones: una más pequeña, que ocupará aproximadamente un cuarto de la pantalla y estará destinada a los *inputs* y widgets secundarios; y otra más amplia, que ocupará los tres cuartos restantes y contendrá un mapa interactivo. Este mapa nos permitirá visualizar con precisión el punto exacto del que estamos obteniendo la información climática.\n\nComencemos por añadir una fuente personalizada desde Google Fonts y un fondo oscuro con un gradiente radial para toda la pantalla. Además, incluiremos un *spinner* que se activará automáticamente cada vez que haya procesos ejecutándose en el servidor:\n\n```{r, eval=FALSE}\nfluidPage(\n  tags$head(tags$link(href = \"https://fonts.googleapis.com/css?family=Oswald\", \n                      rel = \"stylesheet\")),\n  shinybusy::add_busy_spinner(position = \"top-right\", \n                              spin = \"self-building-square\", \n                              color = \"darkred\"),\n  setBackgroundColor(\n    color = \"#2B2B2B\",\n    gradient = \"radial\"\n  )\n)\n```\n\nComenzando ahora con los elementos internos del panel pequeño, crearemos una columna con un tamaño `widht = 3`, que centre automáticamente los elementos:\n\n```{r, eval=FALSE}\ncolumn(\n    width = 3,\n    align = \"center\",\n    ...\n  )\n```\n\nAhora, comencemos a agregar los widgets correspondientes uno a uno. El primer elemento que vamos a añadir será un visual de JavaScript para generar un pequeño globo terraqueo:\n\n```{r, eval=FALSE}\nglobeOutput(\"globo\",\n            width = 250, \n            height = 250)\n```\n\nEl título de nuestro programa, el cual será *El clima en tu ciudad* (aunque esto puede ser ajustado a tu preferencia personal):\n\n```{r, eval=FALSE}\nh1(\"El clima en tu ciudad\",style=\"color:#ebb52d\")\n```\n\nComo siguiente paso, vamos a añadir los *inputs* para poder buscar la dirección en particular, siendo `autocomplete_textOutput()` para la cadena de texto a buscar, *input* creado para poder dar sugerencias a partir de la API de Google Maps, razón por la que la *API Key* respectiva en el archivo global fue guardada como un objeto, ya que la vamos a reutilizar en este *input*. Adicionalmente, dos `numericInput()` para establecer la latitud y longitud, y un `actionButton()` para poder activar el evento, evitando que este se ejecute de manera automática cuando añadamos información a los *inputs* anteriores. Puede parecer un poco redundante el tener dos *inputs* enfocados a latitud y longitud si esta misma la vamos a obtener directamente de la dirección que establezcamos, sin embargo, esto le da la opción al usuario final de poder usar nombres de ubicaciones o, si el lo prefiere, usar ubicaciones exactas. Obviamente, el *input* enfocado a la cadena de texto deberá ser mucho más amplio que los demás, en caso de que introduzcamos direcciones largas, por lo que vamos a crear tres columnas: Fuera de las columnas vamos a introducir `autocomplete_textOutput()`, en las siguientew dos columnas pequeñas que dividirán la sección a la mitad enfocaremos a los *inputs* numéricos de latitud/longitud, y la última columna adicional será para `actionButton()`:\n\n```{r, eval=FALSE}\nautocomplete_textOutput(label = \" \", \n                        inputId = \"direction\", key = google_key,\n                        use_shinywidgets = T, icon = icon(\"globe\"), \n                        placeholder = \"Ingresa tu ubicación\"),\ncolumn(width = 6, tags$style(\"label{color: #ebb52d;}\"),\n       numericInput(\"lat\", \"Latitud\", value = 0)),\ncolumn(width = 6, numericInput(\"lng\", \"Longitud\", value = 0)),\ncolumn(width = 12, actionButton(\"temp_find\", \"Ver clima\", \n                                icon = icon(\"magnifying-glass\")))\n```\n\nYa que tenemos escritos los *inputs* correspondientes, ahora vamos a añadir en esta sección dos *Outputs*: `textOutput(\"temperatura_actual\")` para generar una cadena de texto con la temperatura actual de la zona a busacar, y `reactableOutput(\"tabla_temperaturas\")` para generar una tabla con el pronóstico de las temperaturas de los siguientes días:\n\n```{r, eval=FALSE}\ncolumn(width = 12,\n       align = \"center\",\n       h4(textOutput(\"temperatura_actual\"),\n          style=\"color:#ebb52d\"),\n       reactableOutput(\"tabla_temperaturas\"))\n```\n\nFuera del panel más pequeño, ahora vamos a ajustar el siguiente panel correspondiente al mapa:\n\n```{r, eval=FALSE}\ncolumn(\n    width = 9,\n    mapboxerOutput(\"map\", height = \"1000px\", width = \"1300px\")\n  )\n```\n\nSiendo únicamente una columna con `width = 9` y un único widget, por lo que su implementación es bastante sencilla.\n\nComo último elemento visual, vamos a añadir un gráfico de tendencias para las temperaturas, sin embargo, notarás que todos los elementos ya están ocupando el 100% de los páneles de nuestra app. Lo que haremos para añadir este último visual será sobreponerlo en una esquina del mapa, lo que resultará en un visual más interesante, que no estorbará gravemente a los demás. Para sobreponer este elemento haremos uso del panel `absolutePanel()` y lo posicionaremos en la esquina superior derecha mediante los argumentos `top = 10` y `right = 10`, siendo:\n\n```{r, eval=FALSE}\nabsolutePanel(\n    top = 10, right = 10, style = \"z-index:500; text-align: right;\",\n    echarts4rOutput(\"grafico_temperatura\", height = 250, width = 400)\n  )\n```\n\nFinalmente, el código completo de nuestra interfaz será:\n\n```{r, eval=FALSE}\nfluidPage(\n  tags$head(tags$link(href = \"https://fonts.googleapis.com/css?family=Oswald\", \n                      rel = \"stylesheet\")),\n  shinybusy::add_busy_spinner(position = \"top-right\", \n                              spin = \"self-building-square\", \n                              color = \"darkred\"),\n  setBackgroundColor(\n    color = \"#2B2B2B\",\n    gradient = \"radial\"\n  ),\n  column(\n    width = 3,\n    align = \"center\",\n    globeOutput(\"globo\",\n                width = 250, \n                height = 250),\n    h1(\"El clima en tu ciudad\",style=\"color:#ebb52d\"),\n    hr(),\n    autocomplete_textOutput(label = \" \", inputId = \"direction\", key = key,\n                            use_shinywidgets = T, icon = icon(\"globe\"), \n                            placeholder = \"Ingresa tu ubicación\"),\n    column(width = 6, tags$style(\"label{color: #ebb52d;}\"),\n           numericInput(\"lat\", \"Latitud\", value = 0)),\n    column(width = 6, numericInput(\"lng\", \"Longitud\", value = 0)),\n    column(width = 12, actionButton(\"temp_find\", \"Ver clima\", \n                                    icon = icon(\"magnifying-glass\"))),\n    div(style = \"height: 100px;\"),\n    column(width = 12,\n           align = \"center\",\n           h4(textOutput(\"temperatura_actual\"),style=\"color:#ebb52d\"),\n           reactableOutput(\"tabla_temperaturas\"))\n  ),\n  column(\n    width = 9,\n    mapboxerOutput(\"map\", height = \"1000px\", width = \"1300px\")\n  ),\n  absolutePanel(\n    top = 10, right = 10, style = \"z-index:500; text-align: right;\",\n    echarts4rOutput(\"grafico_temperatura\", height = 250, width = 400)\n  )\n)\n```\n\nNótese que añadimos algunos espaciados de página (*breaks*) mediante las funciones `hr()` y `div(style = \"height: 100px;\")`, simplemente para dar un mejor aspecto a nuestro programa.\n\n##### Archivo Server\n\nEntrando de lleno en la lógica de nuestro programa, comencemos por plantear las funciones respectivas del lado del servidor. Como primer paso, vamos a generar los visuales para el globo terraqueo y el mapa plano, ya que en el estado inicial de la aplicación les daremos una vista genérica:\n\n```{r, eval=FALSE}\noutput$globo <- render_globe({\n    create_globe(height = \"100vh\") |> \n      globe_background(color = \"#2B2B2B\") |> \n      globe_img_url(image_url(\"blue-marble\")) |> \n      globe_pov(\n        altitude = 2.3,\n        0.6345,\n        -80.5528\n      )\n  })\n\noutput$map <- renderMapboxer({\n    mapboxer(\n      style = \"mapbox://styles/mapbox/dark-v11\",\n      token = \"XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX\",\n      center = c(-99,22),\n      zoom = 4.5,\n      maxZoom = 9\n    ) \n  })\n```\n\nVamos ahora a obtener las coordenadas respectivas a partir de la dirección:\n\n```{r, eval=FALSE}\ncoordenadas <- reactive({\n    dir <- geocode(location = input$direction)\n    dir\n  })\n```\n\nNótese que, al ser un objeto `reactive()`, este únicamente se activará cuando `input$direction` sea ejecutado, por lo que no es necesario escribirlo dentro de un `observeEvent()`.\n\nA partir del objeto anterior podemos actualizar los valores de los *inputs numéricos*:\n\n```{r, eval=FALSE}\nobserveEvent(input$direction,{\n    \n    req(coordenadas())\n    \n    updateNumericInput(session, \"lat\", value = coordenadas()$lat)\n    updateNumericInput(session, \"lng\", value = coordenadas()$lon)\n    \n  })\n```\n\nEn este caso si será necesario mantener la actualización de ambos *inputs* dentro de un `observeEvent()` que llame directamente a `input$direction`, ya que necesariamente utilizaremos el valor obtenido a partir de este último *input*, lo que imposibilita que esté vacío para evaluar los otros dos.\n\nYa que tenemos las coordenadas necesarias, podemos crear objetos *proxys* que actualicen el estado inicial del mapa y el globo, respectivamente, sin reiniciarlos, siendo:\n\n```{r}\nbounds <- reactive({\n    x <- c(input$lng,input$lat, input$lng+0.1, input$lat+0.1)\n    x\n  })\n\nobserveEvent(input$temp_find, {\n    globe_proxy(\"globo\") |> \n      globe_pov(\n        altitude = 2.3,\n        input$lat,\n        input$lng\n      ) \n  })\n\nobserveEvent(input$temp_find,{\n    req(bounds())\n  \n    mapboxer_proxy(\"map\") |>\n      fit_bounds(bounds()) |>\n      update_mapboxer()\n  })\n```\n\nSiendo ambos *proxys* activados por el botón `input$temp_find`. En el caso del globo terraqueo únicamente establecimos unas coordenadas específicas y una altitud, pero en el caso del mapa plano tuvimos que crear una serie de coordenadas para poder generar un cuadrado correspondiente al tamaño de la vista del mapa, el cual ajustará automáticamente el zoom basado en el tamaño de este cuadrado.\n\nAprovechando que ya tenemos establecidas las coordenadas, podemos comenzar por extraer los datos necesarios de la API de OpenWeather:\n\n```{r, eval=FALSE}\ntemperaturas_data <- reactive({\n    \n    solicitud <- weather_forecast_req(lat = input$lat, \n                                      lon = input$lng, \n                                      tibble_format = T)\n    \n    x <- \n      tibble(\n        solicitud$dt, solicitud$main$temp,\n        solicitud$main$temp_min,\n        solicitud$main$temp_max,\n        solicitud |> \n          select(weather) |> \n          tidyr::unnest() |> \n          select(icon)) |> \n      setNames(c(\"Datetime\", \"Temp\", \"Min\", \"Max\", \"Icon\")) |> \n      mutate(\n        Icon = paste0(\n          \"http://openweathermap.org/img/w/\",Icon,\".png\"\n        ),\n        Datetime = strftime(Datetime, format = \"%A, %I %p\"),\n        Min = Min -273.15,\n        Max = Max -273.15,\n        Temp = Temp -273.15,\n      )\n    \n    x\n    \n  })\n```\n\nComo puedes ver, le hicimos un pequeño tratamiento a los datos para poder convertirlos a un data frame compatible con `reactable()` (*input* para una tabla HTML). Además de la solicitud inicial, también modificamos cinco columnas: `Icon` para generar los íconos en HTML, `Datetime` para reformatear la columna de fechas y un pequeño ajuste a `Min`, `Max` y `Temp`, para convertir las temperaturas de *Kelvin* a *Celsius*.\n\nVamos ahora a crear los *outputs* finales: El texto condicional para conocer la temperatura actual:\n\n```{r, eval=FALSE}\noutput$temperatura_actual <- renderText({\n    x <- weather_actual_req(lat = input$lat, \n                            lon = input$lng, \n                            tibble_format = T)\n    paste0(\"Temperatura actual: \", x$main.temp-273.15,\"°\")\n  }) |> \n    bindEvent(input$temp_find)\n```\n\nEl gráfico sobrepuesto de tendencias en la temperatura para los sigueintes días:\n\n```{r, eval=FALSE}\noutput$grafico_temperatura <- renderEcharts4r({\n    \n    req(temperaturas_data())\n    x <- temperaturas_data()\n    \n    temperaturas_data() |> \n      e_charts(Datetime) |> \n      e_bar(Temp, symbol = \"none\", smooth = F, \n             name = \"Temperatura\") |> \n      e_theme(\"auritus\") |> \n      e_color(color = \"darkred\") |> \n      e_tooltip(trigger = \"axis\") |> \n      e_y_axis(show = F) |> \n      e_title(\" \", left = \"center\") |> \n      e_legend(right = 0, textStyle = list(color = \"#fff\"))\n    \n  }) |> \n    bindEvent(input$temp_find)\n```\n\nY, finalmente, la tabla con el pronóstico del clima, incluyendo elementos visuales en HTML como los íconos del clima:\n\n```{r, eval=FALSE}\noutput$tabla_temperaturas <- renderReactable({\n    \n    req(temperaturas_data())\n    \n    temperaturas_data() |> \n      reactable(\n        theme = reactablefmtr::nytimes(centered = TRUE, \n                                       header_font_size = 12,\n                                       font_color = \"#fff\",\n                                       background_color = \"transparent\"),\n        defaultColDef = colDef(headerStyle = list(background = \"#2B2B2B\", \n                                                  color = \"#fff\"),\n                               vAlign = \"center\"),\n        compact = TRUE,\n        language = reactableLang(\n          noData = \"No data found\",\n          pageInfo = \"{rowStart}\\u2013{rowEnd} of {rows} pages\",\n          pagePrevious = \"\\u276e\",\n          pageNext = \"\\u276f\",\n        ),\n        bordered = F,\n        onClick = \"select\",\n        #selection = \"single\",\n        pagination = F,\n        height = 390,\n        columns = list(\n          Datetime = colDef(name = \"Dia/hora\",minWidth = 60),\n          Icon = colDef(\n            minWidth = 25,\n            header = icon(\"cloud-sun\"),\n            cell = function(value) {\n              image <- img(src = value, style = \"height: 24px;\", alt = value)\n              tagList(\n                div(style = \"display: inline-block; width: 45px;\", image)\n              )\n            }\n          ),\n          Temp = colDef(\n            minWidth = 60,\n            header = icon(\"temperature-low\"),\n            cell = function(value, index){\n              Min <- temperaturas_data()$Min[index]\n              Max <- temperaturas_data()$Max[index]\n              div(\n                div(style = \"font-weight: 600\", value, \"°\"),\n                div(style = \"font-size: 0.75rem\", \n                    paste0(\"Min: \",Min,\"°, Max: \", Max,\"°\"))\n              )\n            }\n          ),\n          Min = colDef(show = F),\n          Max = colDef(show = F)\n        )\n      )\n    \n    \n  }) |> \n    bindEvent(input$temp_find)\n```\n\nEl código respectivo a la tabla es especialmente grande debido al alto nivel de personalización que aplicamos por ejemplo, siendo que hicimos tratamientos individuales para casi cada columna. Más específicamente, aplicamos un tema preestablecido con `reactablefmtr::nytimes(...)` y `colDef(headerStyle = list(background = \"#2B2B2B\", color = \"#fff\"), vAlign = \"center\")`. Para generar una columna que nos muestre el ícono del clima hicimos uso de una función anómima:\n\n```{r, eval=FALSE}\nIcon = colDef(\n  minWidth = 25,\n  header = icon(\"cloud-sun\"),\n  cell = function(value) {\n    image <- img(src = value, style = \"height: 24px;\", alt = value)\n    tagList(\n      div(style = \"display: inline-block; width: 45px;\", image)\n    )\n  }\n)\n```\n\nMientras que, para el caso de la temperatura, condensamos los valores mínimos, máximos y actuales de la siguiente manera:\n\n```{r, eval=FALSE}\nTemp = colDef(\n  minWidth = 60,\n  header = icon(\"temperature-low\"),\n  cell = function(value, index){\n    Min <- temperaturas_data()$Min[index]\n    Max <- temperaturas_data()$Max[index]\n    div(\n      div(style = \"font-weight: 600\", value, \"°\"),\n      div(style = \"font-size: 0.75rem\", \n          paste0(\"Min: \",Min,\"°, Max: \", Max,\"°\"))\n    )\n  }\n)\n\n```\n\nSeguramente te diste cuenta de que en estos tres *inputs* hicimos uso de `bindEvent()` en lugar de `observeEvent()`, ¿por qué optamos por esta función en particular? Para estos casos, `observeEvent()` solamente registra el primer disparador, lo que implica que cuando cambiemos de dirección (suponiendo que ya habíamos establecido una dirección previamente), los gráficos/tablas se van a actualizar sin que presionemos el boton por segunda vez, caso contrario a `bindEvent()`, ya que este último exige que hagamos click en el disparador en cada ocasión, lo que lo vuelve una mejor opción para este caso particular.\n\nFinalmente, el código completo de nuestro servidor será:\n\n```{r, eval=FALSE}\nfunction(input, output, session){\n  \n  output$globo <- render_globe({\n    create_globe(height = \"100vh\") |> \n      globe_background(color = \"#2B2B2B\") |> \n      globe_img_url(image_url(\"blue-marble\")) |> \n      globe_pov(\n        altitude = 2.3,\n        0.6345,\n        -80.5528\n      )\n  })\n  \n  observeEvent(input$temp_find, {\n    globe_proxy(\"globo\") |> \n      globe_pov(\n        altitude = 2.3,\n        input$lat,\n        input$lng\n      ) \n  })\n  \n  output$map <- renderMapboxer({\n    mapboxer(\n      style = \"mapbox://styles/mapbox/dark-v11\",\n      token = \"XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX\",\n      center = c(-99,22),\n      zoom = 4.5,\n      maxZoom = 9\n    ) \n  })\n  \n  \n  coordenadas <- reactive({\n    \n    dir <- geocode(location = input$direction)\n    dir\n  })\n  \n  observeEvent(input$direction,{\n    req(coordenadas())\n    \n    updateNumericInput(session, \"lat\", value = coordenadas()$lat)\n    updateNumericInput(session, \"lng\", value = coordenadas()$lon)\n    \n  })\n  \n  temperaturas_data <- reactive({\n    solicitud <- weather_forecast_req(lat = input$lat, \n                                      lon = input$lng, \n                                      tibble_format = T)\n    \n    x <- \n      tibble(\n        solicitud$dt, solicitud$main$temp,\n        solicitud$main$temp_min,\n        solicitud$main$temp_max,\n        solicitud |> \n          select(weather) |> \n          tidyr::unnest() |> \n          select(icon)) |> \n      setNames(c(\"Datetime\", \"Temp\", \"Min\", \"Max\", \"Icon\")) |> \n      mutate(\n        Icon = paste0(\n          \"http://openweathermap.org/img/w/\",Icon,\".png\"\n        ),\n        Datetime = strftime(Datetime, format = \"%A, %I %p\"),\n        Min = Min -273.15,\n        Max = Max -273.15,\n        Temp = Temp -273.15,\n      )\n    x\n  })\n  \n  output$tabla_temperaturas <- renderReactable({\n    req(temperaturas_data())\n    \n    temperaturas_data() |> \n      reactable(\n        theme = reactablefmtr::nytimes(centered = TRUE, \n                                       header_font_size = 12,\n                                       font_color = \"#fff\",\n                                       background_color = \"transparent\"),\n        defaultColDef = colDef(headerStyle = list(background = \"#2B2B2B\", \n                                                  color = \"#fff\"),\n                               vAlign = \"center\"),\n        compact = TRUE,\n        language = reactableLang(\n          noData = \"No data found\",\n          pageInfo = \"{rowStart}\\u2013{rowEnd} of {rows} pages\",\n          pagePrevious = \"\\u276e\",\n          pageNext = \"\\u276f\",\n        ),\n        bordered = F,\n        onClick = \"select\",\n        #selection = \"single\",\n        pagination = F,\n        height = 390,\n        columns = list(\n          Datetime = colDef(name = \"Dia/hora\",minWidth = 60),\n          Icon = colDef(\n            minWidth = 25,\n            header = icon(\"cloud-sun\"),\n            cell = function(value) {\n              image <- img(src = value, style = \"height: 24px;\", alt = value)\n              tagList(\n                div(style = \"display: inline-block; width: 45px;\", image)\n              )\n            }\n          ),\n          Temp = colDef(\n            minWidth = 60,\n            header = icon(\"temperature-low\"),\n            cell = function(value, index){\n              Min <- temperaturas_data()$Min[index]\n              Max <- temperaturas_data()$Max[index]\n              div(\n                div(style = \"font-weight: 600\", value, \"°\"),\n                div(style = \"font-size: 0.75rem\", \n                    paste0(\"Min: \",Min,\"°, Max: \", Max,\"°\"))\n              )\n            }\n          ),\n          Min = colDef(show = F),\n          Max = colDef(show = F)\n        )\n      )\n  }) |> \n    bindEvent(input$temp_find)\n  \n  output$temperatura_actual <- renderText({\n    x <- weather_actual_req(lat = input$lat, \n                            lon = input$lng, \n                            tibble_format = T)\n    paste0(\"Temperatura actual: \", x$main.temp-273.15,\"°\")\n    \n  }) |> \n    bindEvent(input$temp_find)\n  \n  \n  output$grafico_temperatura <- renderEcharts4r({\n    \n    req(temperaturas_data())\n    x <- temperaturas_data()\n    \n    temperaturas_data() |> \n      e_charts(Datetime) |> \n      e_bar(Temp, symbol = \"none\", smooth = F, \n             name = \"Temperatura\") |> \n      e_theme(\"auritus\") |> \n      e_color(color = \"darkred\") |> \n      e_tooltip(trigger = \"axis\") |> \n      e_y_axis(show = F) |> \n      e_title(\" \", left = \"center\") |> \n      e_legend(right = 0, textStyle = list(color = \"#fff\"))\n    \n  }) |> \n    bindEvent(input$temp_find)\n  \n  bounds <- reactive({\n    x <- c(input$lng,input$lat, input$lng+0.1, input$lat+0.1)\n    x\n  })\n\n  observeEvent(input$temp_find,{\n    req(bounds())\n    \n    mapboxer_proxy(\"map\") |>\n      fit_bounds(bounds()) |>\n      update_mapboxer()\n  })\n}\n```\n\nY así, con este código tan sencillo, completamos el desarrollo de nuestra app para visualizar el clima. Finalmente podemos cerrar este primer proyecto que fue tanto técnico como creativo. A través del desarrollo de esta aplicación Shiny para visualizar el clima, no solo pusimos en práctica conceptos fundamentales de R, manipulación de datos y visualización interactiva, sino que también dimos un paso hacia la construcción de herramientas que pueden tener un impacto real.\n\nEste no es solo un conjunto de gráficos y tablas; es una ventana dinámica al estado del mundo, un ejemplo de cómo los datos pueden volverse accesibles, comprensibles y, sobre todo, útiles. Desde consumir una API externa, hasta transformar los datos y presentarlos en una interfaz amigable, este proyecto resume muchos de los aprendizajes recorridos a lo largo del libro.\n\nEl resultado final de nuestra aplicación es:\n\n![Shiny App para visualizar el clima](imagenes/app%20clima%20final.png){fig-align=\"center\" width=\"500\"}\n\nComo regalo adicional, podrás encontrar el proyecto completo en [el siguiente enlace](https://github.com/Jorge-hercas/clima-hoy), junto con una versión online de la app.\n\n#### Dashboard de ventas\n\nComo siguiente y último proyecto de este libro, vamos a crear un dashboard de ventas estratégico, es decir, un dashboard enfocado en hacer un análisis de alto nivel para el largo plazo. Este tipo de tablero está dirigido principalmente a tomadores de decisiones y busca brindar una visión general del rendimiento comercial, identificando tendencias, oportunidades y posibles riesgos que puedan influir en la estrategia de negocio. Además del análisis histórico, incluiremos una sección de pronóstico para proyectar el comportamiento futuro de las ventas y así facilitar una mejor planificación a mediano y largo plazo, analizando estacionalidad, tendencia, etc. Para este proyecto en particular vamos a usar el archivo ventas_proyecto_final [@kyanyoga_sales_data], disponible en [el repositorio oficial del libro](https://github.com/Jorge-hercas/r-para-inteligencia-de-negocios/blob/main/CSV/ventas_proyecto_final.csv). Este archivo contiene datos de ventas simulados, lo que nos permitirá practicar con métricas clave como ingresos totales, crecimiento mensual, participación por categoría, y otras métricas adicionales que iremos construyendo a lo largo del desarrollo del proyecto.\n\nComo primer paso lógico, antes de comenzar a construir el dashboard, vamos a leer los datos para entender qué información tenemos y qué alcance podemos tener con este set de datos:\n\n```{r, eval=FALSE}\nsales_data <- vroom::vroom(\"https://raw.githubusercontent.com/Jorge-hercas/\n                           r-para-inteligencia-de-negocios/main/CSV/\n                           ventas_proyecto_final.csv\")\n\nsales_data\n```\n\n```{r, echo=FALSE}\nsales_data <- vroom::vroom(\"https://raw.githubusercontent.com/Jorge-hercas/r-para-inteligencia-de-negocios/main/CSV/ventas_proyecto_final.csv\")\n\nsales_data\n```\n\nComo primera aproximación, podemos notar que se trata de una tabla transaccional, es decir, un tipo de tabla que registra eventos o transacciones individuales a medida que ocurren. En este caso, cada fila representa una orden de venta, con información detallada como la fecha de la transacción (ORDERDATE), la cantidad de producto por cada orden, el precio, estatus de la orden, región, etc. La información de dirección que tenemos es referente al cliente (Customer), sin embargo, esta información se limita únicamente a direcciones como cadenas de texto, por lo que no nos sirve para crear un mapa referente. En este caso, podemos crear una tabla catálogo que contenga la información de las direcciones disponibles, pero para eso, vamos a averiguar que tan completa se encuentra esta información, sacando un cálculo rápido de los valores nulos y la cantidad de valores únicos:\n\n```{r}\nlibrary(dplyr)\nlibrary(purrr)\nlibrary(tidyr)\nlibrary(ggmap)\n\nsales_data |> \n  dplyr::select(ADDRESSLINE1, ADDRESSLINE2, CITY, \n         STATE, POSTALCODE, COUNTRY, TERRITORY) |> \n  summarise(across(everything(), list(\n    Unicos = ~n_distinct(.),\n    Nulos = ~sum(is.na(.))\n  ), .names = \"{.col}_{.fn}\")) |> \n  pivot_longer(\n    cols = everything(),\n    names_to = c(\"Columna\", \"Calculo\"),\n    names_sep = \"_\",\n    values_to = \"valor\"\n  ) \n```\n\nEn primera instancia, podemos notar que las columnas `ADDRESSLINE1`, `CITY` y `COUNTRY` no tienen valores nulos, por lo que podemos basarnos en ellas para hacer nuestro análisis geográfico, sin embargo, `ADDRESSLINE1` tiene 92 valores únicos, `CITY` 73 y `COUNTRY` 19, por lo que tendremos que decidir a qué nivel geográfico vamos a calcular las latitudes y longitudes. Para efectos de este proyecto vamos a calcular dos niveles: 1. País y 2. Dirección. Vamos a generar las latitudes/longitudes respectivas para cada caso, para después guardar ese catálogo en un archivo separado que después retomaremos.\n\n```{r, eval=FALSE}\n# Google maps API KEY\nregister_google(\"xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\")\n\n# Catálogo de direcciones únicas\ncatalogo_direcciones <- \n  sales_data |> \n  select(ADDRESSLINE1, CITY, COUNTRY) |> \n  mutate(\n    Direccion_str = paste(ADDRESSLINE1, CITY, COUNTRY, sep = \", \")\n  ) |> \n  distinct()\n\n# Obtención de latitudes/longitudes\ncatalogo_direcciones <-\n  catalogo_direcciones |> \n  mutate_geocode(\n    Direccion_str\n  ) |> \n  rename(\n    address_lat = lat,\n    address_lng = lon\n  ) |> \n  left_join(\n    catalogo_direcciones |> \n      select(COUNTRY) |> \n      distinct() |> \n      mutate_geocode(\n        COUNTRY\n      ) |> \n      rename(\n        country_lat = lat,\n        country_lng = lon\n      ),\n    by = c(\"COUNTRY\" = \"COUNTRY\")\n  )\n\n# Guardado de catálogo en un objeto\nwrite.csv(catalogo_direcciones, file = \"direcciones.csv\")\n```\n\nAhora, en un contexto real, en el que posiblemente (muy seguramente, en realidad) se van a añadir nuevas ubicaciones al mapa en un medio a largo plazo, tener un archivo local asociado al dashboard tiene poco sentido y, en realidad, es una mala práctica cuando queremos llevar nuestros reportes a producción. Lo ideal en este caso sería generar un flujo de trabajo en, por ejemplo, Github Actions, que periódicamente actualice este catálogo y sobreescriba la tabla en una base datos en la nube. Si tuviéramos por ejemplo una conexión a Snowflake, nuestro flujo podría cambiar, eliminando la función `write.csv()` y añadiendo:\n\n```{r, eval=FALSE}\n# Conexión a Snowflake\ncon <- dbConnect(\n  odbc::snowflake(),\n  account = Sys.getenv(\"SNOWFLAKE_ACCOUNT\"),\n  uid = Sys.getenv(\"SNOWFLAKE_USER\"),\n  pwd = Sys.getenv(\"SNOWFLAKE_PASSWORD\"),\n  warehouse = Sys.getenv(\"SNOWFLAKE_WAREHOUSE\"),\n  database = \"MI_DATABASE\",\n  schema = \"MI_SCHEMA\",\n  role = Sys.getenv(\"SNOWFLAKE_ROLE\")\n)\n\n# Reescribir tabla completa en Snowflake\ndbWriteTable(\n  conn = con,\n  name = DBI::Id(schema = \"MI_SCHEMA\", \n                 table = \"DIRECCIONES\"),\n  value = catalogo_direcciones,\n  overwrite = TRUE,\n  row.names = FALSE\n)\n\n# Cerrar conexión\ndbDisconnect(con)\n```\n\nDe tal forma que ya tendríamos un flujo que mantiene las zonas referentes actualizadas. Para efectos de practicidad de este ejercicio final vamos a mantener únicamente nuestro archivo local, ya que estamos trabajando con dataset preestablecido, por lo que no necesitamos automatizar nada en este momento.\n\nUna vez preparados los datos, comenzaremos a diseñar las vistas del dashboard. El objetivo es construir un tablero de alto nivel que proporcione información general de forma clara y rápida para facilitar la toma de decisiones estratégicas, por lo que para lograrlo, estructuraremos la interfaz en cuatro niveles analíticos:\n\n1.  Indicadores Clave de Rendimiento (KPIs)\n\nComo encabezado, implementaremos tarjetas de información con métricas clave: ventas totales, volumen de órdenes, clientes únicos y ticket promedio. Aunque estos indicadores son fundamentales, por sí solos carecen de contexto temporal o comparativo.\n\n2.  Contexto Temporal y Tendencias\n\nPara solucionar la falta de contexto de los KPIs, incluiremos un gráfico de series de tiempo que muestre la tendencia de las ventas a nivel mensual. Esto nos permitirá evaluar la evolución del negocio en el mediano plazo e identificar incrementos o decrementos estacionales.\n\n3.  Apertura Geográfica y Filtros Dinámicos\n\nUna tendencia general positiva podría estar sesgada por el éxito de una región específica, ocultando caídas en otras zonas. Para mitigar esto, integraremos un mapa interactivo que visualice las principales zonas de venta, complementado con filtros regionales. Así, el usuario podrá aislar y analizar la tendencia particular de cada localidad.\n\n4.  Perfil Comercial y Eficiencia Operativa\n\nPara responder de manera constante a preguntas críticas del negocio como \"¿Cuál es la categoría más vendida?\" o \"¿Quiénes son nuestros principales compradores?\", añadiremos tablas de clasificación (top) de productos y clientes. Esta segmentación es vital para enfocar campañas de marketing y priorizar la distribución. Finalmente, incorporaremos una visualización del estatus de los pedidos (ciclo de vida del envío). Esto permitirá identificar cuellos de botella operativos y servirá como base para futuros análisis de optimización o maximización lineal en la cadena de entregas.\n\nConceptualmente, podemos plantear el siguiente diseño:\n\n![Dashboard: Tendencias generales](imagenes/conceptodashboard.pdf)\n\nEsta interfaz solamente es la primer página del reporte, la cual tendrá información y métricas generales, sin embargo, vamos a implementar una segunda pestaña enfocada en un pronóstico de series de tiempo mediante un modelo *prophet*, ya que estos datos nos favorecen para este modelo en particular (no voy a repasar el proceso de validación de y estadística descriptiva de los datos para el modelo, por lo que quedará como ejercicio para el lector). Para esta página, vamos a simplificar bastante las visualizaciones, ya que será un aspecto del dasboard que el negocio también podría ver, razón por la que debemos mantenerlo al márgen de la simplicidad suficiente para que puedan comprenderlo de forma muy intuitiva. Específicamente, vamos a dividir la página en:\n\n1.  Pronóstico general\n\nUna tendencia general más los valores pronosticados para *x* meses que el usuario va a poder definir, lo que permitirá analizar tendencias esperadas y poder planear estrategias basadas en escenarios futuros.\n\n2.  Tendencia y descomposición\n\nPara esto, únicamente vamos a incluir la tendencia esperada del pronóstico, así como una descomposición de este mismo mediante una tabla, lo que permitirá que quien únicamente desee enfocarse en el pronóstico simple, simplemente se enfocará en el gráfico principal, pero si quieren ir más al detalle en ello, entonces podrán desplazarse hacia abajo, haciendo que la página sea más amigable a la vista.\n\nLa estructura conceptual de esta página será:\n\n![Dashboard: Pronóstico](imagenes/conceptoforecast.pdf)\n\nAhora, comencemos por establecer los archivos que vamos a necesitar: `global.R`, `ui.R`, `server.R`, `body.R`, `data.R`, `controlbar.R` y `sidebar.R`. Nótese que estoy separando el cuerpo de la aplicación, el *sidebar* y el *controlbar* en diferentes archivos para mantener un proyecto limpio, aunque no está modularizado como tal, ya que es un dashboard relativamente pequeño, razón por la que no hace falta convertirlo a su misma versión mediante módulos (aunque te dejaré el código con esta versión al final de este ejercicio). La estructura general de nuestro dashboard será:\n\n```{r, echo=FALSE}\ncat(\"\nSales dashboard\n│\n│\n│\n├── global.R\n│\n├── ui.R\n├── server.R\n│\n├── data.R\n├── catalogo_direcciones.R\n│\n├── sidebar.R\n├── body.R\n├── controlbar.R\n│\n├── data/\n│   └── direcciones.csv\n│\n└── www/\n    └── recursos estáticos\n        ├── imágenes\n        └── logos\n\")\n```\n\n##### La interfaz\n\nComencemos por el archivo `global.R`, en el cuál únicamente cargaremos librerías y llamaremos a los demás archivos relacionados:\n\n```{r, eval=FALSE}\nlibrary(shiny)\nlibrary(shinyjs)    \nlibrary(bs4Dash)\nlibrary(echarts4r)\nlibrary(reactable)\nlibrary(reactablefmtr)\nlibrary(mapboxer)\nlibrary(dplyr)\nlibrary(dtplyr, warn.conflicts = FALSE)\nlibrary(purrr)\nlibrary(tidyr)\nlibrary(lubridate)\nlibrary(prophet)\nlibrary(scales)\nlibrary(RColorBrewer)\nlibrary(htmlwidgets)\nlibrary(shinyWidgets)\nlibrary(openxlsx)\nlibrary(shinymanager)\n\n# Options\nopts <- list(\n  `actions-box` = TRUE,\n  `live-search`=TRUE)\n\nsource(\"data.R\")\nsource(\"sidebar.R\")\nsource(\"controlbar.R\")\nsource(\"body.R\")\n```\n\nDonde `opts` se refiere a las opciones globales aplicables a los filtros que usaremos con `shinyWidgets`.\n\nComo siguiente archivo, vamos con la interfaz `ui.R` de nuestro dashboard, en la cual diseñaremos nuestro dashboard mediante `dashboardPage()`, pero llamando a cada uno de sus componentes de los archivos separados (`sidebar.R`, `controlbar.R`, `body.R`), lo que mantendrá esta interfaz relativamente pequeña en cuestión de código que mantener:\n\n```{r, eval=FALSE}\nfrontend <- \n  dashboardPage(\n  header = dashboardHeader(\n    sidebarIcon = icon(\"home\"),\n    controlbarIcon = icon(\"bars\"),\n    status = \"navy\",\n    fixed = T\n  ),\n  footer = dashboardFooter(\n    left  = \"R-conomics\",\n    right = paste0(\"@Jorge Hernández, \", year(today()))\n  ),\n  sidebar = sidebar,\n  controlbar = controlbar,\n  body = body\n)\n```\n\nComo puedes ver, los argumentos `sidebar`, `controlbar` y `body` son objetos que llamamos de otros archivos, lo que mantiene muy simplificada la interfaz.\n\nVamos ahora con la siguiente parte simple: El menú `sidebar.R`. En este caso, únicamente vamos a establecer un encabezado para nuestro dashboard, un logotipo (opcional, pero siempre recomendable) y las páginas que incluye nuestro reporte:\n\n```{r, eval=FALSE}\nsidebar <-\n  dashboardSidebar(\n    status = \"navy\",\n    minified = T,\n    elevation = 4,\n    fixed = T,\n    skin = \"light\",\n    bs4SidebarUserPanel(\"Sales dashboard\",\n                        image = \"shitpost.webp\"),\n    sidebarMenu(\n      id = \"menu\",\n      menuItem(\n        text = \"General Numbers\",\n        tabName = \"general\",\n        icon = icon(\"coins\")\n      ),\n      menuItem(\n        text = \"Forecast\",\n        tabName = \"forecast\",\n        icon = icon(\"chart-line\")\n      )\n    )\n  )\n```\n\nNada complicado aquí, ya que solo es una estructura en forma de lista, la cual podemos escalar sin mayores dificultades.\n\nRespecto a la barra que contiene todos los filtros, es decir, el `controlbar.R`, vamos dividirla en dos pestañas: `general` y `forecast`:\n\n```{r, eval=FALSE}\ncontrolbar <-\n  dashboardControlbar(\n    width = \"350px\",\n    skin = \"light\",\n    pinned = FALSE,\n    collapsed = TRUE,\n    overlay = TRUE,\n    controlbarMenu(\n      id = \"controlbar_menu\",\n      type = \"tabs\",\n      # Filtros generales\n      controlbarItem(\n        title = \"Filtros\",\n        icon  = icon(\"filter\"),\n        tags$div(\n          style = \"padding: 10px;\",\n          tags$h6(\n            tags$i(class = \"fas fa-calendar-alt me-1\"),\n            \"Período de análisis\",\n            style = \"font-weight:600; margin-bottom:8px;\"\n          ),\n          airDatepickerInput(\n            inputId = \"fecha_rango\",\n            range = T,\n            multiple = T,\n            value = c(min(sales_data$ORDERDATE) - 1, \n                      max(sales_data$ORDERDATE) + 1),\n            minDate = min(sales_data$ORDERDATE) - 1,\n            maxDate = max(sales_data$ORDERDATE) + 1,\n            inline = T\n          ),\n          tags$hr(),\n          tags$h6(\n            tags$i(class = \"fas fa-boxes me-1\"),\n            \"Línea de producto\",\n            style = \"font-weight:600; margin-bottom:8px;\"\n          ),\n          uiOutput(\"ui_productline\"),\n          tags$hr(),\n          tags$h6(\n            tags$i(class = \"fas fa-globe-americas me-1\"),\n            \"País\",\n            style = \"font-weight:600; margin-bottom:8px;\"\n          ),\n          uiOutput(\"ui_country\"),\n          tags$hr(),\n          tags$h6(\n            tags$i(class = \"fas fa-city me-1\"),\n            \"Estado del pedido\",\n            style = \"font-weight:600; margin-bottom:8px;\"\n          ),\n          uiOutput(\"ui_status\"),\n          tags$hr(),\n          actionButton(\n            \"btn_reset\",\n            \"Restablecer filtros\",\n            icon  = icon(\"rotate-left\"),\n            class = \"btn-outline-secondary btn-sm w-100\"\n          )\n        )\n      ),\n      # Filtros del forecast\n      controlbarItem(\n        title = \"Forecast\",\n        icon  = icon(\"chart-line\"),\n        tags$div(\n          style = \"padding: 10px;\",\n          tags$h6(\n            tags$i(class = \"fas fa-forward me-1\"),\n            \"Horizonte de pronóstico\",\n            style = \"font-weight:600; margin-bottom:8px;\"\n          ),\n          sliderInput(\n            inputId = \"forecast_periods\",\n            label = \"Meses a proyectar\",\n            min = 3,\n            max = 36,\n            value = 12,\n            step = 3,\n            ticks = TRUE\n          )\n        )\n      )\n    )\n  )\n```\n\nNótese que aplicamos el argumento `inline = T` en el selector de fechas para poder mantenerlo fijo, ya que infortunadamente este *input* suele tener un bug en el que, si utilizamos ventanas desplegables en el dashboard, entonces este se cierra automáticamente al dar click en un valor, lo que lo vuelve molesto de utilizar. Por otra parte, como puedes notar, la sección del pronóstico únicamente tiene un *slider* para indicar el horizonte de datos que queremos ver, esto debido a que quiero mantener una visión muy simple para el negocio, ya que selectores más avanzados respecto al pronóstico pueden resultar algo confusos para el usuario final.\n\nContinuemos con el cuerpo de nuestra app, específicamente en el archivo `body.R`, el cual va a contener las visualizaciones en cuestión. Para ello, como recordarás, tenemos que englobar nuestras páginas en un contenedor `tabItems()`, y a su vez, en un subcontenedor `tabItem()`, el cual llamaremos a partir de un `tabName`, con el que podremos ligar esa página al nombre en el menú del `sidebar`. Para la hoja de métricas generales:\n\n```{r, eval=FALSE}\ngeneral_metrics_page <-\n  tabItem(\n      tabName = \"general\",\n      fluidRow(\n        bs4ValueBoxOutput(\"vbox_ventas\",   width = 3),\n        bs4ValueBoxOutput(\"vbox_ordenes\",  width = 3),\n        bs4ValueBoxOutput(\"vbox_clientes\", width = 3),\n        bs4ValueBoxOutput(\"vbox_ticket\",   width = 3)\n      ),\n      fluidRow(\n        column(\n          width = 12,\n          style = \"display:flex; justify-content:center; \n          padding-bottom:4px;\",\n          radioButtons(\n            inputId = \"granularidad_mapa\",\n            label = NULL,\n            choices = c(\"País\" = \"pais\", \"Ciudad\" = \"ciudad\"),\n            selected = \"pais\",\n            inline = TRUE\n          )\n        )\n      ),\n      fluidRow(\n        bs4Card(\n          title = \"Ventas mensuales\",\n          width = 12,\n          maximizable = TRUE,\n          status = \"navy\",\n          echarts4rOutput(\"chart_monthly\", height = \"380px\")\n        )\n      ),\n      fluidRow(\n        bs4Card(\n          title = \"Concentración geográfica de ventas\",\n          width = 8,\n          maximizable = TRUE,\n          status = \"navy\",\n          mapboxerOutput(\"mapa_ventas\", height = \"380px\")\n        ),\n        bs4Card(\n          title = \"Distribución por estatus\",\n          width = 4,\n          maximizable = TRUE,\n          status = \"navy\",\n          echarts4rOutput(\"chart_status\", height = \"380px\")\n        )\n      ),\n      fluidRow(\n        bs4Card(\n          title = \"Ventas por línea de producto\",\n          width = 6,\n          maximizable = TRUE,\n          status = \"navy\",\n          reactableOutput(\"tbl_productline\")\n        ),\n        bs4Card(\n          title = \"Ventas por cliente (Top)\",\n          width = 6,\n          maximizable = TRUE,\n          status = \"navy\",\n          reactableOutput(\"tbl_customer\")\n        )\n      ),\n      fluidRow(\n        bs4Card(\n          title = \"Ventas por región\",\n          width = 12,\n          maximizable = TRUE,\n          status = \"navy\",\n          echarts4rOutput(\"chart_region\", height = \"320px\")\n        )\n      )\n    )\n```\n\nPor su parte, para la hoja referente al pronóstico:\n\n```{r, eval=FALSE}\nforecast_page <-\n  # Forecast\n  tabItem(\n    tabName = \"forecast\",\n    fluidRow(\n      column(\n        width = 12,\n        style = \"display:flex; justify-content:flex-end; padding-bottom:8px;\",\n        downloadButton(\n          outputId = \"download_forecast\",\n          label = \"Descargar forecast\",\n          class = \"btn btn-primary btn-sm\"\n        )\n      )\n    ),\n    fluidRow(\n      bs4Card(\n        title = \"Pronóstico de ventas (Prophet)\",\n        width = 12,\n        maximizable = TRUE,\n        status = \"navy\",\n        echarts4rOutput(\"chart_forecast\", height = \"420px\")\n      )\n    ),\n    fluidRow(\n      bs4Card(\n        title = \"Tendencia\",\n        width = 4,\n        maximizable = TRUE,\n        status = \"navy\",\n        echarts4rOutput(\"chart_trend\", height = \"280px\")\n      ),\n      bs4Card(\n        title = \"Descomposición\",\n        width = 8,\n        maximizable = TRUE,\n        status = \"navy\",\n        reactableOutput(\"chart_descomp\", height = \"280px\")\n      )\n    )\n  )\n```\n\nY, ambos objetos, van a ir en:\n\n```{r, eval=FALSE}\nbody <- dashboardBody(\n  useShinyjs(),\n  tabItems(\n    # General\n    general_metrics_page,\n    # Forecast\n    forecast_page\n  )\n)\n```\n\n##### El Server\n\nVeamos ahora la parte interesante: El archivo `server.R`. Comencemos con una customización condicional sencilla, ya que `dashboardPage()` nos permite utilizar un modo obscuro, sin embargo, los gráficos y tablas no están ligadas por default a este modo, por lo que tendremos que modificar manualmente las paletas de colores. Para identificar el modo actual de nuestro dashboard, únicamente necesitamos llamar a `input$dark_mode`, por lo tanto, para una paleta de colores acorde, dependiendo del estado:\n\n```{r, eval=FALSE}\nes_dark <- reactive({ isTRUE(input$dark_mode) })\n  \n# Tema para echarts\ntema_e <- reactive({ if (es_dark()) \"dark\" else \"infographic\" })\n  \n# Paleta de texto para reactable\ncolor_texto <- reactive({ if (es_dark()) \"#e0e0e0\" else \"#111111\" })\ncolor_header <- reactive({ if (es_dark()) \"#1a2a4a\"  else \"#081e3d\" })\ncolor_bg <- reactive({ if (es_dark()) \"#1e1e2e\"  else \"transparent\" })\ncolor_bg_strip <- reactive({ if (es_dark()) \"#16213e\"  else \"#f9f9f9\" })\ncolor_borde <- reactive({ if (es_dark()) \"#2e3a5a\"  else \"#eeeeee\" })\n  \n# Helper para reactable\ntema_reactable <- reactive({\n  reactableTheme(\n    backgroundColor = color_bg(),\n    borderColor = color_borde(),\n    stripedColor = color_bg_strip(),\n    color = color_texto(),\n    searchInputStyle = list(background = color_bg()),\n    pageButtonStyle = list(color = color_texto())\n  )\n})\n  \n# Colores de fondo\ntooltip_dark_bg <- reactive({\n  if (es_dark())\n    list(backgroundColor = \"#1e1e2e\", borderColor = \"#3a4a6a\",\n         textStyle = list(color = \"#e0e0e0\"))\n  else\n    list(backgroundColor = \"#fff\")\n})\n```\n\nAvanzando ahora con los filtros, como recordarás, para la página de métricas generales no los incluí directamente en el archivo `controlbar.R` (con excepción de `airDatepickerInput()`), si no que creamos un `renderUI()` para poder generar estos códigos directamente en el archivo `server.R`. Quisiera decir que hay una razón muy importante por la cual lo hice de esta manera, pero realmente no, simplemente fue por una cuestión de practicidad, ya que así no tengo que reescribir los valores del *input* dos veces, es decir:\n\n```{r, eval=FALSE}\n# Filtros\noutput$ui_productline <- renderUI({\n    vals <- sort(unique(sales_data$PRODUCTLINE))\n    pickerInput(\"productline\", choices = vals, selected = vals,\n                options = opts, multiple = TRUE)\n})\n  \noutput$ui_country <- renderUI({\n    vals <- sort(unique(sales_data$COUNTRY.x))\n    pickerInput(\"country\", choices = vals, selected = vals,\n                options = opts, multiple = TRUE)\n})\n  \noutput$ui_status <- renderUI({\n    vals <- sort(unique(sales_data$STATUS))\n    pickerInput(\"status_filter\", choices = vals, selected = vals,\n                options = opts, multiple = TRUE)\n})\n  \n# Reset de filtros\nobserveEvent(input$btn_reset, {\n    updatePickerInput(session, \"productline\", \n                      selected = unique(sales_data$PRODUCTLINE))\n    updatePickerInput(session, \"country\", \n                      selected = unique(sales_data$COUNTRY.x))\n    updatePickerInput(session, \"status_filter\", \n                      selected = unique(sales_data$STATUS))\n})\n```\n\nDonde, en lugar de escribir los valores a utilizar dos veces, tanto para `choices` como para `selected`, solo tengo que asignar un objeto `vals <- sort(unique(sales_data$PRODUCTLINE))`, por ejemplo (y aplicarlo para cada caso). Claro que podría asignar estos valores desde un inicio en la sección `global.R` o en `data.R`, guardarlos en la memoria global y simplemente llamarlos, sin embargo, me parece una opción más práctica el englobarlos dentro del filtro que estamos renderizando, ya que de esta manera es más fácil identificar qué valores estamos llamando.\n\nAhora, vamos a generar un objeto dinámico para nuestros datos, el cual tendrá los filtros respectivos aplicados, para que simlemente podamos reutilizarlo después para todas las demás visualizaciones:\n\n```{r, eval=FALSE}\ndatos_filtrados <- reactive({\n    req(input$fecha_rango)\n    \n    df <- sales_data |>\n      filter(\n        ORDERDATE >= input$fecha_rango[1],\n        ORDERDATE <= input$fecha_rango[2]\n      )\n    \n    if (!is.null(input$productline))\n      df <- df |> filter(PRODUCTLINE %in% input$productline)\n    \n    if (!is.null(input$country) )\n      df <- df |> filter(COUNTRY.x %in% input$country)\n    \n    if (!is.null(input$status_filter))\n      df <- df |> filter(STATUS %in% input$status_filter)\n    \n    df\n})\n```\n\nComo puedes darte cuenta, establecimos una condición en caso de que los filtros todavía no tengan ningún valor asignado. Esto es importante porque, cuando una aplicación de Shiny que comienza a hacerse más pesada está cargando, puede ocurrir que algunos *inputs* aún no existan o todavía no hayan sido inicializados, razón por la que, antes de aplicar cada filtro, verificamos que el valor del input no sea `NULL`. De hecho, en algunas versiones de JavaScript, al tener un *controlbar* oculto, los objetos dentro de él (los filtros) no se cargarán hasta que explícitamente lo abramos (cosas que he aprendido a la mala desarrollando estos dashboards en el pasado), por lo que estas condiciones evitan que el dashboard se inicie con visualizaciones vacías por default.\n\nPasando a las visualizaciones de la página principal, comencemos por generar las cajas de texto simples:\n\n```{r, eval=FALSE}\noutput$vbox_ventas <- renderbs4ValueBox({\n    total <- sum(datos_filtrados()$SALES, na.rm = TRUE)\n    bs4ValueBox(\n      value = scales::dollar(total, big.mark = \",\", prefix = \"$\"),\n      subtitle = \"Ventas totales\",\n      icon = icon(\"dollar-sign\"),\n      color = \"navy\",\n      width = 12\n    )\n})\n  \noutput$vbox_ordenes <- renderbs4ValueBox({\n    n <- n_distinct(datos_filtrados()$ORDERNUMBER)\n    bs4ValueBox(\n      value = scales::comma(n),\n      subtitle = \"Órdenes\",\n      icon = icon(\"receipt\"),\n      color = \"info\",\n      width = 12\n    )\n})\n  \noutput$vbox_clientes <- renderbs4ValueBox({\n    n <- n_distinct(datos_filtrados()$CUSTOMERNAME)\n    bs4ValueBox(\n      value = scales::comma(n),\n      subtitle = \"Clientes únicos\",\n      icon = icon(\"users\"),\n      color = \"success\",\n      width = 12\n    )\n})\n  \noutput$vbox_ticket <- renderbs4ValueBox({\n    total  <- sum(datos_filtrados()$SALES, na.rm = TRUE)\n    orders <- n_distinct(datos_filtrados()$ORDERNUMBER)\n    ticket <- if (orders > 0) total / orders else 0\n    bs4ValueBox(\n      value = scales::dollar(ticket, big.mark = \",\", prefix = \"$\"),\n      subtitle = \"Ticket promedio\",\n      icon = icon(\"tag\"),\n      color = \"warning\",\n      width = 12\n    )\n})\n```\n\ncomo puedes ver, realmente son muy sencillas, ya que únicamente tomamos el objeto reactivo `datos_filtrados()` previamente filtrado y hacemos el cálculo que necesitamos para cada caso.\n\nVamos ahora a desarrollar el primer gráfico de series de tiempo:\n\n```{r, eval=FALSE}\noutput$chart_monthly <- renderEcharts4r({\n    paleta <- if (es_dark()) \"Blues\" else \"YlGn\"\n    \n    datos_filtrados() |>\n      mutate(ORDERDATE_round = floor_date(ORDERDATE, \"months\")) |>\n      group_by(ORDERDATE_round) |>\n      summarise(TOTAL_SALES = sum(SALES, na.rm = TRUE), .groups = \"drop\") |>\n      mutate(\n        color = scales::col_quantile(paleta, TOTAL_SALES)(TOTAL_SALES)\n      ) |>\n      e_charts(ORDERDATE_round, dispose = FALSE) |>\n      e_bar(TOTAL_SALES, name = \"Ventas\") |>\n      e_add(\"itemStyle\", color) |>\n      e_tooltip(\n        trigger = \"axis\",\n        backgroundColor = tooltip_dark_bg()$backgroundColor,\n        borderColor = tooltip_dark_bg()$borderColor,\n        textStyle = tooltip_dark_bg()$textStyle,\n        formatter = htmlwidgets::JS(\"\n          function(params) {\n            var d = new Date(params[0].value[0]);\n            var mes = d.toLocaleDateString('es-MX', \n            {year:'numeric', month:'short'});\n            var val = '$' + params[0].value[1].toLocaleString('es-MX', \n            {maximumFractionDigits: 0});\n            return mes + '<br/>' + params[0].marker + \n            ' Ventas: <b>' + val + '</b>';\n          }\n        \")\n      ) |>\n      e_x_axis(\n        type = \"time\",\n        axisLabel = list(color = if (es_dark()) \"#c0c0c0\" else NULL)\n      ) |>\n      e_y_axis(\n        axisLabel = list(\n          color = if (es_dark()) \"#c0c0c0\" else NULL,\n          formatter = htmlwidgets::JS(\"function(v){ return '$' \n                                      + (v/1000).toFixed(0) + 'k'; }\")\n        )\n      ) |>\n      e_theme(tema_e()) |>\n      e_toolbox_feature(\"dataZoom\") |>\n      e_toolbox_feature(\"restore\") |>\n      e_grid(backgroundColor = \"transparent\")\n})\n```\n\nDonde únicamente agrupamos a nivel mensual y calculamos la suma de ventas totales a partir del objeto reactivo `datos_filtrados()`. Como puedes notar, utilizamos una función de JavaScript para customizar el *tooltip* de nuestro gráfico, lo que nos permitirá tener un control más claro de la información que queremos retornar cuando pasamos el puntero sobre alguna barra. De igual forma, aplicamos una función de JavaScript al eje *y* de nuestro gráfico, únicamente para mostrar valores en miles, lo que lo hace más facilmente legible para el público en general.\n\nContinuemos con el siguiente gráfico, en el que mostraremos la suma total del monto de `SALES`, pero separado por estatus de la venta. La mejor forma de representar este tipo de métricas, en las que se supone que debe haber una disparidad importante en los valores (por ejemplo, teniendo un 98% de ventas cerradas y el resto de categorías distribuidas en el 2% restante) suele ser mediante gráficos de *pie* o *dona*. Vamos a utilizar un gráfico de *dona* para este caso (porque se me hace más agradable visualmente que el de pastel):\n\n```{r, eval=FALSE}\noutput$chart_status <- renderEcharts4r({\n    datos_filtrados() |>\n      group_by(STATUS) |>\n      summarise(total = sum(SALES, na.rm = TRUE), .groups = \"drop\") |>\n      e_charts(STATUS, dispose = FALSE) |>\n      e_pie(\n        total,\n        radius = c(\"50%\", \"70%\"),\n        name = \"Ventas\",\n        label = list(\n          color = if (es_dark()) \"#e0e0e0\" else NULL\n        ),\n        labelLine = list(\n          lineStyle = list(color = if (es_dark()) \"#888\" else NULL)\n        )\n      ) |>\n      e_tooltip(\n        backgroundColor = tooltip_dark_bg()$backgroundColor,\n        borderColor = tooltip_dark_bg()$borderColor,\n        textStyle = tooltip_dark_bg()$textStyle,\n        formatter = htmlwidgets::JS(\"\n          function(params){\n            return params.name + '<br/>' +\n                   params.marker + ' $' +\n                   params.value.toLocaleString('es-MX', \n                   {maximumFractionDigits:0}) +\n                   ' (' + params.percent + '%)';\n          }\n        \")\n      ) |>\n      e_legend(\n        bottom = 0,\n        textStyle = list(color = if (es_dark()) \"#e0e0e0\" else \"#000\")\n      ) |>\n      e_theme(tema_e())\n})\n```\n\nEn este caso, aplicamos una función de JavaScript simplemente para formatear el tooltip, mostrando el valor y su respectivo valor porcentual referente al total de todos los estatus que tenemos.\n\nPasemos ahora al mapa en cuestión, que de hecho es de los códigos que más se me complicaron, ya que lo dividí en varias partes. Primero, este mapa podrá ser visualizado a nivel país y nivel ciudad, por lo que podemos definir una función para cambiar la granularidad de los datos:\n\n```{r, eval=FALSE}\nagregar_mapa <- function(df, granularidad) {\n    group_cols <- if (granularidad == \"ciudad\")\n      c(\"Direccion_str\", \"address_lng\", \"address_lat\")\n    else\n      c(\"COUNTRY.y\", \"country_lng\", \"country_lat\")\n    \n    df |>\n      group_by(across(all_of(group_cols))) |>\n      summarise(total = sum(SALES, na.rm = TRUE), .groups = \"drop\") |>\n      mutate(radio = (total / sum(total)) * 100) |>\n      setNames(c(\"Zone\", \"lng\", \"lat\", \"total\", \"radio\")) |>\n      filter(!is.na(lng), !is.na(lat))\n}\n```\n\nAhora, vamos a definir la capa inicial del mapa. Para efectos de visualización, previamente preparé dos temas acorde al estado de la aplicación (si se encuentra o no en modo oscuro) en el sitio oficial de [mapbox](https://www.mapbox.com), por lo que, en tu caso particular, será necesario que desarrolles tus propios temas en este sitio y uses tu propia API Key. Veamos el código:\n\n```{r, eval=FALSE}\noutput$mapa_ventas <- renderMapboxer({\n    datos_iniciales <- agregar_mapa(sales_data, \"pais\")\n    \n    style <- ifelse(\n      isTRUE(input$dark_mode),\n      \"mapbox://styles/mapbox/dark-v11\",\n      \"mapbox://styles/jorgehdez1998/clhsaghny00ys01qn6f27870h\"\n    )\n    \n    datos_iniciales |>\n      as_mapbox_source() |>\n      mapboxer(\n        center = c(0, 20),\n        maxZoom = 7,\n        zoom = 1.5,\n        token = \"xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\",\n        style = style\n      ) |>\n      add_circle_layer(\n        circle_color = \"red\",\n        circle_radius = c(\"get\", \"radio\"),\n        circle_opacity = 0.7\n      )\n})\n```\n\nPor defecto, en la capa inicial los datos se van a visualizar a nivel país sin filtrar. Lógicamente, debemos ahora establecer datos dinámicos en un objeto reactivo, así como definir el área que vamos a dibujar, dependiendo del filtro de países seleccioandos:\n\n```{r, eval=FALSE}\ndatos_mapa <- reactive({\n    req(input$granularidad_mapa)\n    agregar_mapa(datos_filtrados(), input$granularidad_mapa)\n})\n  \nbounds_mapa <- reactive({\n    df <- datos_mapa()\n    req(nrow(df) > 0)\n    c(\n      min(df$lng, na.rm = TRUE),\n      min(df$lat, na.rm = TRUE),\n      max(df$lng, na.rm = TRUE),\n      max(df$lat, na.rm = TRUE)\n    )\n})\n```\n\nFinalmente, aplicamos la capa dinámica del gráfico:\n\n```{r, eval=FALSE}\nobserveEvent(datos_mapa(), {\n    df <- datos_mapa()\n    req(nrow(df) > 0)\n    \n    mapboxer_proxy(\"mapa_ventas\") |>\n      set_data(data = df) |>\n      fit_bounds(bounds_mapa()) |>\n      update_mapboxer()\n}, ignoreInit = TRUE)\n```\n\nPasemos ahora a las tres visualizaciones más sencillas: Las tablas de resumen por cliente/producto y el gráfico de barras por estado/ciudad. En el caso de las tablas, bastará con que agrupemos los datos dinámicos por cada nivel de granularidad y generemos la tabla en cuestión. Para la tabla a nivel producto:\n\n```{r, eval=FALSE}\noutput$tbl_productline <- renderReactable({\n    x <- datos_filtrados() |>\n      group_by(PRODUCTLINE) |>\n      summarise(total = sum(SALES, na.rm = TRUE), .groups = \"drop\") |>\n      arrange(desc(total))\n    \n    # Paleta de barras: tonos cálidos en light, neones fríos en dark\n    pal_barras <- if (es_dark())\n      c(\"#1a3a5c\",\"#1e5f8a\",\"#2180b8\",\"#3fa0d4\",\"#6abde6\",\"#9dd4f0\",\"#c5e8fa\")\n    else\n      RColorBrewer::brewer.pal(9, \"Reds\")\n    \n    x |> \n      reactable(\n      theme  = tema_reactable(),\n      defaultPageSize = 10,\n      striped = TRUE,\n      highlight = TRUE,\n      defaultColDef = colDef(\n        headerStyle = list(\n          background = color_header(),\n          color = \"#ffffff\",\n          fontWeight = \"600\",\n          fontSize = \"12px\"\n        ),\n        style = list(\n          color = color_texto(),\n          fontSize = \"12px\"\n        )\n      ),\n      columns = list(\n        PRODUCTLINE = colDef(\n          name = \"Categoría de producto\",\n          style = list(fontSize = \"12px\")\n        ),\n        total = colDef(\n          name = \"Ventas totales\",\n          align = \"left\",\n          minWidth = 180,\n          format = colFormat(digits = 0),\n          style = list(fontSize = \"12px\"),\n          cell = data_bars(\n            data = x,\n            fill_color = pal_barras,\n            background = \"transparent\",\n            bar_height = 7,\n            number_fmt = scales::comma,\n            text_position = \"outside-end\",\n            round_edges = TRUE,\n            text_color = color_texto()\n          )\n        )\n      )\n    )\n})\n```\n\nPor su parte, para la tabla a nivel cliente:\n\n```{r, eval=FALSE}\noutput$tbl_customer <- renderReactable({\n    x <- datos_filtrados() |>\n      group_by(CUSTOMERNAME) |>\n      summarise(total = sum(SALES, na.rm = TRUE), .groups = \"drop\") |>\n      arrange(desc(total))\n    \n    pal_barras <- if (es_dark())\n      c(\"#2d1b5c\",\"#4a2d8a\",\"#6a3fb8\",\"#8a5cd4\",\"#aa80e6\",\"#c8a8f0\",\"#e0ccfa\")\n    else\n      RColorBrewer::brewer.pal(9, \"Blues\")\n    \n    x |> \n      reactable(\n      theme = tema_reactable(),\n      pagination = FALSE,\n      height = 350,\n      striped = TRUE,\n      highlight = TRUE,\n      defaultColDef = colDef(\n        headerStyle = list(\n          background = color_header(),\n          color = \"#ffffff\",\n          fontWeight = \"600\",\n          fontSize = \"12px\"\n        ),\n        style = list(\n          color = color_texto(),\n          fontSize = \"12px\"\n        )\n      ),\n      columns = list(\n        CUSTOMERNAME = colDef(\n          name = \"Cliente\",\n          style = list(fontSize = \"12px\")\n        ),\n        total = colDef(\n          name = \"Ventas totales\",\n          align = \"left\",\n          minWidth = 180,\n          format = colFormat(digits = 0),\n          style = list(fontSize = \"12px\"),\n          cell = data_bars(\n            data = x,\n            fill_color = pal_barras,\n            background = \"transparent\",\n            bar_height = 7,\n            number_fmt = scales::comma,\n            text_position  = \"outside-end\",\n            round_edges = TRUE,\n            text_color = color_texto()\n          )\n        )\n      )\n    )\n})\n```\n\nFinalmente, para el gráfico del total de ventas por ciudad/país (específicamente, un top 15), únicamente debemos definir el nivel de granularidad y aplicarla dinámicamente mediante `group_by_at()`:\n\n```{r, eval=FALSE}\noutput$chart_region <- renderEcharts4r({\n    group_var <- if (input$granularidad_mapa == \"ciudad\") \n      \"CITY.x\" else \"COUNTRY.x\"\n    \n    datos_filtrados() |>\n      group_by_at(group_var) |>\n      summarise(total = sum(SALES, na.rm = TRUE), .groups = \"drop\") |>\n      arrange(desc(total)) |>\n      slice_head(n = 15) |>\n      e_charts_(group_var, dispose = FALSE) |>\n      e_bar(total, name = \"Ventas\") |>\n      e_x_axis(\n        axisLabel = list(\n          color = if (es_dark()) \"#c0c0c0\" else NULL\n        )\n      ) |>\n      e_tooltip(\n        trigger = \"axis\",\n        backgroundColor = tooltip_dark_bg()$backgroundColor,\n        borderColor = tooltip_dark_bg()$borderColor,\n        textStyle = tooltip_dark_bg()$textStyle,\n        formatter = htmlwidgets::JS(\"\n      function(params) {\n        var p = params[0];\n        var ciudad = p.axisValue || p.name;\n        var valor = Array.isArray(p.value) ? p.value[1] : p.value;\n        var val = '$' + Number(valor).toLocaleString('es-MX', {\n          maximumFractionDigits: 0\n        });\n        return ciudad + '<br/>' +\n               p.marker + ' Ventas: <b>' + val + '</b>';\n      }\n    \")\n      ) |>\n      e_theme(tema_e())\n})\n```\n\nComo puedes ver, la función de JavaScript únicamente nos ayuda a customizar el *tooltip*, para que nos diga el valor en un formato de moneda y el país/ciudad en cuestión.\n\nCon esto, terminamos la primera página de nuestro dashboard, la cual tendrá el siguiente aspecto (@fig-dashboardfinal).\n\n::: {#fig-dashboardfinal layout-ncol=\"2\"}\n![Original](imagenes/dashboardnormal.png){#fig-normaldash}\n\n![Modo noche](imagenes/dashboardnoche.png){#fig-nochedash}\n\nPágina principal del dashboard de ventas\n:::\n\nContinuemos ahora con la sección referente al pronóstico. Como mencioné previamente, por ahora vamos a obviar las pruebas de validación previas al modelo `prophet` para simplificar esta parte, ya que, de hecho, en el punto en el que decidimos llevar un pronóstico a un ambiente de producción, como lo es un dashboard en la nube, es porque todas las validaciones previas ya fueron llevadas a cabo. Ahora bien, comencemos por establecer el modelo en un objeto reactivo, llamando a los datos filtrados:\n\n```{r, eval=FALSE}\nmodelo_reactive <- reactive({\n    \n    sales_ts <- datos_filtrados() |>  \n      mutate(ORDERDATE_round = floor_date(ORDERDATE, \"month\")) |> \n      group_by(ORDERDATE_round) |> \n      summarise(total = sum(SALES, na.rm = TRUE), .groups = \"drop\") |> \n      arrange(ORDERDATE_round)\n    \n    # columnas para Prophet\n    prophet_data <- sales_ts |> \n      transmute(\n        ds = as.Date(ORDERDATE_round),\n        y = total\n      )\n    \n    prophet_data\n})\n  \n  \nforecast_reactive <- reactive({\n    \n    # Modelo Prophet\n    modelo_prophet <- prophet(\n      modelo_reactive(),\n      yearly.seasonality = TRUE,\n      weekly.seasonality = FALSE,\n      daily.seasonality = FALSE,\n      seasonality.mode = \"multiplicative\",\n      changepoint.prior.scale = 0.05,\n      seasonality.prior.scale = 10\n    )\n    \n    # Pronóstico a 12 meses\n    future <- make_future_dataframe(\n      modelo_prophet,\n      periods = if (is.null(input$forecast_periods)) 12 \n      else input$forecast_periods,\n      freq = \"month\",\n      include_history = TRUE\n    )\n    \n    forecast_prophet <- predict(modelo_prophet, future)\n    \n    forecast_prophet\n    \n})\n```\n\nNótese que establecemos un periodo de 12 meses por default, en caso de que no se detecte el *input*, si es que el *controlbar* no se ha desplegado en ese punto.\n\nComo puedes ver, el objeto `modelo_reactive()` nos devuelve los datos en el formato que necesitamos, mientras que el objeto `forecast_reactive()` genera el modelo en cuestión. A partir de estos objetos, podemos generar el gráfico referente al pronóstico:\n\n```{r, eval=FALSE}\noutput$chart_forecast <- renderEcharts4r({\n    \n    historico <- modelo_reactive()\n    forecast <- forecast_reactive()\n    max_fecha <- max(historico$ds, na.rm = TRUE)\n    \n    plot_data <- forecast |> \n      select(ds, yhat, yhat_lower, yhat_upper) |> \n      left_join(historico, by = \"ds\") |> \n      mutate(\n        `Point Forecast` = if_else(ds >= max_fecha, yhat, NA_real_),\n        `Lower 95%` = if_else(ds >= max_fecha, yhat_lower, NA_real_),\n        `Upper 95%` = if_else(ds >= max_fecha, yhat_upper, NA_real_)\n      )\n    \n    plot_data |> \n      e_charts(ds, dispose = FALSE) |> \n      e_line(\n        y, name = \"Ventas reales\", symbol = \"none\") |> \n      e_line(`Point Forecast`, name = \"Pronóstico\", symbol = \"none\",\n        lineStyle = list(width = 3)) |> \n      e_line(`Lower 95%`, name = \"Límite inferior 95%\",\n        symbol = \"none\", lineStyle = list(type = \"dashed\")) |> \n      e_line(`Upper 95%`, name = \"Límite superior 95%\",\n        symbol = \"none\", lineStyle = list(type = \"dashed\")) |> \n      e_tooltip(\n        trigger = \"axis\",\n        backgroundColor = tooltip_dark_bg()$backgroundColor,\n        borderColor = tooltip_dark_bg()$borderColor,\n        textStyle = tooltip_dark_bg()$textStyle,\n        formatter = htmlwidgets::JS(\"\n        function(params) {\n          var d = new Date(params[0].value[0]);\n          var mes = d.toLocaleDateString('es-MX', {\n            year: 'numeric',\n            month: 'short'\n          });\n          var html = mes + '<br/>';\n          params.forEach(function(p) {\n            var val = p.value[1];\n            if (val !== null && val !== undefined && !isNaN(val)) {\n              var val_fmt = '$' + Number(val).toLocaleString('es-MX', {\n                maximumFractionDigits: 0\n              });\n              html += p.marker + ' ' + p.seriesName + ': <b>' \n              + val_fmt + '</b><br/>';\n            } }); return html;}\")\n      ) |> \n      e_x_axis(\n        type = \"time\",\n        axisLabel = list(\n          color = if (es_dark()) \"#c0c0c0\" else \"#000000\"\n        )\n      ) |> \n      e_y_axis(\n        axisLabel = list(\n          color = if (es_dark()) \"#c0c0c0\" else \"#000000\",\n          formatter = htmlwidgets::JS(\"\n          function(v){\n            return '$' + (v / 1000).toFixed(0) + 'k';\n          }\"))) |> \n      e_legend(\n        textStyle = list(color = if (es_dark()) \"#c0c0c0\" else \"#000000\")) |> \n      e_theme(tema_e()) |> \n      e_toolbox_feature(\"dataZoom\") |> \n      e_toolbox_feature(\"restore\") |> \n      e_grid(backgroundColor = \"transparent\")\n})\n```\n\nY agregamos algunas customizaciones sencillas con JavaScript, para formatear el eje *y* y el *tooltip*.\n\nPasemos ahora a la tendencia del pronóstico, la cual podemos obtener mediante la columna `trend`, de nuestro objeto `forecast_reactive()`, por lo tanto, el gráfico final:\n\n```{r, eval=FALSE}\noutput$chart_trend <- renderEcharts4r({\n  \n    forecast <- forecast_reactive()\n    \n    plot_data <- forecast |> \n      mutate(\n        `Tendencia` = trend,\n        `Límite inferior 95%` = if (\"trend_lower\" %in% \n                                    names(forecast)) \n          trend_lower else NA_real_,\n        `Límite superior 95%` = if (\"trend_upper\" \n                                    %in% names(forecast)) \n          trend_upper else NA_real_\n      ) |> \n      select(ds, `Tendencia`, `Límite inferior 95%`, `Límite superior 95%`)\n    \n    plot_data |> \n      e_charts(ds, dispose = FALSE) |> \n      e_line(`Tendencia`, name = \"Tendencia\", symbol = \"none\",\n        lineStyle = list(width = 3)) |> \n      e_line(`Límite inferior 95%`, name = \"Límite inferior 95%\",\n        symbol = \"none\", lineStyle = list(type = \"dashed\")) |> \n      e_line(`Límite superior 95%`, name = \"Límite superior 95%\",\n        symbol = \"none\", lineStyle = list(type = \"dashed\")) |> \n      e_tooltip(\n        trigger = \"axis\",\n        backgroundColor = tooltip_dark_bg()$backgroundColor,\n        borderColor = tooltip_dark_bg()$borderColor,\n        textStyle = tooltip_dark_bg()$textStyle,\n        formatter = htmlwidgets::JS(\"\n        function(params) {\n          var d = new Date(params[0].value[0]);\n          var mes = d.toLocaleDateString('es-MX', {\n            year: 'numeric',\n            month: 'short'\n          });\n          var html = mes + '<br/>';\n          params.forEach(function(p) {\n            var val = p.value[1];\n            if (val !== null && val !== undefined && !isNaN(val)) {\n              var val_fmt = '$' + Number(val).toLocaleString('es-MX', {\n                maximumFractionDigits: 0});\n              html += p.marker + ' ' + p.seriesName + ': <b>' \n                + val_fmt + '</b><br/>';} });\n          return html;}\")) |> \n      e_x_axis(type = \"time\",\n        axisLabel = list(\n          color = if (es_dark()) \"#c0c0c0\" else \"#000000\"\n        )) |> \n      e_y_axis(\n        axisLabel = list(\n          color = if (es_dark()) \"#c0c0c0\" else \"#000000\",\n          formatter = htmlwidgets::JS(\"\n          function(v){\n            return '$' + (v / 1000).toFixed(0) + 'k';}\"))) |> \n      e_legend(\n        textStyle = list(\n          color = if (es_dark()) \"#c0c0c0\" else \"#000000\")) |> \n      e_theme(tema_e()) |> \n      e_toolbox_feature(\"dataZoom\") |> \n      e_toolbox_feature(\"restore\") |> \n      e_grid(backgroundColor = \"transparent\")\n    \n})\n```\n\nEn este punto ya está de más explicar para qué sirve el código JavaScript en `e_tooltip()`, ya que es bastante trivial.\n\nComo puedes notar, el gráfico únicamente toma en cuenta los valores pronosticados, ya que estamos analizando el componente individual de este mismo. Realmente podríamos extraer el componente de tendencia de los datos históricos, sin embargo, para efectos de esta vista, únicamente nos interesa visualizar el pronóstico.\n\nComo último apoyo visual, vamos a generar una tabla con los componentes que generamos de nuestro pronóstico, así como los valores históricos. Aunque parezca redundante, considerando el gráfico general referente al pronóstico, estamos agregando esta tabla porque no todo el público se suele acomodar con series de tiempo. Usualmente, hay usuarios que prefieren ver los números de forma explícita en tablas, razón por la que este recurso nos permite complementar la interpretación visual con una lectura más directa de los valores. Realmente la tabla funciona como un segundo nivel de consulta, ya que, mientras el gráfico nos ayuda a identificar tendencias, patrones, etc, la tabla nos permitirá revisar con mayor precisión los valores observados. La tabla en realidad es bastante sencilla de definir. Comencemos englobando los datos en un objeto reactivo, ya que los vamos a reutilizar más adelante:\n\n```{r, eval=FALSE}\ntabla_reactive <- reactive({\n    \n    historico <- modelo_reactive()\n    forecast <- forecast_reactive()\n    max_fecha <- max(historico$ds, na.rm = TRUE)\n    \n    x <- forecast |> \n      left_join(historico, by = \"ds\") |> \n      mutate(\n        tipo = if_else(ds <= max_fecha, \"Histórico\", \"Pronóstico\"),\n        fecha = format(ds, \"%Y-%m\"),\n        `Ventas reales` = y,\n        `Pronóstico` = yhat,\n        `Límite inferior 95%` = yhat_lower,\n        `Límite superior 95%` = yhat_upper,\n        `Tendencia` = trend,\n        `Tendencia inferior` = if (\"trend_lower\" %in% names(forecast)) \n          trend_lower else NA,\n        `Tendencia superior` = if (\"trend_upper\" %in% names(forecast)) \n          trend_upper else NA,\n        `Estacionalidad anual` = if (\"yearly\" %in% names(forecast)) \n          yearly else NA,\n        `Términos aditivos` = if (\"additive_terms\" %in% names(forecast)) \n          additive_terms else NA,\n        `Términos multiplicativos` = if (\"multiplicative_terms\" \n                                         %in% names(forecast)) \n          multiplicative_terms else NA\n      ) |> \n      select(fecha, tipo, `Ventas reales`, `Pronóstico`,\n             `Límite inferior 95%`, `Límite superior 95%`, `Tendencia`,\n             `Tendencia inferior`, `Tendencia superior`,`Estacionalidad anual`,\n             `Términos aditivos`, `Términos multiplicativos`) |> \n      arrange(fecha)\n    \n    x\n})\n```\n\nAhora, generamos la tabla en cuestión:\n\n```{r, eval=FALSE}\noutput$chart_descomp <- renderReactable({\n    \n    fmt_money <- colFormat(prefix = \"$\", separators = TRUE, digits = 0)\n    fmt_pct <- colFormat(percent = TRUE, digits = 2)\n    \n    cols_money <- c(\"Ventas reales\", \"Pronóstico\", \"Límite inferior 95%\", \n                    \"Límite superior 95%\", \"Tendencia\", \"Tendencia inferior\", \n                    \"Tendencia superior\", \"Términos aditivos\")\n    \n    cols_pct <- c(\"Estacionalidad anual\", \"Términos multiplicativos\")\n    \n    tabla_reactive() |> \n      reactable(\n        theme = tema_reactable(), defaultPageSize = 12, searchable = FALSE,\n        striped = TRUE, highlight = TRUE,\n        defaultColDef = colDef(\n          minWidth = 130,\n          headerStyle = list(background = color_header(), \n                             color = \"#ffffff\", fontWeight = \"600\", \n                             fontSize = \"12px\"),\n          style = list(color = color_texto(), fontSize = \"12px\")),\n        columns = c(\n          list(fecha = colDef(name = \"Mes\", minWidth = 95),\n               tipo = colDef(name = \"Tipo\", minWidth = 100)),\n          setNames(lapply(cols_money, \\(z) colDef(align = \"right\", \n                                                  format = fmt_money)), \n                   cols_money),\n          setNames(lapply(cols_pct, \\(z) colDef(align = \"right\", \n                                                format = fmt_pct)), \n                   cols_pct)\n        )\n      )\n})\n```\n\nNótese que usamos funciones `lapply()` para poder aplicar el tema definido de forma más eficiente. Nótese que simplificamos bastante el código de la tabla aquí, ya que tenemos múltiples columnas y, francamente, me da bastante pereza el tener que definir cada columna por separado, así como lo hicimos en las tablas de la página previa (aunque en aquellos casos cada columna tenía una definición diferente).\n\nComo último paso, vamos a habilitar el botón de descarga para los datos del pronóstico, porque muy seguramente cuando decidas presentar un proyecto similar, alguien del negocio te preguntará: *¿puedo exportar esto a excel?*. Realmente odio esa pregunta, pero así es esto.\n\nComo recordarás, en este capítulo discutimos el uso de la librería `openxlsx` para generar archivos excel, por lo que vamos a definir el siguiente archivo para descarga:\n\n```{r, eval=FALSE}\noutput$download_forecast <- downloadHandler(\n  filename = function() paste0(\"componentes_forecast_\", Sys.Date(), \".xlsx\"),\n  content = function(file) {\n    x <- tabla_reactive()\n    n_rows <- nrow(x); n_cols <- ncol(x)\n\n    cols_money <- c(\"Ventas reales\", \"Pronóstico\", \"Límite inferior 95%\", \n                    \"Límite superior 95%\",\"Tendencia\", \"Tendencia inferior\", \n                    \"Tendencia superior\", \"Términos aditivos\")\n    cols_pct <- c(\"Estacionalidad anual\", \"Términos multiplicativos\")\n\n    money_cols <- which(names(x) %in% cols_money)\n    pct_cols   <- which(names(x) %in% cols_pct)\n    date_cols  <- which(names(x) == \"fecha\")\n\n    wb <- createWorkbook(creator = \"Shiny App\")\n    addWorksheet(wb, \"Forecast\", gridLines = FALSE)\n\n    title_style <- createStyle(fontSize = 16, textDecoration = \"bold\", \n                               fontColour = color_header(), halign = \"left\")\n    subtitle_style <- createStyle(fontSize = 10, fontColour = \"#666666\", \n                                  textDecoration = \"italic\")\n    header_style <- createStyle(fontSize = 12, fontColour = \"#FFFFFF\", \n                                fgFill = color_header(),\n                                textDecoration = \"bold\", halign = \"center\", \n                                valign = \"center\",\n                                border = \"TopBottom\", borderColour = \"#D9D9D9\")\n    body_style  <- createStyle(fontSize = 11, fontColour = \"#333333\", \n                               border = \"bottom\", borderColour = \"#EFEFEF\")\n    money_style <- createStyle(numFmt = '\"$\"#,##0', halign = \"right\")\n    pct_style   <- createStyle(numFmt = \"0.00%\", halign = \"right\")\n    date_style  <- createStyle(numFmt = \"mmm yyyy\", halign = \"left\")\n\n    writeData(wb, \"Forecast\", \"Componentes del pronóstico de ventas\", \n              startRow = 1, startCol = 1, colNames = FALSE)\n    mergeCells(wb, \"Forecast\", cols = 1:n_cols, rows = 1)\n    addStyle(wb, \"Forecast\", title_style, rows = 1, \n             cols = 1:n_cols, gridExpand = TRUE)\n\n    writeData(wb, \"Forecast\", paste(\"Archivo generado el\", Sys.Date()), \n              startRow = 2, startCol = 1, colNames = FALSE)\n    mergeCells(wb, \"Forecast\", cols = 1:n_cols, rows = 2)\n    addStyle(wb, \"Forecast\", subtitle_style, rows = 2, \n             cols = 1:n_cols, gridExpand = TRUE)\n\n    writeData(wb, \"Forecast\", x, startRow = 4, startCol = 1, \n              colNames = TRUE, withFilter = TRUE)\n    addStyle(wb, \"Forecast\", header_style, rows = 4, \n             cols = 1:n_cols, gridExpand = TRUE)\n\n    if (n_rows > 0) {\n      body_rows <- 5:(n_rows + 4)\n\n      addStyle(wb, \"Forecast\", body_style, rows = body_rows, \n               cols = 1:n_cols, gridExpand = TRUE)\n      if (length(money_cols) > 0) addStyle(wb, \"Forecast\", \n                                  money_style, rows = body_rows, \n                                  cols = money_cols, gridExpand = TRUE, \n                                  stack = TRUE)\n      if (length(pct_cols) > 0) addStyle(wb, \"Forecast\", \n                                  pct_style, rows = body_rows, \n                                  cols = pct_cols, gridExpand = TRUE, \n                                  stack = TRUE)\n      if (length(date_cols) > 0 && inherits(x$fecha, \"Date\")) \n        addStyle(wb, \"Forecast\", date_style, rows = body_rows, \n                 cols = date_cols, gridExpand = TRUE, stack = TRUE)\n    }\n    \n    setColWidths(wb, \"Forecast\", cols = 1:n_cols, widths = \"auto\")\n    setRowHeights(wb, \"Forecast\", rows = 1, heights = 28)\n    freezePane(wb, \"Forecast\", firstActiveRow = 5, firstActiveCol = 1)\n\n    saveWorkbook(wb, file = file, overwrite = TRUE)\n  }\n)\n```\n\nOriginalmente planeaba utilizar `openxlsx2` para este ejercicio, pero me encontré con algunos problemas al generar el archivo excel, por lo que regresé a la versión original para evitarme dolores de cabeza.\n\nA pesar de ser un código extenso, realmente es bastante sencillo, ya que únicamente definimos argumentos para customizar las celdas, agregar un encabezado y fijarlo. Nuestra página referente al pronóstico tendrá el siguiente aspecto:\n\n::: {layout-ncol=\"2\"}\n![Original](imagenes/forecastdia.png)\n\n![Modo noche](imagenes/forecastnoche.png)\n\nPronóstico del dashboard de ventas\n:::\n\nEstamos a un paso de terminar este dashboard, pero aún nos falta un pequeño detalle: La seguridad. Supongamos por ejemplo que tenemos estos usuarios:\n\n```{r, eval=FALSE}\n# Usuarios autorizados\nusuarios <- tibble::tibble(\n  user = c(\"admin\", \"analista\"),\n  password = c(\"ejemplo_123\", \"datos_456\"),\n  name = c(\"admin\", \"ventas\"),\n  posicion = c(\"Gerente\", \"Vendedor\")\n)\n```\n\nPara este ejercicio, los vamos a generar mediante un data frame, pero lo más lógico sería obtener esta información a partir de una base de datos con un mayor nivel de seguridad (en ambientes reales). Dados los usuarios, vamos a englobar nuestra interfaz mediante `secure_app()`, función que ya discutimos previamente:\n\n```{r, eval=FALSE}\nsecure_app(frontend,\n           theme = \"cosmo\",\n           background  = \"linear-gradient(rgba(0, 0, 0, 0.5),\n                    rgba(0, 0, 0, 0.5)),\n                    url('https://cdn.pixabay.com/photo/2018/08/21/23/29/\n                    forest-3622519_1280.jpg')\n                    no-repeat center fixed;\n                    -webkit-background-size: cover;\n                    -moz-background-size: cover;\n                    -o-background-size: cover;\n                    background-size: cover;\")\n```\n\n(Si, reciclé el diseño usado en el ejemplo que vimos anteriormente, cuando explicamos este tema).\n\nRespectivamente, para el servidor:\n\n```{r, eval=FALSE}\nautenticacion <- secure_server(\n    check_credentials = check_credentials(usuarios)\n  )\n```\n\nYa que tenemos una capa de contraseñas, ahora podemos añadir un encabezado en nuestro dashboard con el usuario en cuestión. Si regresamos a la sección `x`, en la función `dashboardHeader()` simplemente tenemos que añadir `rightUi = userOutput(\"user\")`, es decir:\n\n```{r, eval=FALSE}\nfrontend <- \ndashboardPage(\n  header = dashboardHeader(\n    sidebarIcon = icon(\"home\"),\n    controlbarIcon = icon(\"bars\"),\n    status = \"navy\",\n    fixed = T,\n    rightUi = userOutput(\"user\")\n  ),\n  footer = dashboardFooter(\n    left  = \"R-conomics\",\n    right = paste0(\"@Jorge Hernández, \", year(today()))\n  ),\n  sidebar = sidebar,\n  controlbar = controlbar,\n  body = body\n)\n```\n\nY, del lado del servidor, definimos al usuario:\n\n```{r, eval=FALSE}\noutput$user <- renderUser({\n    dashboardUser(\n      name = autenticacion$name,\n      image = \"url_de_usuario\",\n      title = autenticacion$posicion\n    )\n})\n```\n\nDe esta forma, concluimos el proyecto final de este libro: Un dashboard completamente funcional, sencillo de entender para el negocio y con los niveles de seguridad requeridos.\n\nComo regalo adicional y final, podrás encontrar el proyecto completo en [el siguiente enlace](https://github.com/Jorge-hercas/ejercicio-dashboard-ventas), junto con una versión online de la app.\n\nCon este proyecto, damos por concluido este libro, cuyo objetivo principal fue mostrar cómo R puede utilizarse como una herramienta integral para la inteligencia de negocios. A lo largo de los distintos capítulos, pasamos por muchísimos dolores de cabeza, análisis, más análisis, manipulación de datos e incluso desarrollo web (a un nivel bastante sencillo).\n\nMás allá de revisar funciones o paquetes de forma aislada, la intención fue construir una visión práctica del uso de R dentro de un flujo real de trabajo: desde la obtención y limpieza de los datos, hasta la generación de productos analíticos que puedan ser consultados por usuarios finales, y parece ser que se logró el objetivo, aunque tú tendrás la opinión final.\n\nMuchas gracias por leer este libro, por dedicar tu tiempo a recorrer estas páginas y, sobre todo, por atreverte a ensuciarte las manos con código.\n\nEl final de este libro es, en realidad, el inicio de tu propio camino en la analítica de datos y el desarrollo de soluciones de negocio. El código que construimos y el dashboard que desplegamos son solo los cimientos, y eso es lo más divertido, porque siempre habrá un proyecto nuevo, un nuevo dataset y un nuevo método a utilizar en tu carrera profesional. Este libro se basó meramente en temas y recursos que yo he utilizado a lo largo de mi carrera profesional, pero lo cierto es que hay un sinfín de temas que aún puedes explorar.\n"},"formats":{"html":{"execute":{"fig-width":7,"fig-height":5,"fig-format":"retina","fig-dpi":96,"df-print":"default","error":false,"eval":true,"cache":null,"freeze":false,"echo":true,"output":true,"warning":true,"include":true,"keep-md":false,"keep-ipynb":false,"ipynb":null,"enabled":null,"daemon":null,"daemon-restart":false,"debug":false,"ipynb-filters":[],"engine":"knitr"},"render":{"keep-tex":false,"keep-source":false,"keep-hidden":false,"prefer-html":false,"output-divs":true,"output-ext":"html","fig-align":"default","fig-pos":null,"fig-env":null,"code-fold":"none","code-overflow":"scroll","code-link":false,"code-line-numbers":false,"code-tools":false,"tbl-colwidths":"auto","merge-includes":true,"latex-auto-mk":true,"latex-auto-install":true,"latex-clean":true,"latex-max-runs":10,"latex-makeindex":"makeindex","latex-makeindex-opts":[],"latex-tlmgr-opts":[],"latex-input-paths":[],"latex-output-dir":null,"link-external-icon":false,"link-external-newwindow":false,"self-contained-math":false,"format-resources":[]},"pandoc":{"standalone":true,"wrap":"none","default-image-extension":"png","to":"html","css":["styles.css"],"output-file":"dashboards.html"},"language":{},"metadata":{"lang":"en","fig-responsive":true,"quarto-version":"1.2.335","bibliography":["references.bib","bib/references.bib","bib/packages.bib"],"editor":"visual","theme":"cosmo"},"extensions":{"book":{"multiFile":true}}},"pdf":{"execute":{"fig-width":5.5,"fig-height":3.5,"fig-format":"pdf","fig-dpi":300,"df-print":"default","error":false,"eval":true,"cache":null,"freeze":false,"echo":true,"output":true,"warning":true,"include":true,"keep-md":false,"keep-ipynb":false,"ipynb":null,"enabled":null,"daemon":null,"daemon-restart":false,"debug":false,"ipynb-filters":[],"engine":"knitr"},"render":{"keep-tex":false,"keep-source":false,"keep-hidden":false,"prefer-html":false,"output-divs":true,"output-ext":"pdf","fig-align":"default","fig-pos":null,"fig-env":null,"code-fold":"none","code-overflow":"scroll","code-link":false,"code-line-numbers":false,"code-tools":false,"tbl-colwidths":true,"merge-includes":true,"latex-auto-mk":true,"latex-auto-install":true,"latex-clean":true,"latex-max-runs":10,"latex-makeindex":"makeindex","latex-makeindex-opts":[],"latex-tlmgr-opts":[],"latex-input-paths":[],"latex-output-dir":null,"link-external-icon":false,"link-external-newwindow":false,"self-contained-math":false,"format-resources":[]},"pandoc":{"pdf-engine":"xelatex","standalone":true,"variables":{"graphics":true,"tables":true},"default-image-extension":"pdf","to":"pdf","output-file":"dashboards.pdf"},"language":{},"metadata":{"block-headings":true,"bibliography":["references.bib","bib/references.bib","bib/packages.bib"],"editor":"visual","documentclass":"scrreprt"},"extensions":{"book":{}}}}}